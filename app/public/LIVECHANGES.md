# Fenster Glazing Live Changes Runbook

Last updated: 2026-09-02

This is the short operational guide for any Codex agent or developer making changes after launch. Read this before touching test or live.

- **THE LIVE MODEL AS OF 12 AUGUST: COOKIES ON BY DEFAULT, AUTOMATED TRAFFIC EXCLUDED, AD CLICKS LOGGED.** Owner instruction, after a full revert earlier the same day. Optional cookies are granted from the first paint and stay on until refused, and the banner says so; `chosen` is recorded but **not** required, so nobody is re-prompted. Crawlers are filtered server-side in `inc/traffic-classification.php` and again at the dashboard ingest, so they no longer become visitors with journeys — **the CUBOT exception in both classifiers is load-bearing**, because that Android brand sends `bot` in its user agent and a naive test deletes real customers. `inc/ad-attribution.php` logs paid clicks without consent via `/wp-json/fenster/v1/ad-click`; the raw click id never leaves WordPress. A refusal erases what was already collected, which matters MORE under this default than under consent-first, because identifiers exist before the banner is answered.
- **Two pieces are deliberately absent and must not be reinstated without the consent default changing with them.** The stale-identifier purge fires when no choice is stored, which under granted-by-default is every visitor who has not answered — it would wipe their journey on every page load and mint a new one each time. And requiring `chosen === true` for validity would invalidate every existing record and re-prompt the whole audience for nothing. **If the default is ever flipped back to off, both come back together.**
- **This is weaker than the ICO's position** that consent must precede non-essential storage and that default-on is not valid consent. It was raised on 11 August, briefly changed, and reverted on the owner's instruction. Do not quietly re-tighten it, and keep the banner and policy copy true to whatever the default actually is.
- **THE SITEGROUND PROXY CACHES BY PATH AND IGNORES THE QUERY STRING, AND THIS BREAKS ANY SERVER-SIDE READ OF A LANDING URL.** Proven on production 2026-08-11: a `?gclid=` value never used before returned `X-Proxy-Cache: HIT`, and adding an unknown parameter did not change it. So on the one request that matters — a paid visitor arriving on a popular product page — **PHP never executes at all**. A `template_redirect` hook does not fire and `nocache_headers()` cannot help, because nothing is running to send it. The ad click is therefore reported by the browser to `/wp-json/fenster/v1/ad-click`, which is REST and not proxy-cached.
- **THE TEST SITE CANNOT REPRODUCE THIS, and that is a standing limitation worth knowing before trusting any test-only verification.** `test.fensterglazing.com` is Basic Auth protected, which disables the proxy cache, so every page there is a cache miss. The server-side version of the ad click recorder passed every check on test and recorded nothing on live. **If a change depends on caching behaviour, it can only be proven on production.**
- **Never render a per-visitor value into the page config.** The same caching makes it a leak, not just a dead value: a reference printed into cacheable HTML is baked into the cached copy and served to every later visitor of that page. `adAttributionRef` was removed from `fenster_enqueue_website_tracking_config()` for exactly this.

## Current Truth

- **LIVE IS `f26681b0`, TAG `live-seo-2026-08-31f`, STILL NOT ON `main`, 2026-08-31.** Thirteenth isolated release, cut from `1f4ad261`. Three theme files. Thirteen guards before, ten re-checked after, deletion list **EMPTY**, dry run **three lines**, backup proven, residual **0**, socket purge `msg:OK`.
- **AN OWNER REVIEW OF ONE PAGE PRODUCED SIX FIXES, AND FIVE OF THEM WERE THINGS THAT READ FINE FROM INSIDE THE BUSINESS.** Owner: *"there's a few things that make sense from our side but not from a consumer."*
- **THE UPGRADES SECTION WAS REWRITTEN A THIRD TIME AND ONLY NOW ADDRESSES ITS AUDIENCE.** Owner: *"it's really about potential upgrades, whether you're replacing the glass anyway or not."* The section had only ever offered upgrades to somebody whose unit had already failed, so anybody wanting integral blinds or privacy glass in windows that work perfectly well was told nothing. It now says so explicitly. The ordering constraint survives as a closing line rather than the headline, because the previous version *"reads like a warning"*.
- **THE PAGE WAS EXPLAINING THE WEBSITE TO THE CUSTOMER.** "What we put glass into" over "If a new unit has to be made, **this is the page**" is information architecture spoken aloud. Nobody arrives knowing which page they are on. Replaced with "What we can reglaze" over "Your frame stays, whatever it is made of", which is the question the four cards actually answer.
- **`Get pricing or book a design chat` IS THE NEW-WINDOWS JOURNEY TALKING.** Owner: *"doesnt make sense this for this page."* Somebody with a blown unit is not designing anything. Scoped to this slug alone; casement and sash still show the original.
- **LIKE FOR LIKE WAS WRITTEN AS IF IT WERE A SINGLE-GLAZING RULE.** Owner: *"this should also cover double and triple?"* It is the rule for every frame on the page, and to a homeowner with a double glazed unit it read as the one card that did not apply to them, on the page they were already on. **Triple confirmed as supplied**, same day.
- **AN HOUR WAS THE CEILING, NOT THE AVERAGE.** Owner: *"it's an hour max for a single unit."* Corrected in all three places it was stated, including the FAQ, which would otherwise have contradicted the page it sits on. The internal "service engineer, sometimes a fitter" distinction was cut on the owner's instruction; the same engineers do both.
- **THE REVIEW SHOWCASE LED WITH FRONT DOOR TESTIMONIALS ON A MISTED-GLASS PAGE.** A `glass` context now re-sorts it, which promoted the two glazing reviews. **A PREFLIGHT CHECK OF THE PLACES API RETURNED A FALSE NEGATIVE** because it tested the wrong constant name: the key IS configured, 5 live reviews at 4.9 from 138. **The real ceiling is that the Places API returns only five reviews and Google chooses them**, and the nine curated fallbacks contain no glass job at all, so the fix is bounded until glass reviews are added to the curated set.
- **A TWO-MINUTE CLIENT TIMEOUT DURING A 453MB BACKUP LOOKED EXACTLY LIKE A FAILED DEPLOY AND WAS NOT ONE.** The local `ssh` was killed after the guard passed and while `tar` was still running; the remote script kept going and completed the rsync. **Checking the live hashes was the only thing that distinguished "it never ran" from "it finished without me".** Never re-run a deploy after a timeout on the assumption it did not land: hash the target first. The post-rsync steps genuinely had not run from this side and were completed separately.
- **VERIFIED ON THE APEX:** all six changes present, `design chat`, `about an hour`, `sometimes a fitter` and `this is the page` all returning **zero**, ten guards unmoved, six routes `200`, zero PHP notices.

- Superseded: **LIVE WAS `1f4ad261`, TAG `live-seo-2026-08-31e`, STILL NOT ON `main`, 2026-08-31.** Twelfth isolated release, cut from `615dcd03`. One theme file. Nine guards, deletion list **EMPTY**, dry run **one line**, backup proven, residual **0**, socket purge `msg:OK`.
- **A SECTION THIS FILE CALLED "THE ONE HONEST UPSELL ON THE PAGE" WAS FALSE IN BOTH HALVES.** It ran "While the glass is being made" over "The one moment it costs almost nothing to change something." Owner: *"this is very misleading and weird."*
- **THE TIMING DESCRIBED A MOMENT THAT DOES NOT EXIST.** The spec is fixed at order; nothing can be changed during manufacture. The heading invited the reader to act at a point they cannot act at.
- **THE SAVING DID NOT EXIST EITHER.** Owner-confirmed: **any change means a new unit**, the choice is made **at order**, there is **no discount** for specifying with the original order, and *"blinds are a big upgrade"*. So "costs almost nothing" and "easiest and cheapest now" were both untrue, on a page whose own header comment forbids writing what is not so.
- **THE TRUE ARGUMENT IS A CONSTRAINT, NOT A SAVING, AND IT IS THE STRONGER ONE.** Choose at order, or buy a second unit later. The section now says that and makes no price claim at all. **Do not reintroduce one.**
- **THE SECTION'S HEADER COMMENT HAD TO GO WITH IT.** It asserted the false rationale as settled fact and called the section honest for exactly the reason it was not, so it would have taught the error to the next reader and outlived any copy fix.
- **THREE FAULTS ON THIS PAGE IN ONE DAY, ALL THE SAME ROOT.** The repair definition, the wordy rewrite, and now this: each was written from what the codebase asserted rather than from how the business works, and each was caught by the owner reading the page rather than by any check available locally.
- **VERIFIED ON THE APEX:** the new section live, all three withdrawn phrases returning **zero**, zero PHP notices.

- Superseded: **LIVE WAS `615dcd03`, TAG `live-seo-2026-08-31d`, STILL NOT ON `main`, 2026-08-31.** Eleventh isolated release, cut from `c3b8164f`. One theme file, one paragraph. Nine guards, deletion list **EMPTY**, dry run **one line**, backup proven, residual **0**, socket purge `msg:OK`.
- **THE FIX FOR THE FALSE CLAIM WAS ITSELF THE PROBLEM.** Owner: *"way too wordy now and unnatural."* The correction ran to **58 words** and spent most of them arguing the point rather than making it, on a page whose own file header records the owner calling three sentences waffle once already. It is **43** now, four short sentences, shorter than the 46 it originally replaced. **Correcting a claim is not licence to defend it in the copy.**
- **THE REAL FIX WAS ASKING.** Owner: *"ask me questions if you dont understand the operation of how we do it."* Four questions produced four facts, and each one kills a distinction the copy might otherwise have reached for: **the split is glass or not**, and for these two pages that is the whole of it; **a combined job is one job**, priced and carried out together; **the same engineers do both**, so who attends is not a difference; and **the `£96` repairs minimum does not apply to glass**, because the cheapest unit is `£170` plus VAT. All four are now in `AI.md`.
- **THE PATTERN ACROSS THIS PAGE IS CONSISTENT AND WORTH NAMING.** Every error here came from inferring how the business runs from the codebase: the ordering claim, the length, and the near-miss of implying different people attend. Structure, schema, links and rendering can be verified locally. **How the work actually happens cannot, and the owner answers in one line what an audit will get wrong at length.**
- **VERIFIED ON THE APEX:** the short paragraph live, the false claim and the wordy version both returning **zero**, zero PHP notices.

- Superseded: **LIVE WAS `c3b8164f`, TAG `live-seo-2026-08-31c`, STILL NOT ON `main`, 2026-08-31.** Tenth isolated release, cut from `47838b36`. One theme file, one paragraph. Deletion list **EMPTY**, dry run **one line**, eleven guards, backup proven, residual **0**, socket purge `msg:OK`.
- **THE PAGE CARRIED A FALSE DEFINITION OF WHAT A REPAIR IS, AND IT WAS MINE.** The boundary paragraph read that a repair is *"what we call it when the fix happens on the visit and nothing has to be ordered"*. Owner: *"this is nonsense, sometimes we have to order parts for a repair."* He is right, and it contradicted its own sibling in the plainest way available: `/window-and-door-repairs/` is built around a **parts wall**, sourcing hardware for systems nobody sells any more, and telling you when a part is genuinely obsolete. The two pages were calling each other liars.
- **THE BOUNDARY IS THE SUBJECT OF THE WORK, NEVER THE LOGISTICS.** The owner's own wording, quoted at the top of `replacement-glazing-v2.php` since the rebuild, is *"new unit must be ordered"*, which is about **the glass being manufactured to the opening**. That got flattened into "nothing has to be ordered" for repairs, which is a different claim and a false one. The corrected copy says a repair puts right the hardware, the alignment or the seals, that parts get ordered for those too, and that what makes it the glazing page is the glass having to be made to measure.
- **THE SAME ERROR WAS IN THE `AI.md` RULE WRITTEN THE DAY BEFORE**, which is the more dangerous half: a wrong rule in the standing rules propagates into every future pass. Corrected there too, with the owner's quote and an explicit instruction never to define a repair by whether something gets ordered.
- **A REVIEWED PARAGRAPH IS NOT A VERIFIED ONE.** This copy shipped in the 2026-08-10 rebuild, survived a full technical-SEO audit of both pages the day before that read the boundary closely enough to write a rule about it, and was caught by the owner reading the page. **Domain claims about how the business works need the owner, not the codebase**, however confidently the codebase states them.
- **VERIFIED ON THE APEX:** the corrected paragraph present, the false claim returning **zero**, three routes `200`, zero PHP notices, and the `£170` starting price from the release below still in place.

- Superseded: **LIVE WAS `47838b36`, TAG `live-seo-2026-08-31b`, STILL NOT ON `main`, 2026-08-31.** Ninth isolated release, cut from `12c7732e`. Owner: *"double glazed units on our windowcad tool start at £170+vat, installed."* Two theme files, both bundles byte-identical, deletion list **EMPTY**, dry run **exactly two lines**, seventeen guards, backup proven, residual **0**, socket purge `msg:OK`.
- **THE PRICE GAP THE RELEASE BELOW COULD NOT CLOSE IS CLOSED.** `/double-glazing-replacement/` now publishes **"Units start at £170 plus VAT, fitted"** in the cost FAQ and in the meta description, on the same reasoning already recorded for the repairs page: a published figure is the one thing in this SERP no competitor snippet offers, and it pre-qualifies the click. It is verifiable by the reader on the same WindowCAD collection the route already links to.
- **THE OLD ANSWER HAD TO GO OR THE PAGE WOULD CONTRADICT ITSELF.** It read *"we price each one rather than publish a range"*. Anything that publishes a figure must sweep for the sentence that promised not to.
- **THE TWO SIBLING PAGES NOW QUOTE ON DIFFERENT VAT BASES, DELIBERATELY.** Repairs publishes `£96 including VAT`; this publishes `£170 plus VAT`. It was drafted inc VAT at £204 and **both bases were put to the owner, who chose plus VAT**. That is an accepted inconsistency recorded in `inc/site-data.php`, not an oversight. **Do not "fix" it by converting one side**; if they are ever aligned it is the owner's call which way and both pages move together.
- **VERIFIED ON THE APEX:** `£170` present once, `£204` **zero**, the withdrawn "publish a range" line **zero**, five valid JSON-LD blocks with the figure carried into the `FAQPage` answer, and four routes `200` with zero PHP notices.

- Superseded: **LIVE WAS `12c7732e`, TAG `live-seo-2026-08-31`, STILL NOT ON `main`, 2026-08-31.** Eighth isolated release on the live line, cut from `873234ed`. Owner: a full technical-SEO brief on `/window-and-door-repairs/` and `/double-glazing-replacement/`. **Checksum live and expect `12c7732e`.** Five theme files, both compiled bundles byte-identical to live's own, deletion list **EMPTY**, dry run **exactly five lines**, seventeen guards before and after, backup proven by extracting `inc/site-data.php`, ver-bearing mtimes unchanged, residual **0**, socket purge `msg:OK`.
- **THE BEST CONTENT ON THE REPAIRS PAGE WAS STRUCTURALLY INVISIBLE, and that was the finding the whole audit turned on.** `repair_services` is a seventeen-item list naming window mechanisms, friction stays, door gearboxes, multi-point locks and euro cylinders, which is precisely the vocabulary the component-level searches are built from. It rendered as one flat `<ul>` under the heading **"What we are called out for"**, so there was nothing for a query about a window hinge or a door gearbox to match. It is now four groups under four `H3`s: Window repairs, Door repairs, Locks and security, Glass and sealed units. **No item was added, removed or reworded** and the count was asserted at 17 before and after. Those headings are also the first place on the page the phrases "window repairs" and "door repairs" appear as nouns rather than inside a sentence.
- **NO CSS CHANGED, AND THAT WAS A DESIGN CONSTRAINT RATHER THAN LUCK.** Each group repeats the existing `fg-rp-wall__scope` class instead of nesting under a new wrapper, because that class already carries the top border, the spacing and the three-column list with its 2-column and 1-column responsive steps. So the compiled bundles stayed byte-identical to live's and the release needed no rebuild. Do not "tidy" it into modifier classes without rebuilding `main.css`.
- **THE REPLACEMENT GLAZING PAGE NAMED THE SYMPTOM AND NEVER THE SERVICE.** H1 and title were "Misted and Blown Double Glazing"; the phrase **"replacement glass" appeared zero times on a page about replacing glass**, and there was no local signal in either. Google was already rewriting the title in results to "Double Glazing Replacement **Glass in Milton Keynes**", which is the search engine saying out loud what the page should have said itself. H1 is now "Replacement Glass for Misted and Blown Double Glazing", title "Replacement Glass Milton Keynes | Misted Double Glazing" (55 chars, cap 60), description rewritten to 157 (cap 160). **The `Service` schema's `name` and `serviceType` derive from the H1, so entity clarity improved with no schema added.**
- **COVERAGE EXISTED ONLY IN SCHEMA, WHERE NO READER SEES IT.** The towns were in `areaServed` and nowhere in the body. A `Where we work` band now states them in copy, taken from that same list unchanged, and carries the owner-confirmed ten year manufacturer guarantee on the sealed unit. It reuses `fg-rg-band` untouched. **Stated positively throughout, per the standing no-exclusions rule on this page.**
- **ONE SITEWIDE ANCHOR WAS DESCRIBING BOTH SERVICES AS ONE.** The footer's only link here read **"Repairs and replacement units"** and pointed at the *replacement glazing* page, so on all 721 pages the most repeated anchor text for either service led with the wrong word, and the repairs page had **no footer link at all**. Split into "Replacement glass" and "Window and door repairs".
- **CANNIBALISATION WAS REAL AND DELIBERATELY NOT CONSOLIDATED.** `/misted-double-glazing-cloudy-windows/` (766 words, informational, "why windows go cloudy") overlaps the service page's primary term. Both were kept: the blog owns the informational query, and the fix was to sharpen the service page onto commercial intent rather than canonicalise away a page that legitimately ranks. `/replacement-glazing/` is already a clean **301** to the money page and needed nothing.
- **NO PRICE WAS ADDED IN THIS RELEASE, AND IT WAS CLOSED BY THE ONE ABOVE.** Competitors publish figures (£80 to £400 ranges, one at "as little as £120 + VAT"); Fenster publishes `£96 inc VAT` for repairs and **nothing anywhere for replacement units**, including on `/window-door-prices-milton-keynes/`. Inventing one was not an option. **This needs the owner's number and is the highest-value item outstanding on the page.**
- **VERIFIED ON THE APEX WITH NO CACHE-BUSTER:** twelve routes `200` with zero PHP notices, five valid JSON-LD blocks per page and zero invalid, one `H1` each, canonicals correct, the four group headings rendering, the new band and guarantee present, both footer anchors live and the old conflated one returning **zero**, and `page-sitemap.xml` still **721**. Rendered at 1440 and at a true 390 first.
- **A ZSH TRAP COST A ROUND AND IS WORTH KNOWING: `A="-u fenster:Fenster"; curl $A` DOES NOT WORD-SPLIT IN ZSH.** curl received one argument, auth failed, and the test page came back as the 19-byte `401 - Unauthorized` body that `nick.md` already warns reads as a working page. Same family as the `d41d8cd9…` empty-hash trap. Pass credentials literally or run the loop under `bash`.

- Superseded: **LIVE WAS `873234ed`, TAG `live-commercial-2026-08-30g`, STILL NOT ON `main`, 2026-08-30.** Seventh isolated release on the live line, cut from `30e3a543`. Owner: *"yes, do the image fix and push it live"*. **Checksum live and expect `873234ed`.** One theme file, `inc/commercial-product-data.php`, and it was a clean splice again because the blob was identical at `30e3a543` and at `main~1`. Both compiled bundles byte-identical to live's own. Deletion list **EMPTY**, dry run **exactly one line**, thirteen guards before and after, backup `fenster-pre-873234ed-20260830-130651.tar.gz` (453M, 2,162 entries) **proven** by extraction. Ver-bearing mtimes asserted unchanged. Residual dry run **0**. Socket purge `msg:OK`.
- **THE DUPLICATE PHOTOGRAPH FROM THE RELEASE BELOW IS FIXED, AND THE CAUSE IS WORTH KNOWING BEFORE CHOOSING ANY COMMERCIAL IMAGE.** `/office-and-retail-glazing/` was printing `comm-curtain-walling-parade-1600w.jpg` twice: once as its Shopfronts figure and once as the related-products card for `/curtain-walling/`. **That strip is positional, not semantic** — `commercial-product.php:73` takes every commercial route, drops the current one and slices the **first three in declaration order** — so the same three cards, and the same three photographs, sit on **all thirteen** commercial pages. Any figure that reuses one of those three prints twice. Reordering to dodge it would move the strip on all thirteen, so the figure changed instead.
- **SHOPFRONTS TOOK THE HEAL'S ENTRANCE DOOR FRAME and WORKING HOURS TOOK THE OCCUPIED OFFICE LIFT-IN.** The first is a glazed screen with the door in it, which is what the copy describes; the second is the inside of a furnished office lounge while a unit goes in from outside, which is a better subject for a section about keeping a building usable than the courtyard it replaced. It also moved Heal's off a third slot: the page now runs three distinct Heal's frames, two Water End Barn frames and one new project, with **no photograph appearing twice**.
- **BOTH ALTS WERE WRITTEN FROM THE FRAMES, NOT THE FILENAMES**, which is the second time in two days that rule has earned its place. The candidates were opened and two were rejected on what was actually in them: the Green Man frontage is an 1835 brick village pub with timber-look casements, nothing like a shopfront screen, and the Heal's door frame is a courtyard elevation rather than a high street.
- **A RENDER GUARD CAUGHT A SILENTLY WRONG MOBILE CAPTURE.** Headless Chrome clamps `--window-size` at 500px, so the 390 render goes through CDP `Emulation.setDeviceMetricsOverride` — but the extracted fragment carried no `<meta name="viewport">`, so emulation fell back to the **980px** layout viewport and would have produced a confident, wrong mobile screenshot. The assertion `innerWidth === 390` failed the run instead. **Assert the viewport you asked for, not just that a PNG was written.**
- **VERIFIED AS A VISITOR ON THE APEX WITH NO CACHE-BUSTER:** six routes `200` with zero PHP notices and zero V2 markers, both new images `200`, both new alts present, both replaced alts returning **zero**, and the parade shot down from two occurrences to one. Rendered at 1440 and at a true 390 before shipping: layout intact, images filling their containers, chips wrapping, no horizontal scroll.

- Superseded: **LIVE WAS `30e3a543`, TAG `live-commercial-2026-08-30f`, STILL NOT ON `main`, 2026-08-30.** Sixth isolated release on the live line, cut from `10a596a7`. Owner: *"push it live"*. **Checksum live and expect `30e3a543`; it matches no commit on `main`.** Live was established on **six** theme files scored against all five release tags and `origin/main`: `live-configuration-2026-08-30e` / `10a596a7` at **6/6 uniquely**, `origin/main` at **0/6**, and no other tag above 2/6. The empty-input hash guard was in the script, because `d41d8cd9…` has faked a sweep on this project before.
- **THE THEME DIFF BETWEEN THE LIVE TAG AND `main` IS 247 FILES.** That is the whole unapproved stack — composite doors V2, `/why-distinction/`, decorative glass, the quiz and the showroom strand. It is the reason this was cut from live rather than from `main`, and the reason the release branch carries **exactly one** changed theme file.
- **THE SPLICE WAS CLEAN BECAUSE THE FILE WAS IDENTICAL ON BOTH LINES.** `inc/commercial-product-data.php` hashed to `3b723461…` on live and on `main`'s base alike, so `git checkout origin/main -- <that file>` onto a branch cut from `10a596a7` carried the four approved commits and nothing else. `git diff --name-status 10a596a7 30e3a543` returns one line. Both compiled bundles are **byte-identical to live's own** (`main.css` `535e95c1…`, `main.js` `802871e6…`), and the diff contains **zero** `why-distinction|composite-door-styles|fg-cdq|showroom|experimental` tokens.
- **`rsync -a` WOULD HAVE RE-STAMPED EVERY MTIME ON THE THEME AND BUST EVERY `?ver=` ON THE SITE. The documented command has this hazard and it is not theoretical.** The dry run itemised **2,162 lines: one real data transfer and 2,161 `.f..t` mtime-only fix-ups.** The cause is visible in the mtimes — live's whole theme is stamped `2026-08-30` from that morning's deploy, while the server repo carries its original checkout dates back to `2026-07-06` — so `-a` (which implies `-t`) would have dragged every file's mtime *backwards* to the repo's dates. `inc/assets.php` builds the cache-buster from `filemtime()`, so that is a sitewide `?ver=` change on the CSS, the JS, **all eight font faces** and every video, for zero content change. **`--checksum` does not prevent this**; it stops the data transfer and rsync still fixes up the attribute. **Adding `--no-times` collapsed the dry run from 2,162 lines to exactly one.** Deployed with `rsync -a --no-times --checksum --delete`, and the three ver-bearing mtimes were captured before and after and asserted unchanged.
- **THIS IS ALSO THE STRONGEST ISOLATION PROOF THE FILE HAS CARRIED.** `--checksum` makes rsync hash **every** file on both sides before deciding, so the single `>fcst` line is a content-level assertion across the entire 2,162-entry theme that nothing but the one file differs — the check `LIVECHANGES.md` line 192 had to do by hand on eight files, done here on all of them for free.
- **RELEASE MECHANICS.** Guard on **thirteen** files — the one changing plus twelve expected-unchanged, including both compiled bundles and the whole tracking strand (`inc/consent.php`, `inc/website-tracking.php`, `inc/ad-attribution.php`, `inc/traffic-classification.php`, `inc/adminbase.php`, `inc/enquiries.php`) — computed never-typed, asserted in the seconds before the rsync and again after it. Backup `fenster-pre-30e3a543-20260830-122307.tar.gz` (453M, 2,162 entries) **proven** by extracting `inc/commercial-product-data.php` from the tarball and hashing it to the pre-deploy value `3b723461…`. Deletion list asserted **EMPTY**. **1 file written.** Residual dry run **0**. `wp cache flush` Success; socket purge `msg:OK`. Pruned to three (1.4G); disk 86% used, 313G free.
- **VERIFIED AS A VISITOR ON THE APEX WITH NO CACHE-BUSTER:** twelve routes `200` with **zero PHP notices**, every new string present on its route, and the regression that matters — `/composite-doors/` returning **zero** for `composite-door-styles`, `fg-cdq` and `why-distinction`, so live's original composite doors survived a release cut from live. All twelve images on `/office-and-retail-glazing/` `200`.
- **ONE DEFECT SHIPPED AND WAS FIXED BY THE RELEASE ABOVE.** `/office-and-retail-glazing/` prints `comm-curtain-walling-parade-1600w.jpg` **twice**: once as the Shopfronts detail figure and once as the related-products card for `/curtain-walling/`, which uses it as its own card image. The related strip is **not semantic** — `commercial-product.php:73` takes all commercial routes, drops the current one and slices the **first three in declaration order** — so that card is on every commercial page and reordering would move it on all thirteen. The fix is to change the detail figure, not the strip. Content: the specifier terminology pass and the shopfront intent split.

- Superseded: **LIVE WAS `10a596a7`, TAG `live-configuration-2026-08-30e`, STILL NOT ON `main`, 2026-08-30.** Fifth isolated release on the live line, cut from `2d8bfab7`. Owner: *"push it live"*. `main`'s theme tree is `c6409d49` and live's is `41c13718` — **compare theme trees, not SHAs**, because docs commits move `main` without changing the theme. **Checksum live and expect `10a596a7`; it matches no commit on `main`.**
- **THREE ROUTES STOPPED BEING PRODUCT PAGES.** `/french-casement-windows/`, `/french-doors/` and `/bow-bay-windows/` are CONFIGURATION pages now: what the arrangement is, which of our products it can be specified on as linked cards, the one thing worth knowing about it, and how it is put together. Owner: *"it's actually a configuration where the mullion is floating like french doors ... doable on all of our windows except t&t"*, and later *"french doors and bay windows will also be config pages"*.
- **A PRODUCT PAGE AND A CONFIGURATION PAGE ANSWER DIFFERENT QUESTIONS, which is why four bands had to come off.** The Liniar EnergyPlus tech banner, the uPVC colour chart, the four-item key-specification strip (whose third item read `Profile system: Liniar`) and the journey's `fg-product-why` / `fg-product-intel` all name ONE material, on pages whose whole point is that the arrangement is not tied to one. The gallery band is deliberately KEPT — photographs of the arrangement are what these pages want.
- **ONE LIST DECIDES ALL OF IT.** `fenster_configuration_routes()` is consulted by the tech banner, the colour chart, the spec strip and the template dispatch, so a route cannot end up half converted. That is exactly how the Liniar banner survived the first pass: the slug was removed from one framing and left in two lists.
- **THE TEMPLATE IS SHARED AND WAS PROVEN SHARED.** French doors and bow-and-bay adopted it without a line of it changing — a data entry and a slug each. One thing had to generalise first and it would have broken the second page: the highlight band hardcoded `Means of escape` as its eyebrow, and that band is the single thing worth knowing about a configuration, which differs per route. **Adding a fourth route is a data entry plus a slug.**
- **SCOPE CAME FROM THE OWNER, NOT FROM INFERENCE.** *"all of our windows except t&t"* taken literally puts sliding sash on the French casement list, and a vertical slider has no side-hung sashes for a floating stile to sit on — a capability claim we cannot meet. Asked; the answer was uPVC casement and flush plus all three aluminium. French doors is uPVC, aluminium and heritage aluminium. A bay is any of the seven, tilt and turn and sash included, because a bay is a shape.
- **EGRESS LEADS ON THE FRENCH CASEMENT.** Owner: *"egress is totally applicable for french. it's perfect for that."* An earlier pass had softened it to "we check it at survey", which was too cautious. The section carries Approved Document B's figures — 0.33 m² clear openable area, 450mm clear in both directions, opening 800–1100mm above floor — and says the size decides whether a given unit meets them. Quoting the criteria describes a regulation; promising compliance on an unspecified window is not ours to make in advance.
- **SEO: `/french-doors/` HAD NO OVERRIDE AT ALL** and was serving `data/pages.json` straight out — a title ending in a town and a description that was a list of them. Both written. Bow and bay gets a description that says what is distinct about the route. All three carry **six** FAQs and the limit to render them; at the default of five the last would have been sliced off silently, which this file has now been caught by twice.
- **PROVED ISOLATED against live's own bundles: `assets/js/main.js` is BYTE-IDENTICAL**, and the compiled CSS **adds 24 rules and removes none, every one inside the configuration, choice-map or obscure families**. Nothing else moved, +2,608 bytes.
- **RELEASE MECHANICS.** Guard on **eleven** files immediately before the rsync. Backup `fenster-pre-10a596a7-20260830-104129.tar.gz` (453M, 2,160 entries) **proven** by extracting `generated-page.php` and hashing it to the pre-deploy value. Deletion list **EMPTY**. **10 files written, 8 modified and 2 added.** Socket purge `msg:OK`. Verified on the apex: eight routes `200`, zero PHP notices, served CSS re-hashed to the deployed file, and `/casement-windows/`, `/upvc-doors/` and `/aluminium-windows/` all still carrying their tech banner, colour chart and spec strip.

- **LIVE IS `2d8bfab7`, TAG `live-blinds-2026-08-30d`, STILL NOT ON `main`, 2026-08-30.** Fourth isolated release on the live line, cut from `0718b840`. Owner: *"much better! push live"*. `main` is `e67125c0`. **Checksum live and expect `2d8bfab7`; it matches no commit on `main`.**
- **THE ISOLATED LIVE LINE IS NO LONGER AN OBSCURED-GLASS THING, and the tag name says so.** This is the first release on it that is a different page entirely — the integral blind visualiser. The line is simply *what is approved for live*, carried forward release by release while the rest of `main` waits. Expect the next tag to be named for whatever it ships, not for glass.
- **THE INTEGRAL BLIND'S BUNCHED STACK WAS TWICE THE SIZE IT SHOULD BE.** Owner: *"when they bunch up, they end about half as small irl."* `STACK_PITCH` — how tightly slats nest once raised, as distinct from `PITCH`, the spacing while hanging — was **2.15mm**, stacking this unit's 52 slats into **112mm**: nearly a fifth of the 626mm drop and **twelve times the 0.18mm thickness of a slat**. **1.08** puts it at **56mm**, six times the slat thickness, which is enough for the crown of a curved slat and the ladder tape between them and sits inside the 45–60mm stack manufacturers quote for a 12.5mm slat over this drop. Halved as asked, and defensible on its own terms.
- **ONE VISIBLE CONSEQUENCE.** The stack draws per-slat lines only while they are at least 1.4 CSS px apart, and `L.scale` comes from `getBoundingClientRect()`, so it is CSS pixels: at 0.796 px/mm the spacing goes 1.71px → 0.86px and the stack now takes the **gradient branch** instead of the line branch. That fallback already existed for exactly this case and is the better result — sub-pixel lines alias into grey mush — but the bunch reads as a dense band rather than discrete slats.
- **THE TIGHTEST ISOLATION PROOF SO FAR, AND THE SHAPE TO AIM FOR.** Against live's own bundles: `assets/css/main.css` **byte-identical**, and `assets/js/main.js` the **same byte length with exactly ONE differing span of four characters** — `2.15` → `1.08` — sitting in the blind visualiser's constant block (`u=520,l=700,g=12.5,b=.18,p=11.9,k=1.08,r=15`). Nothing else in either bundle moved. Confirmed again on the served file after deploy.
- **RELEASE MECHANICS.** Guard on **eight** files immediately before the rsync. Backup `fenster-pre-2d8bfab7-20260830-083251.tar.gz` (453M, 2,160 entries) **proven** by extracting `assets/js/main.js` and hashing it to the pre-deploy value. Deletion list **EMPTY**. **2 files written.** Socket purge `msg:OK`. Backups pruned to three. Verified on the apex: four routes `200`, zero PHP notices, served JS re-hashed to the deployed file with `k=1.08` in it, served CSS unchanged, and the obscured-glass work from the three earlier releases still present.
- **NOT VISUALLY VERIFIED BY ME, AND THAT IS A STANDING LIMITATION OF THIS ENVIRONMENT.** The blind visualiser repaints inside a `requestAnimationFrame` loop, and when the browser pane is hidden rAF is frozen — measured at **zero ticks in 700ms** with `document.hidden` true. Driving the lift input and forcing a resize both left the canvas byte-identical, so the raised state cannot be seen here at all. **Anything rAF-driven has to be checked by the owner on test**, or by a harness that does not depend on the pane being visible. The owner confirmed this one.

- **LIVE IS `0718b840`, TAG `live-obscured-glass-2026-08-30c`, STILL NOT ON `main`, 2026-08-30.** Third isolated release on the live line, cut from `c07a7c3c`. Owner: *"push it live"*. **Checksum live and expect `0718b840`; it matches no commit on `main`, which is the arrangement rather than drift.** `main` is `2563cc22`.
- **SATIN'S REMAINING DETAIL WAS THE SHARP SCENE BLEEDING THROUGH, NOT WEAK BLUR, AND THAT IS THE FINDING TO REMEMBER.** `.fg-obscure-stage__glass::before` carries `opacity: calc(0.64 + min(var(--privacy), 5) * .055)` — **0.915 at privacy 5** — and satin overrode the filter and the transform but never the opacity. So **8.5% of the unblurred scene sat on top of the frosting**, and against an otherwise flat field an 8.5% sharp overlay is very legible: the rooflines, windows and chimneys still readable were the clear image, not blur that had failed to bite. The partial opacity is correct for every other glass, where this element is a semi-transparent TEXTURE over a canvas render; satin has no canvas pass, so here the element IS the whole obscured image and anything under 1 is a leak.
- **IT WAS FOUND BY GOING THE WRONG WAY ON PURPOSE.** At 60px, 90px and 110px the houses stayed **equally** readable — which no blur curve does, so the blur could not be the variable. A layer probe then showed the sharp `__main-image` at z-index 1 beneath a `::before` at 0.915. **When a parameter sweep changes nothing, stop sweeping and look at what else is painting.**
- **AND I HAD ALREADY SEEN IT AND CALLED IT STALE.** An earlier screenshot showed exactly this and was dismissed because the browser pane was hidden and a genuinely stale capture had just been hit. It was not stale, it was the bug, and it cost a full round of tuning against an offline model that never included the bleed — which is why the model kept predicting a clean pane while the browser showed houses. **When the model and the render disagree, the render is right.**
- **WITH THE LEAK CLOSED THE BLUR CAME DOWN, 110 -> 64, and the scale with it, 1.52 -> 1.30.** Less magnification is less of the scene cropped away, which is more colour — the crop was the other thing costing it. Note the browser applies `filter` BEFORE `transform`, so the on-screen blur is 64 x 1.30 = **83 effective**; the raw number never needed to be large. The near scene keeps blur 24 and gains only `opacity: 1`, which turns the cat from a ghosted photograph into the silhouette it was always meant to be.
- **SATIN ALSO STOPPED DESATURATING AND WAS MATCHED TO THE OWNER'S PHOTOGRAPH OF A REAL PANE.** `saturate(0.8)` is deleted: scattering does not desaturate, so it was subtracting the exact thing being asked for. The photo was measured rather than admired — cropped to the glass and resampled to the pane's 950px it reads **lumaSpread 9.02** against live's 11.05, where lumaSpread is the standard deviation of a heavily blurred luminance, i.e. how much SHAPE survives. Its raw gradient number is higher than ours and is sensor grain, not structure.
- **LAYOUT, ALL OWNER-DRIVEN.** The scene switch moves to the foot of the rail as a direct grid child — inside the readout it was a control buried in descriptive text. The privacy numbers gain a visible `Privacy level` title, because five bare digits in a pill say nothing about what they select. And the mobile pattern picker **wraps to two columns instead of demanding a sideways swipe**: it was a horizontal snap rail, so at the default privacy 5 two of six patterns sat off the right edge. A sideways rail suits a gallery you browse, not options you are choosing between.
- **RELEASE MECHANICS, ALL PASSED.** Guard on **eight** files immediately before the rsync. Backup `fenster-pre-0718b840-20260830-080355.tar.gz` (453M, 2,160 entries) **proven** by extracting `assets/css/main.css` and hashing it to the pre-deploy value. Deletion list **EMPTY**, dry run agreed. **3 files written, nothing added, nothing deleted.** `--checksum` again. Socket purge `msg:OK`. Backups pruned to three, 1.4G.
- **PROVED ISOLATED RULE BY RULE:** against live's own compiled CSS this release **adds 8 rules and removes 7, none outside `.fg-obscure*` / `.is-level-filtered`. Zero non-glass rules touched**, +128 bytes, and `assets/js/main.js` is **byte-identical** to live. Verified on the apex: four routes `200`, zero PHP notices, served bundles re-hashed to the deployed files (`ea315523…`, `6ca2e0b3…`), every behaviour marker present and **zero `fg-cd3-slab` selectors**.

- **LIVE IS `c07a7c3c`, TAG `live-obscured-glass-2026-08-30b`, AND IT IS STILL NOT ON `main`, 2026-08-30.** Second isolated release on the live line, cut from `c11cece9` rather than from `main`. Owner: *"once these are fixed you can push live"*. **Checksum live and expect `c07a7c3c`; it will match no commit on `main`, and that is the arrangement, not drift.** `main` is `09476ddf`.
- **THIS SPLICE WAS TRIVIAL WHERE THE FIRST WAS NOT, AND THE REASON IS WORTH KNOWING: everything on `main` since `98d1b62c` is glass work.** Three code commits and two docs, five theme files, two of them build artefacts. So the source diff applied to the live line with `git apply --3way` **cleanly, with no hunk classification at all**. The expensive part of the first release was never the mechanism, it was the interleaving.
- **PROVED ISOLATED RULE BY RULE, which is a sharper check than the token sweep used last time.** Against live's own compiled CSS this release **adds 15 rules and removes 7, and not one of them falls outside `.fg-obscure*` / `.is-level-filtered`. Zero non-glass rules touched.** CSS +757 bytes, JS +50. The token sweep would have reported nothing here, because these edits change existing rules rather than introducing new class names — worth remembering before trusting it alone.
- **THE SELECTOR CARD NEEDED THREE SEPARATE FIXES AND THE FIRST TWO LOOKED SUFFICIENT.** Owner: *"the text in the selector boxes are still aligned for text underneath"*. Hiding the privacy line with `visibility: hidden` left it holding its grid row; taking it out of flow left the card's own `grid-template-rows: auto auto`; collapsing that left the **swatch at `grid-row: 1 / 3` manufacturing a second implicit row on its own.** All three had to go. `getComputedStyle().gridTemplateRows` returns the **USED** tracks, not the declared ones, which is what kept reporting two rows and naming the culprit each time. The level is hidden the screen-reader way, so a reader still hears "Cotswold, Privacy 5" — the only route to that fact for someone who cannot see which chip is pressed.
- **THE HERO GAP WAS LITERALLY ZERO AND ONLY BECAME VISIBLE ONCE THE BAND WAS FIXED.** Owner: *"there's no padding below hero on mobile"*. The eyebrow began on the exact pixel the last tile ended, because the hero is a grid with no row gap; the previously ragged mid-tile cut had been masking it. Fixed with a row gap rather than padding on the copy, because the veil is absolutely positioned so the hero has exactly two rows in flow.
- **AND THE BAND ITSELF WAS A TWELVE-ROW GRID SHOWN THROUGH A TWO-ROW WINDOW.** Tiles carry `aspect-ratio: 1`, so at three columns two rows is exactly 66.667% of the width; the rule asked for `clamp(210px, 66vw, 340px)`. **2.6px short at every size**, so row two was sliced and rows three to twelve sat clipped behind `overflow: hidden`. It is `aspect-ratio: 3 / 2` now, which cannot drift and is immune to the scrollbar `100vw` is not. **The desktop comment two rules above already warned against exactly this** — it refuses a fixed row height in vw — and the mobile rule had pinned the container height in vw against tiles sized from the column width.
- **A NAMED GRID AREA THAT NOTHING CLAIMED PUT THE MOBILE CONTROLS IN THE WRONG ORDER.** The template declared a `filter` area no element carried, so `.fg-obscure-options` — the privacy chips — fell out of the named grid and auto-placed **after** the pattern rail: on a phone you scrolled past every pattern to reach the control that narrows them. The readout was unplaced for the same reason and only landed correctly by luck of source order. Every area is claimed now.
- **TWO THINGS FOUND ON THE MOBILE PASS THAT NOBODY ASKED FOR.** The privacy chips were **36px** while the scene switch inches away is 46px and cites the Forms rules' 44px tap target as its reason; keyed off `hover: none` so it follows the input device rather than the window. And the comparison grid was **8,146px** tall on a 375px screen at one card per row, so comparing two patterns meant scrolling between them — the one thing that grid exists to prevent. Two columns below 560px brings it to **3,345px**; that reverses an existing deliberate `1fr` rule, so the reasoning is recorded in the stylesheet.
- **RELEASE MECHANICS, ALL PASSED.** Guard on **eight** files immediately before the rsync. Backup `fenster-pre-c07a7c3c-20260829-173954.tar.gz` (453M, 2,160 entries) **proven** by extracting `assets/css/main.css` and hashing it to the pre-deploy value. Deletion list **EMPTY**, dry run agreed. **5 files written, nothing added, nothing deleted.** `--checksum` again, so only the five real changes were written and no other asset's cache-buster moved. Socket purge `msg:OK`. Backups pruned to three, 1.4G.
- **VERIFIED ON THE APEX AS A VISITOR.** Four routes `200`, zero PHP notices. Served `main.css` and `main.js` re-hashed to the deployed files exactly (`db2cf870…`, `6ca2e0b3…`) and carry `is-level-filtered`, `aspect-ratio:3/2`, the `stage/readout/options` areas and the `hover: none` tap-target rule, with **zero `fg-cd3-slab` selectors**. `/composite-doors/` still renders live's original.

- **LIVE IS `c11cece9` AND IT IS NOT ON `main`. THE OBSCURED GLASS PAGE IS LIVE, 2026-08-30.** Owner: *"push the obscure glass page stuff live"*. Live moved off `b2420743` for the first time since 26 August. **`main` is `443a1b44` and live is NOT an ancestor of it** — the release is an isolated commit off `b2420743`, tagged **`live-obscured-glass-2026-08-30`**, which is the only ref that reaches it. **Checksum live before any future deploy and expect `c11cece9`, not a commit on `main`.**
- **THE GLASS WORK COULD NOT BE SHIPPED AS A COMMIT RANGE, AND BOTH OBVIOUS WAYS OF TRYING WERE MEASURED BEFORE GIVING UP ON THEM.** `b2420743..98d1b62c` is 181 commits: ~73 obscured glass, ~53 other product work, ~55 docs. Replaying the glass commits onto live gave **2 of 73**; reverting the other work off test left **145 unresolved conflicts**. The work is interleaved inside `src/js/main.js`, `src/scss/main.scss` and `generated-page.php`. **Classifying by commit subject is also unreliable** — `Fix: close the Cassini comment` is glass work and matches no glass prefix.
- **WHAT WORKED IS A HUNK-LEVEL SPLICE, AND IT IS WORTH REUSING.** Only **7 files** are shared between the two strands and **2 of those are build artefacts that get rebuilt**, so the real merge surface is five source files. Every hunk of their `b2420743..98d1b62c` diff was classified by content and only the glass ones applied, **each as its own single-hunk patch** so a dropped hunk cannot corrupt the offsets of the ones after it. 32 hunks, zero failures. Seven hunks matched no keyword either way and were read by hand: `.fg-composite-glass__grid`, the composite card and the `composite-doors h2` blanket rule were composite; four were glass.
- **THE ISOLATION WAS PROVED AT THE TOKEN LEVEL, NOT ASSERTED.** Against live's bundles the released CSS adds **17 selectors and the JS 6, every one of them glass** — no composite, showroom or experimental token leaked in. The fifteen glass-*looking* selectors on `main` that this does not carry are all composite door glass (`fg-cd3-slab__glass-*`, `fg-composite-glass__*`, `fg-glass-door*`, `fg-glass-patterns*`), which is correct.
- **AND PROVED AGAIN BY RENDER BEFORE IT SHIPPED.** The isolated theme was put on test temporarily and `/obscured-glass/` rendered **identically to the full test tree** — one differing line after normalising cache-busters, and that line is the footer's `/why-distinction/` link, deliberately excluded because that page is not going live either. Test was restored to `main` immediately after and re-hashed to confirm it.
- **DELIBERATELY NOT LIVE, AND STILL UNAPPROVED ON `main`:** the composite doors V2 overhaul and its 148 images, `/why-distinction/`, decorative glass, the quiz, the finishes pass, the site-footer link and the whole showroom and experimental strand.
- **RELEASE MECHANICS, ALL PASSED.** Live re-established by checksum on **five** theme files (5/5, with three other candidates separating at 1/5) and re-guarded on **ten** files in the seconds before the rsync. Backup `fenster-pre-c11cece9-20260829-170914.tar.gz` (445M, 2,127 entries) **proven** by extracting `inc/site-data.php` and hashing it to the pre-deploy value. Deletion list asserted **EMPTY** and the dry run agreed. **40 files written: 33 added, 7 modified, 0 deleted.** Socket purge `msg:OK` — `wp sg purge` is still not a live command, exactly as this file has recorded since July. Backups pruned to three, 1.4G, disk 86% with 315G free.
- **RSYNC WAS RUN `--checksum`, AND THAT MATTERED.** The staging copy carried this Mac's mtimes, so a plain `-a` dry run wanted to rewrite **all 2,050 files** — which would have re-stamped every asset's `?ver=<mtime>-<size>` cache-buster and dumped the whole site's asset cache for a 40-file release. With `--checksum` it wrote exactly the 40 that differ.
- **VERIFIED ON THE APEX AS A VISITOR, no cache-buster.** `/obscured-glass/` `200` with the new title, H1, 155-character description, `FAQPage` with six questions, the privacy-scale and onward-route sections, no `All` segment, the Cotswold WebP in place of the 2.29MB PNG, windows in the related band and the traced Cassini tile. Seven routes `200`, **zero PHP notices on any of them**. Served `main.css` and `main.js` re-hashed to the deployed files exactly (`af0c71d2…`, `a39d2afd…`) and carry the glass rules with **zero composite selectors**. `/composite-doors/` confirmed still rendering live's original, not V2.

- **THE VISUALISER OPENS ON PRIVACY 5 AND THE PANE IS HALF AS BIG AGAIN. TEST IS `98d1b62c`, LIVE IS UNMOVED AT `b2420743`, 2026-08-30.** Owner: *"it should start on 5 and not show the 'all'. the title should be top right and the actual visualiser should be in the same ratio but much bigger now the title has moved."* Pane goes **720x540 -> 950x713** at 1440x900, 4:3 held at every width checked. Two commits, three files, deletion list EMPTY.
- **THE `All` SEGMENT WAS REMOVED ON 2026-08-30 BUT THE STATE IT SELECTED WAS STILL WHAT YOU LANDED ON** — no level pressed, all twenty-one patterns listed. `applyPrivacyFilter('5')` now runs at init. **5 is not an arbitrary default**: it is what someone searching privacy glass wants, and it is the level of `cotswold`, which the markup already renders as `data-active-glass`, so the pane keeps its glass and the list narrows around it with no first-paint swap. Change either and check the other.
- **`max-width` COULD NEVER HAVE WIDENED THAT CARD, AND THAT IS A SITE-WIDE TRAP.** `.container` sets `width: min(100% - 2rem, var(--container))`, so an explicit width already resolves to the house 1180px and **`max-width` only clamps DOWNWARD**. The old 1202.8px cap was inert and raising it to 1362.8px stayed inert — the pane crept 720 -> 750 and stopped, with the pattern list crushed to its 320px minimum because every extra pixel was coming out of the list rather than the card. It is a `width` now, with a **1380px ceiling** so the card does not run to the window gutter, and it degrades to the house container under ~1200px so it can never end up narrower than the sections around it. Owner approved the ~100px overhang; this is the only element on the page that is a tool rather than prose.
- **THE 720px CAP WAS DOING THE LIMITING, NOT THE HEIGHT BUDGET.** `min(748.8, 828.8)` picked the cap at 1440x900, so the height term never bound above an 840px window. **The 300px subtrahend was re-measured rather than nudged** and was describing the old layout: it was already too small for the whole-section-on-one-screen rule it was written for, and only held because the cap pinned the pane at 540. The rule is deliberately narrowed to **the pane and its divider** — the actual 2026-08-29 complaint — and the stylesheet records that the number is 327 if the whole section must fit again.
- **THE READOUT IS A GRID CHILD NOW, AND IT HAD TO BECOME ONE TO BE PLACED AT ALL.** Filtering to 5 leaves six patterns, which left the right column half empty; the readout fills it, groups the controls on one side and hands ~88px of height back to the pane. **CSS grid places only DIRECT children** — a `grid-area` on a grandchild does nothing and fails silently — and `display: contents` on the stage would have placed it while destroying the thin white surround kept since 2026-08-29. Source order is stage-then-readout, so below 861px it still stacks under the pane with its original white card, unchanged.
- **A STRAY OUTLINE ROUND THE READOUT, WHICH IS THE SOURCE-ORDER TRAP IN A NEW SHAPE.** The base rule gives it a full border, radius, white fill and 1rem padding; the 2026-08-29 reset cleared all of it. Putting the new separator in an EARLIER rule and dropping `border: 0` from the reset left the base border showing on **three sides with square corners**. **Declare a component's box in one rule** — if a reset already exists, extend that one. Also single-column in the rail: the base lays text and scene switch side by side, right under a 950px photograph and wrong in a 320px column, where it wrapped the description to three lines beside the icons.
- **VERIFIED BY INTERACTION, NOT JUST GEOMETRY.** Picking a pattern updates the readout and the stage; switching to level 4 lists the right seven and moves the stage on when the active pattern is filtered out; pressing the pressed level clears back to all twenty-one with nothing pressed, which is the only way back now that `All` is gone. Served `main.css` and `main.js` hash byte-identical to the local build; `/obscured-glass/`, `/` and `/casement-windows/` all render.

- **THE OBSCURED GLASS PAGE IS AN SEO PAGE NOW, AND TWO OF THE THREE REAL WINS WERE INVISIBLE IN THE DIFF. TEST IS `d18bd518` (theme tree `b39116a7`), LIVE IS UNMOVED AT `b2420743`, 2026-08-30.** Owner: *"its pilkingtons collection and we now have a way better visualiser than them ... work top to bottom and get this as optimised as possible."* The page was **492 words** with no FAQ markup, no onward route and a title that named the page after itself. It is **1,396 words** now, with a privacy-scale explainer, an onward-route band, six FAQs and `FAQPage` schema. **Three commits, seven files, deletion list EMPTY.**
- **THE BIGGEST MEASURABLE WIN WAS AN ASSET, NOT COPY: Cotswold pointed at a 2,379,026-byte PNG for a 64px swatch, on this page and THIRTEEN product routes.** It is a CSS background on an in-render-tree element, so it can **never** lazy-load — it was roughly 40% of the mobile image payload on the site's most-viewed page. `Cotswold-privacy-5.webp` is the same picture at **93,080 bytes** (correlation 0.987 — the difference is the resize alone) and had sat unused in the same folder since July. A **one-line data edit**. It was sitting in `FULL-SITE-AUDIT-2026-08-13.md` [133] the whole time. The 2.3MB PNG is now referenced only from a comment; it is still in the repo and could be deleted, which is a separate call.
- **THE RELATED BAND HAD ELEVEN DOORS AND NO WINDOWS, ON A PAGE ABOUT BATHROOM GLASS.** `$context` is slug + title + intro and is matched by substring: `/obscured-glass/` hit `door`, and hit neither `window` nor `glaz` — **`glass` does not contain `glaz`**. Fixed with an explicit `obscure` branch placed AHEAD of the door test, because `$add_related_route` keys by slug so the first block to add a route fixes its position. Ten windows and eight doors now, windows leading. **Only `/obscured-glass/` and its `/obscure-glass/` alias match, so nothing else moved.**
- **NEITHER FAULT WAS VISIBLE IN THE SOURCE AND BOTH WERE OBVIOUS IN THE OUTPUT.** Reading the rendered page is what found them. **The test site is Basic Auth protected, so `curl` cannot read it** — do not guess at credentials. Render it server-side instead: `wp eval-file` with `$_SERVER` populated, `wp()`, then `require ABSPATH . WPINC . '/template-loader.php'`. **Two traps in that, both of which return success while producing nothing useful.** `template-loader.php` is entirely gated on **`WP_USE_THEMES`**, which WP-CLI never defines — without it the render is silently EMPTY, not an error. And SiteGround's `Supercacher_Helper::set_cache_headers()` does a bare `return` under CLI, handing **null** to the next `wp_headers` filter and fataling the theme's `array`-typed callback. **That is CLI-only and NOT a production risk** — over HTTP it always returns the array — so drop the filter in the harness rather than weakening the theme to suit it.
- **`max-image-preview:large` IS DELIBERATELY NOT EMITTED, so setting it on a new page does nothing.** `fenster_render_generated_head()` treats that exact value as the default and prints a robots meta only for anything else. The absent meta is not a bug. Turning the opt-in on is a one-line site-wide change and should be decided as one, not smuggled in per-page.
- **NAMING PILKINGTON IS THE OWNER'S CALL AND IT PASSES THE SUPPLIER NAMING RULE'S OWN TEST** — they do not sell direct, which is the same answer that rule already gives for Sheerline and Liniar. Both `obscure` and `privacy` lead the title because **22 content in-links point here with obscure-glass anchors**; dropping that to chase one phrase costs more than it wins. Title is 63 characters, so **the brand is what truncates**, deliberately.
- **THE PRIVACY BANDS ARE BUILT FROM THE DATA, NOT WRITTEN OUT**, so a re-rated pattern cannot leave stale prose behind. The FAQs go through `template-parts/components/faq-block.php`, which owns the rendered questions and the `FAQPage` schema together so the two cannot drift. **No answer quotes a light-transmission figure, because none is confirmed** — that is still one of the five figures blocked on the owner.
- **A SPECIFICITY TRAP WAS WALKED INTO AND CAUGHT BEFORE IT SHIPPED.** `.fg-obscure-scale__row p` is (0,1,1) and outranks a bare `.fg-obscure-scale__names` at (0,1,0), so the new margin did nothing. The first fix was `!important`; the right one was qualifying the class with the row. **Verified in a browser rather than assumed** — computed `margin-top` reads 5.6px. Same namespaced-element-beats-class trap this file already records.
- **VERIFIED: served `main.css` and `main.js` hash byte-identical to the local build**, PHP lints clean, `FAQPage` JSON-LD parses with six questions, `BreadcrumbList` intact, all 15 images in `main` carry alt/loading/width, no horizontal overflow at 1440, route cards equal height, and the visualiser is one white card with the title above the selector, no `All` and no visible pattern count. The `aria-label="Privacy 5, 6 patterns"` text stays — it is what makes a button reading `5` mean anything to a screen reader.

- **CASSINI'S SHAPES NOW COME FROM THE CLOCK PHOTOGRAPH, AND `faceFlat` HAS STOPPED MATCHING `groundFlat`. TEST IS `e0bd7c56`, LIVE IS UNMOVED AT `b2420743`, 2026-08-30.** Owner: *"texture and opacity are good now. you need to exactly copy the shapes and layout from the clock image though."* Right on the diagnosis. Put the two sources side by side **at matched leaf scale** and it is obvious: the clock shows **pointed teardrops**, a broad lobe tapering to a point, densely packed and overlapping in rosette clusters, each with a strong dome shade, in many sizes. WindowCAD draws **rounded blobs** — no point, no taper, no dome, sparser and far more uniform. **WindowCAD renders an approximation of Cassini; the clock is a photograph of the real sheet.** The plate comes from the clock now, hatch included, since the real sheet's own hatch is in it at the real 21.8 ratio against WindowCAD's 16.0. The tile is also lighter, 168KB against 446KB.
- **`faceFlat` AND `groundFlat` MUST NOT BE EQUAL, AND ACROSS TWO PLATES THEY EFFECTIVELY WERE** — 0.55/0.58, then 0.80/0.84. **A lens is visible only because its face is flatter than the ground around it**, so setting the two within a few percent of each other flattens everything equally and the shapes stop reading at all. That is most of why this glass has looked like amorphous blobs through several rebuilds, and it was hiding in plain sight in the material for weeks. 0.96 against 0.62 now, with `heightBlur` 9 -> 4 so a 58px teardrop keeps its point.
- **NEVER BAND-PASS SOMETHING WHOSE EDGES ARE THE POINT.** Removing the clock from behind the glass was first tried as `gaussian(2) - gaussian(30)`. It looked reasonable in isolation and destroyed the one thing the asset exists to carry: subtracting a wide blur puts a halo either side of every hard step, so the teardrops came out as **soft grey blobs with their outlines gone** — a plate measurably worse than the photograph it came from. Mean gradient magnitude: photograph 32.6, band-pass 11, **local contrast normalisation 18.9**. Dividing by the local standard deviation leaves a step a step.
- **THE SEGMENTATION WAS DUMPED RATHER THAN ASSUMED, AND IT CLEARED THE PLATE OF SUSPICION.** Run offline at the renderer's own percentiles and blur, the clock plate yields **84 seed regions that are visibly pointed and leaf-shaped**, against WindowCAD's rounder, more uniform 107. The shape information does reach the renderer, so the remaining fault is downstream of the plate.
- **STILL NOT DONE, AND IT IS THE THING THAT WAS ASKED FOR: the reference's lenses have HARD OUTLINES and this model cannot draw one.** It expresses a lens only as a difference in how the scene is displaced and flattened, never as a boundary. Everything available was tried — the blur terms down, **`washMix` up to 0.95, which has almost no visible effect at all**, the face/ground split above, and `rim2`/`rimDark` up, **which draws a literal dark line round every region and is exactly the topographic-map failure this file already records for that same gradient**. The outline needs a NEW MECHANISM, not a parameter, and that is a decision rather than a tweak.
- **A PERSPECTIVE RECTIFICATION WAS BUILT AND THROWN AWAY** on the WindowCAD source: the hatch pitch reads 6.5px one side and 5.0px the other, which looks exactly like the pane receding. Measured with 512px windows and a sub-bin parabolic fit rather than the raw FFT bin, **the scale is constant to within 4%** and the apparent gradient was the HATCH ANGLE changing between sectors. **Bin-quantised measurements invent gradients.**
- **CASSINI'S PLATE WAS THE WRONG PATTERN AND THE ZEBRA WAS `fluteShade`. TEST IS `1a80e85e`, LIVE IS UNMOVED AT `b2420743`, 2026-08-30.** Owner, with a screen recording of the glass in the WindowCAD designer: *"currently its opacity is ok but the pattern is completely wrong."* Two separate faults, one in the asset and one in the renderer, and neither would have been found by fixing the other.
- **EVERY CASSINI PLATE SO FAR CAME FROM A PHOTOGRAPH OF A SAMPLE, AND WHAT THOSE GAVE WAS A MAT OF ROUNDED PEBBLES PACKED EDGE TO EDGE.** Real Cassini is **pointed, overlapping leaves** floating over a ruled hatch that runs in large straight-edged angular sectors. The recording shows that and so does Pallot's own `Textured-Cassini_5.jpg` — **two sources independent of us agreeing with each other and disagreeing with what we shipped**, which is what settled the identity question the recording could not answer on its own (it never shows the picker). Built now by `scripts/build-cassini-windowcad.py` from a frame committed at `scripts/reference-cassini-windowcad.png`. **The blue cast in the recording is the quote tool's toughened-glass indicator, not the glass**; the build works on luminance only so it never reaches the plate.
- **THE BARS WERE `fluteShade`, AND KNOCKOUT FOUND IT IN TWO RENDERS WHERE READING THE CODE HAD NOT.** The page was drawing broad black-and-white diagonal bars over everything. `hatch: 0` changed almost nothing; `fluteShade: 0` removed them completely. It was at **1.30**, and this file already records it going 1.90 -> 0.70 and back up again, so it had been overshooting for a while — the accumulation trap, arriving on the one glass with the worst record for it. **0.25** is where the hatch reads as the reference's fine sectored texture. The bars were also doing much of the obscuring, so `groundFlat`/`faceFlat`/`washMix` come up to put a privacy 5 glass back where it belongs. **Do not restore privacy by drawing bolder pattern.**
- **A "PERSPECTIVE GRADIENT" WAS NEARLY RECTIFIED AND IT WAS NOT THERE.** The vertical hatch pitch reads 6.5px on the left of the frame and 5.0px on the right, which looks exactly like the pane receding. Measured properly — 512px windows and a sub-bin parabolic peak fit rather than the raw FFT bin — **the scale is constant to within 4% across the band the crop is taken from**, and the apparent gradient was the HATCH ANGLE changing between sectors: lines closer to horizontal show a wider vertical pitch at the same true pitch. A whole horizontal warp was built and thrown away. **Bin-quantised measurements invent gradients.**
- **A NORMALISATION REPORTED A STANDARD DEVIATION IT WAS NOT ASKED FOR, AND THAT WAS THE BUG.** The lighting removal computed local variance through a PIL Gaussian blur, which round-trips through uint8, so the squared deviations saturated at 255 and the variance was silently wrong. The tell was an output at std 52 when 21.5 had been requested. Float blurs (`scipy.ndimage`) fixed it. **If a normalisation returns a number it was not asked for, suspect the blur before the maths.**
- **THE TILE IS DATA, NOT A PICTURE, AND IT SHIPS LOSSLESS.** The renderer derives its height field, oval segmentation and hatch steering from those pixels, so a lossy encode moves the render rather than softening the plate: q95 against lossless shifts **20% of output pixels by more than two levels**, and **q90 lands closer to lossless than q95 does** — non-monotonic, so it cannot be tuned by picking a quality. The renderer itself is deterministic (two runs on identical input diff to **exactly zero**, checked), so whatever ships IS the render. The display copy is the opposite case — only looked at, and the eager one, since textures live in `data-texture` until their pattern is picked — so it is 760px at q72.
- **A METRIC WAS TRIED AND THROWN AWAY, RECORDED SO IT IS NOT TRIED AGAIN.** Mid-scale RMS over the glass half reported the ZEBRA build as carrying the most structure, because it cannot tell scene detail from pattern drawn on top of it. Same family as the fine-band contrast trap this file already records. Judged against Pallot by eye instead.
- **SCOPED, AND PROVEN SCOPED. Florielle shares the `hatchlens` path** and is pixel-identical, as are Reeded, Digital and Charcoal Sticks. Rebuilding both assets from the committed reference reproduces the tile **byte-for-byte**, and re-rendering after that rebuild diffs to zero. Served image bytes on test hash to the local files, so the no-version-string trap is closed by measurement rather than by the rename convention. Old plates left in place, deletion list still **EMPTY**.
- **STILL SHORT, AND IT IS THE THING THE EARLIER REBUILDS GOT STUCK ON: the lens OUTLINES are softer than the reference's, which are crisp-edged.** That is pre-existing `hatchlens` character, not something this release introduced. It needs the owner's eye before anyone spends another rebuild on it.
- **REEDED'S FLUTES NO LONGER INVERT, AND THE REFERENCE IS WHAT SETTLED IT. TEST IS `094ab386`, LIVE IS UNMOVED AT `b2420743`, 2026-08-30.** Owner on the live page: *"the physics are weird, like it inverts everything too much."* Correct. `flip: true` ran the sample backwards inside every flute; the Pilkington reference does not. **Two files, both JavaScript, no SCSS, no template, no data, no assets.** Served bundle re-hashed to the local build and the minified `close:{spread:2.5,jitter:3}` greps out of it; `/obscured-glass/`, `/` and `/casement-windows/` all `200`.
- **THE TEST THAT PROVED IT IS CHEAP AND RE-RUNNABLE, AND IT ONLY WORKS BECAUSE IT USES TWO REGIONS WITH OPPOSITE TRUE SLOPES.** On `Textured-Reeded_2.jpg`, take the top edge of the clock's red rim, cut it at the flute boundaries, fit each segment, and compare the sign of the segment slope against the arc's own trend. Left shoulder (arc rising) and right shoulder (arc falling): **15 of 16 segments lean WITH the arc**, at roughly 3-4x its gradient. One region alone would have proved nothing, because a sawtooth leans somewhere whatever the sign. The numerals agree independently — the reference reads **"12 | 12 | 2"** and the glyphs are not mirrored, so the comment in the source claiming it reads "12 | 21 | 2" was wrong and is corrected.
- **A MIRROR INSIDE A FLUTE HAS A SIGNATURE WORTH RECOGNISING ON SIGHT: BUTTERFLY CHEVRONS.** Reflecting about each flute centre turns a single diagonal into symmetric V and X shapes straddling every seam. Legend's polo shirt carried one the width of the pane and the house's gables came out in mirrored pairs. The reference has none: its clock hands break into a **sawtooth whose teeth all lean the same way**. Upright plus a hard seam gives repetition without reflection, which is the whole difference.
- **THE NEAR SCENE'S `spread` WENT 1.5 -> 2.5 AND THAT IS NOT A SECOND OPINION ON THE OWNER'S 2026-08-29 CALL.** The mirror had been doing a large share of the obscuring, so removing it left Legend almost clear at 1.5. 2.5 puts the disruption back to about where it was approved. **The house is untouched at 3.0**, so the far scene still scrambles harder than the near one — which is the direction distance works in, and the sign never changes with distance: the clock IS the near case and it is upright.
- **SCOPED, AND PROVEN SCOPED RATHER THAN ASSERTED.** Charcoal Sticks is the only other `rib` glass and never set `flip`. Charcoal Sticks, Cassini and Digital all **pixel-diff to ZERO** against the pre-change bundle on the same scene, and the compiled artefact rebuilds byte-identical from source.
- **STILL OPEN, AND VISIBLE IN THE COMPARISON: the reference carries pale washed caustic bands either side of every flute seam and ours are hard edges.** That is the next real difference between us and the photograph, it is a separate change, and it should not be bundled into a tuning pass.
- **THE OBSCURED GLASS PAGE IS ON TEST AT `819b374d`, NOT LIVE, 2026-08-29.** Live is unmoved at `b2420743`; test is **162 commits ahead of it and none of it is approved**. Nineteen commits this session (`8cfb7207..819b374d`), every one answering something the owner saw on the real page. **Fourteen files: eight ADDED (seven re-cut plates and the script that cuts them), six modified, deletion list EMPTY.** Served `main.js` and `main.css` both re-hashed byte-identical to the local build after each deploy; checklist routes `200`.
- **THE MIRROR THE OWNER SAW WAS IN THE PHOTOGRAPH, NOT IN THE RENDERER, AND READING THE TILING CODE WOULD NEVER HAVE FOUND IT.** Owner: *"they all have a shitty mirror repeat which they dont have irl ... sycamore and oak for eg have it. they mirrors vertically."* Seven plates had been extended by reflecting the photograph about a vertical axis at roughly two thirds across. These glasses lay at `cover` — laid ONCE, no tiling, no mirroring anywhere in the path — and the reflection is still on the pane, because the axis sits inside the visible window at every pane width. Proven outside the renderer entirely: replicating `drawCover`'s arithmetic in isolation and laying the raw plate reproduces the chevrons exactly. `scripts/build-unmirrored-plates.py` finds each axis, keeps the larger (original) side, and **refuses to write if the residual reflection is still above 0.45**.
- **IT WAS MISSED TWICE BECAUSE THE SWEEP TESTED EACH PLATE ABOUT ITS CENTRE. The axis is at 68%.** Sycamore scored **-0.011** about the middle and was declared clean while the fault was plainly visible in the owner's screenshot. **Search the axis position; do not assume the middle.** Same family as everything else this file records — the probe was wrong, not the site.
- **THE NEW FILES CARRY `-unmirrored` IN THE NAME, and that is not tidiness.** Texture images are emitted with no version string, so replacing a `.webp` in place leaves every browser and proxy that has seen it serving the old one — the corrected Cassini was live and invisible for exactly that reason. Third time this rule has been paid for.
- **`kind` DECIDES WHETHER A GLASS CAN OBSCURE AT ALL, AND NO AMOUNT OF PARAMETER WORK SUBSTITUTES FOR THE RIGHT FAMILY.** `const src = mat.kind === 'frost' ? soft : scene;` (`src/js/main.js:5604`) — **only `frost` samples a pre-blurred copy of the scene. Every other kind displaces the SHARP scene**, so it moves detail around instead of destroying it. A privacy 4 or 5 glass built in a non-frost family therefore reads as visible distortion over a fully legible view: the owner's *"a bit of a mister effect"* for Contora, and *"on test it shows a bit like reeded"* for Cotswold. **Both were fixed by changing family, not by tuning** — Contora `dapple` -> `frost`, Cotswold `rib` -> `frost`. Check the family before spending a sweep on the numbers.
- **`emboss` CAN ONLY PUSH TOWARD WHITE. IF SOMETHING MUST BE MORE DEFINED WITHOUT BEING WHITER, THE CONTROL IS `shade`.** `v += eV > 0 ? (255 - v) * eV : v * eV * shadeAmp;` (`src/js/main.js:5665`) — the positive lobe adds a fraction of the REMAINING HEADROOM, so raising emboss to sharpen a feature necessarily bleaches it. Owner, holding a reference photograph up against it: *"youve made it too defined and added too much white. if you look at the ref pic where it's actually over an object, it doesn't show as white as yours."* Minster's blobs were defined by taking emboss DOWN to 0.44 and `shade` UP to 0.75 — the definition comes from the shadow side.
- **COMPRESSION STRAIGHTENS, AND THAT IS WHERE "COMPUTERISED" STRAIGHT LINES COME FROM.** Owner on Reeded: *"theres obvious white angled lines that look too computerised and not natural."* They were **the scene**, not an overlay and not the texture. `spread` compresses horizontally inside each flute, so a soft diagonal cloud edge squeezed 4.2x comes out a hard straight streak, and the flute pitch then repeats it on an exact interval — which is what makes the eye read a drawn line rather than weather. Isolated before anything was changed: the streaks survive in the flat sky band, hiding the `shine`/`scan`/`glass` CSS layers changes nothing, and the sky's gradient energy is **7.5 horizontal against 2.96 vertical** (flute edges, not an angled overlay). Fixed by `spread` 4.2 -> 3.0, `period` 34 -> 30, `wander` 1.2 -> 4.5, `jitter` 10, and `emboss` 0.34 -> 0.24 — the rib crown is what made the streaks read WHITE rather than merely light.
- **HOW MUCH A GLASS OBSCURES DEPENDS ON HOW FAR AWAY THE SUBJECT IS, SO A MATERIAL CAN NOW VARY BY SCENE.** A material may carry a `close: {}` block, merged over the base when `background === 'cat'` (`src/js/main.js:4179`). Owner: *"the physics for the reeded on house is good but as legend is closer its too diffuse. irl you'd see him a bit clearer, ie not as broken up."* At `spread: 3.0` the near subject's eyes were gathered into three flutes at once and the face read as texture; Reeded close takes `spread: 1.5, jitter: 3`. **Satin was already doing this through CSS because it never reaches the canvas** — this is the canvas equivalent, and every other material can use it.
- **SATIN IS LIFTED WITH WHITE, NOT WITH `brightness()`.** Owner wanted no legible detail at all — *"more of a coloured silouette"* — and more of it on the distant scene; blurring to 24px close and 44px distant got there and then read dark, *"its ok but looks kinda dark now"*, because a heavy blur averages a scene toward its own mean. `brightness()` scales what is there and re-darkens as soon as the blur goes further; a white veil at fixed opacity does not.
- **A CASCADE TRAP, FOR THE SECOND TIME IN THIS FILE: AN EQUAL-SPECIFICITY RULE ADDED ABOVE AN EXISTING ONE LOSES.** The Satin `::after` opacity was written into a new block above the block that already set it, so only the house variant worked — and only because it carried one extra attribute selector. **Set the value on the rule that already owns the property.**
- **THE "MARGINS ON THE LEGEND PICTURE" WERE A MOBILE LETTERBOX, AND NO IMAGE EXTENSION WAS NEEDED.** Owner: *"dont shrink the image to fit but just extend the pic in an ai way."* `contain` was leaving **21.8px each side at 800px wide and 38.1px top and bottom at 500px**, because the mobile pane keeps `aspect-ratio: auto` and is not 4:3. Both scenes now lay at `cover`, which fills rather than shrinks; at a matching ratio it crops about the same few pixels the gap would have been, so nothing is generated and nothing is lost. **Desktop was never affected** — an earlier "4px letterbox" measured there was the card edge OUTSIDE the pane, because the sample region ran past it.
- **A 22px ICON CANNOT BE JUDGED INSIDE A FULL-PAGE RENDER.** Four passes went by on the scene-switch cat — *"looks like a gremlin lol"*, then *"you kinda reverted to the gremlin one"* — while it was being reviewed at its real size in a screenshot of the whole page. **A 190px standalone preview of the SVG alone exposed the fault in seconds.** Render small artwork at a size you can actually see.
- **`el.hidden = true` IS INERT WHEN THE ELEMENT HAS AN AUTHOR `display`.** The privacy filter hides patterns with an `is-filtered-out` class for exactly this reason: the UA sheet's `[hidden]` rule is outranked by any author `display`, so the property assignment silently does nothing.
- **STILL OPEN, AND ALL THREE ARE CHEAP:** the glass canvas is sized from `getBoundingClientRect()` in **CSS pixels with no `devicePixelRatio`** (`src/js/main.js:4189-4198`, capped 900x675), while three other components in the same file do apply one — that is the likeliest reason the pane reads softer on the owner's retina screen than in a headless capture. `layTexture`'s **mirror-brick** for non-seamless pinned textures (`src/js/main.js:4117-4118`) was measured seam-neutral to remove and the removal was offered and not taken. And `.fg-obscure-stage__scene--glass` (`src/scss/main.scss:10605`) targets **an element that is not in the DOM** — three passes were spent tuning it before three identical renders across three blur values gave it away. It is left in place, unremoved, deliberately flagged.
- Superseded, and it is the entry most likely to mislead a reader who stops at the first heading: - **THE CANVAS GLASS RENDERER IS REVERTED. TEST IS `f602b7a7` AND THE STAGE COMPOSITES EXACTLY AS LIVE DOES, 2026-08-27.** The renderer was re-commissioned by the owner on 2026-08-28 and stands; the findings below are still the best record of how the earlier attempts failed. Owner: *"they're all mostly terrible tbh. the ones that are currently on live are more realistic."* Verified rather than taken on trust — rendered three ways (Pallot reference, live's CSS, the canvas) and **live is closer on all four glasses tested**. The controller and the stage CSS block are both byte-identical to `b2420743`, checked. Live has never carried any of it.
- **THE REFERENCE I MISSED IS THE ONE THAT MATTERED.** `pallotglass.com/pilkington-decorative-glass/` carries a `Textured-<pattern>.jpg` per glass — the same red clock through every one. **A text fetch of that page reported no contextual images and I believed it instead of looking**, so an entire optical rebuild ran on a partial brief. Same family as every probe fault this file records: **a tool's summary is a probe, and a probe is not evidence until it is checked.**
- **THE PREMISE WAS ALSO WRONG.** Those photographs show the pattern's own crisp structure IS most of what you see. Deriving soft displacement from a texture and discarding its structure loses the thing that reads as glass — which is why six attempts all came back milkier than the CSS multiply they were replacing.
- Superseded: - **THE GLASS RENDERER PASSED THE OWNER'S GLASS-BY-GLASS QA PASS AT `ceeb386b`, ON TEST, 2026-08-27 (LATE NIGHT).** Twelve glasses reworked per glass in the owner's priority order; Satin, Pelerine, Stippolyte and Sycamore held as passed. **Reeded's lock is superseded by the owner's own QA** — its darkness and repetition were the multiply compositing it was locked to, and it now renders through the directional optics. The acceptance test is recorded in `AI.md`: Cassini, Autumn, Cotswold, Reeded and Minster side by side must be five different signatures. Live is unmoved at `b2420743`.
- Superseded: - **THE GLASS RENDERER IS A MATERIAL SYSTEM, NOW AT `717823a6` ON TEST, 2026-08-27 (NIGHT).** After the material types landed, a differentiation review named three look-alike clusters inside the irregular family; they were real and are separated by CHARACTER — pattern-draws-itself (Chantilly, Oak, Tribal, Mayflower, Florielle: relief up, wobble down), pure dapple (Minster broad, Autumn fine), glossy folds (Taffeta broadest, Everglade deep, Arctic chopped). Florielle left the lenticular mask because its real grammar is the INVERSE of Cassini's. Cotswold's privacy inversion — clearer at p5 than the p2 glasses — was real and is fixed.
- **A REVIEW'S REFERENCES NEED CHECKING BEFORE ITS VERDICTS ARE ACTED ON.** Two of that review's three "worst match" rulings graded Minster and Contora against the brochure's REEDED photographs — both were correct as built. The pattern is the audit lesson from August restated: a verification failure is not evidence until the probe is checked, and a judge's citation is part of the probe.
- Superseded: - **THE GLASS RENDERER IS A MATERIAL SYSTEM AS OF `a77740e2`, ON TEST, 2026-08-27 (NIGHT).** Owner correction after the first optical build: everything had converged on the Minster look, because one isotropic pipeline was scaled per glass. Four sampling physics now — directional, diffusion, cellular, lenticular — plus the irregular family; proven on three maximally different glasses first, then propagated with a per-glass audit. Live is unmoved at `b2420743`. Canvas engagement re-verified on the real test site after deploy; served JS byte-identical to local.
- Superseded: - **THE OBSCURED GLASS STAGE RENDERS REAL OPTICS ON TEST, `926b7f91`, 2026-08-27 (EVENING). OWNER-COMMISSIONED, NOT A SIXTH UNPROMPTED ATTEMPT.** The owner supplied Pilkington's Texture brochure as the benchmark and authorised changing the rendering technique. Live is unmoved at `b2420743`. Two files changed (`src/js/main.js` + compiled), no SCSS beyond the canvas gating, no data, no template, no assets — **UI, backgrounds and glass photographs untouched, Reeded and Satin locked on the approved CSS compositing.** The CSS layers remain as the no-JS fallback; a failed render sets `data-glass-render="css"` and the pane never goes blank.
- **The model: the texture drives optics instead of being the image.** Per pick, a 2D canvas (blind-visualiser precedent) flat-fields the pattern, turns its relief into a refraction field, its residual into local diffusion, and lifts the pane toward a veil that carries the local scene colour. Ten iterations against the brochure's same-scene photography, three rounds of three-judge independent panels driving them; the commit messages carry the findings. **Canvas engagement verified on the real test site** — `data-glass-render="canvas"` probed through Basic Auth, served JS byte-identical to local.
- **HONEST STATUS: the final panel still scores Cassini short of "passes as a photograph"** — unanimous "meaningfully closer than any Photoshop filter", correct construction grammar, but flat-lit 150mm sample photographs cap the fidelity. The owner's eye on the real page is the judgement that counts. **A photograph of our own obscured glass with a scene behind it beats further code**; added to `PHOTO-CHECKLIST.md` territory.
- Superseded: - **THE OBSCURED GLASS REWRITE IS REVERTED. TEST IS `2839b4bf` AND THE STAGE COMPOSITES AS LIVE DOES, 2026-08-27.** Owner: *"theyre all fucked. undo this. reeded was spot on before so lock that one in. the others were all better in their original pilkington source form."* Live is unmoved at `b2420743` and never carried any of it.
- **FIVE ATTEMPTS, ALL REVERTED:** a three-layer focus model with baked rim and clear masks; a mean-absolute-deviation normalisation across them; an SVG `feDisplacementMap` off a quantised facet map; a shine reduction on top; and 42 pre-rendered composites. **Each was measurably closer to Pilkington's own photograph on some axis and each looked worse on the page.** Measuring closer is not looking better.
- **THE REVERT WAS PROVED, NOT ASSUMED.** The stage block in the compiled CSS is **byte-identical** to `b2420743`'s, and so is the obscured-glass controller in `src/js/main.js`. The files themselves could not be checked out wholesale — other sessions' composite-door and finishes work has landed in all four since — so each was spliced at the block level and then compared.
- **REEDED IS THE ONE KEEPER, because its two faults were in the ASSET rather than in any effect**, so they survive the compositing going back. Its photograph is lit from one side, spread 130 against a set median of 49, which is what caused the tile seam — cropping to whole ribs only got it from 125 to 55. Flat-fielded and mirrored: **48.8 to 0.00**, pin doubled to 360px. And its portrait is lifted to the set's mean because it was the one near-black square in a pale column; the tile is not lifted, so the pane is as photographed. `scripts/build-reeded-texture.py` derives both and is the only script that survives.
- **`tile` versus `image` is the only structural change left standing.** The stage is the only surface that repeats; a mirrored tile in a 58px swatch reads as a chevron. Reeded is the only texture carrying a `tile`.
- **Verified on test:** page `200`, zero PHP notices, and zero references to bakes, maps, the refract layer, the SVG filter or Cassini rev3/rev4. Cassini is back on rev2 at its original 500px pin; Reeded's levelled copy is in the swatches and its seamless tile on the stage.
- Superseded: - **THE OBSCURED GLASS STAGE IS PRE-RENDERED AS OF `036a88ea`, ON TEST AND NOT LIVE, 2026-08-27.** Live is unmoved at `b2420743`. Owner instruction after four rounds of live compositing failed to reach the reference. The stage now paints **one baked image** per pattern per background; the divider clips it exactly as before.
- **THE LIVE APPROACH HAD A CEILING RATHER THAN A BAD SETTING.** Every facet in the reference is a flat wash of one tone with a hard edge, because a lens averages what it magnifies. `feDisplacementMap` moves pixels and never averages within a region, so a displaced photograph keeps its gradients and smears. Averaging per facet is trivial offline. And the obscured half was always a static composite over one of two fixed scenes, so there was never a reason to compute it per visit.
- **THIS REMOVED MORE THAN IT ADDED:** the refract and relief layers, the SVG filter, all 40 rim/clear/facet maps, the faceted-route rules and both stage flags. **42 bakes at 1.99MB in, 40 maps at 1.58MB out**, and a visitor now fetches one bake instead of a texture plus two maps.
- **TWO FAULTS FOUND AND FIXED ON TEST BEFORE THE OWNER SAW THEM.** The band quantisation speckled into thousands of fragments and read as noise until a mode filter merged them — that also halved the byte cost. And the per-facet offset used `np.roll`, which wraps, so a facet near the top showed the dark pond from the bottom as a black blotch in the corner; edge padding fixed it.
- **THE REVISION RULE WAS BROKEN WITHIN MINUTES OF BEING WRITTEN.** The blotch fix rewrote the bakes in place at `b1` — exactly what the script's own header forbids, because bakes carry no version string. Corrected to `b2` in the following commit. **A rule you have just documented is the easiest one to break.**
- **Verified on test:** page `200` with zero PHP notices, 85 `baked/b2/` references and zero to `b1` or to the removed maps, three bakes served as `image/webp`, and the background toggle proven to swap the bake by probing `--active-glass-bake` through a click — `cassini--house.webp` to `cassini--cat.webp`.
- **THE COST: the effect is fixed to the baked scenes.** A third background means re-running the script, not editing a stylesheet.
- Superseded: - **THE OBSCURED GLASS REBUILD IS ON TEST AND NOT LIVE, 2026-08-27, NOW AT `ca8ee519` AFTER A FOURTH PASS.** Live is unmoved at `b2420743`. This pass is **one stylesheet change, no deletions**, answering "too shiny on cassini".
- **THE PANE WAS BLOWING 10.8% OF ITSELF TO WHITE, and the measurement found it rather than taste.** 10.8% of pixels above 230 and 3.0% above 245; Pilkington's own photograph of the same glass has **none** above 230. Three sources turned down together — `brightness(132%)` clipping the sky, a 0.27 white veil, and `__shine`, which paints a hard diagonal white streak that reads as gloss on a pane this pale. Now **0.6% above 230**.
- **THE RIM MAP IS NOT A HIGHLIGHT SOURCE, though it is the obvious suspect and I assumed it was.** `RIM_SHADE` exceeds `RIM_LIGHT` everywhere, so the map never rises above 128 and under `soft-light` it can only darken. Four variants were generated before checking, and all four were pointless.
- **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `69989797`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed. All ten other glass PANES byte-identical.
- **AN OUTPUT PIXEL IS AN INTEGRAL, NOT A SAMPLE — and the integral is DIRECTIONAL.** Real obscured glass gathers light over a cone whose width and shape vary point to point: a rolled rib scatters widely ACROSS its length and hardly at all along it, a polished lens face barely scatters. This renderer took one bilinear sample and then averaged the result with a round blur — isotropic softness on a strongly directional surface, which is why the pane read as either too sharp or too smeared but never as glass. Samples now spread along the rib normal and collapse on the lens faces.
- **REBUILDING THE PLATE FROM THE DARK-FIELD PHOTOGRAPH WAS TESTED AND REJECTED.** It separates lens from hatch about as well as the product plate (hatch-energy ratio 2.01 against 1.96, inverted as a dark field should be) but is markedly **less bimodal, 0.147 against 0.336**, carrying torch falloff and perspective residue. **It is better for MEASURING than for SEGMENTING** — its measurements are already banked; the product plate stays as the segmentation source.
- **THE SUMMARY METRICS CANNOT SEE THIS CHANGE, and that is stated rather than glossed.** Bands moved 25.8 -> 25.6 and entropy 0.881 -> 0.878, because they measure contrast magnitude and orientation spread, **not the shape of the point-spread function**. Some real improvements are invisible to the metrics that exist; do not require a measured win before accepting one, and do not claim one either.
- **WHAT REMAINS NEEDS DATA OR A DIFFERENT RENDERER, not more tuning.** The three still open: the plate is a photograph rather than a height map (needs photometric stereo — sample and camera FIXED, only the torch moving); the renderer displaces rather than refracts, so a flat backdrop gives no parallax and no true caustics; and there is no environment for the front surface to reflect. Every measurable criterion is now inside or beside the reference bands, so further parameter work has low expected value.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `6e7026f5`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **PHOTOMETRIC STEREO WAS ATTEMPTED ON THE THREE TORCH-LIT FRAMES AND IT DOES NOT WORK — do not retry it on these.** The lighting genuinely does differ between them, so the idea was sound, but the sample, the hand and the camera all moved between shots. Registering on the label (the only reliable anchor: the glass edge is too low-contrast against cladding) aligns the LENS scale but leaves 10-20px of residual error, and **the hatch pitch is 11px** — the fine structure cannot be brought into register at all. Downsampling 8x to get inside the error and integrating the difference images returns **the torch's own illumination falloff, a smooth blob correlating -0.26 with the pattern**, not geometry.
- **WHAT A USABLE PHOTOMETRIC SET NEEDS: the sample and camera FIXED, only the light moving.** Tape or prop the sample flat against the dark cladding; prop the phone so the framing is identical in every frame; then take four shots moving ONLY the torch — left, right, above, below, roughly 45deg to the surface and far enough back (a foot or two) that its falloff across the sample is even. No zoom, no refocus, no repositioning between shots. Everything moving at once is what defeated this attempt, not the idea.
- **PHOTOGRAPH A GLASS SAMPLE AGAINST SOMETHING DARK AND PLAIN, NOT AGAINST A SCENE.** The owner's dark-cladding photographs are a DARK-FIELD view: the smooth lenses pass the dark backdrop and read dark, the hatched ground scatters ambient light and reads light, so the pattern separates itself with almost no background to subtract. It is far better source material than a sample held up to a garden, and it is the reference committed at `scripts/reference-cassini-darkfield.png` — **re-measure from it rather than re-guessing.**
- **IT CORRECTED TWO NUMBERS THAT PHOTOGRAPHS-THROUGH-A-SCENE HAD GIVEN WRONGLY.** Rib bearings are **55deg and 125deg**, symmetric about vertical and separated by 70deg — not the 45/145 measured earlier through a busy backdrop. And the hatch pitch is **1.10% of sheet width** where the renderer was at 1.86%: the hatch was **1.7x too coarse**, which is the opposite of the direction several rounds of "make the ribs bolder" had been pushing it.
- **ALSO MEASURED, for anyone re-deriving this:** lens major axis 15% of sheet width, aspect 2.0, coverage 35-40%, lens:hatch ratio about 14:1. Bands after the correction 25.8/27.1/28.8/30.5, direction entropy 0.881 against the references' 0.891-0.936, clipping 0.00%.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `d8c35a8e`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **WHAT ACTUALLY STOPS THIS BEING PHOTOREALISTIC, tested rather than asserted.** The first guess — that we lack specular glints and caustics — was **refuted**: we carry MORE extreme glint than the references (4.1% of pixels 80+ above their local surround, against 0.7-2.5%). Heavy-tail contrast and saturation are also in range. What remains is structural: **the plate is a photograph, not a height map**, so every geometric term is inferred from an image whose brightness is a product of that photo's lighting and background; **the renderer displaces rather than refracts**, so with a flat backdrop there is no depth, no parallax and no true caustic; **there is no environment to reflect** off the front surface, which is much of what makes the sample photographs read as glass; and **one sample per pixel** cannot integrate the varying lobe real obscure glass gathers over. **The cheap unlock is photometric stereo — three or four photographs of the same sample lit from different sides give a real normal map.**
- **THE BEARING DISTRIBUTION IS MEASURED, AND IT HAS A TAIL.** Six hundred directional patches across the owner's two photographs: 32% at 40-50deg, 36% at 140-150deg, and **30% spread across every other direction**. Snapping to four exact bearings reproduced the families and threw the tail away. Most regions now take a family plus a jitter the width of the measured peak; a genuine 30% keep the plate's own bearing.
- **RIBS CURVE, THEY DO NOT RULE — and the remaining alignment was INSIDE the regions, not between them.** Matching the between-region distribution moved direction entropy only 0.821 -> 0.844 against the references' 0.891-0.936. A slow bend at a far longer wavelength than the rib pitch takes it to **0.904**, on reference C's 0.902.
- **FIFTH TEMPORAL-DEAD-ZONE BUG, AND THIS ONE MASQUERADED AS SUCCESS.** The curvature used a seed declared below it, so the render threw and fell back to CSS — and that fallback measured **0.997** direction entropy, apparently a triumph, because a blur has no orientation at all. Three different curvature settings returning identical screenshots is what gave it away. **A metric can look like a win when the renderer is dead: assert the images DIFFER before reading any number off them.**
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `e865ee56`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **CASSINI'S PLATE TILES, AND IT IS CUT NOT MIRRORED.** `build-reeded-texture.py` makes Reeded tile by mirroring and warns in terms not to generalise it — ribs are near enough identical to their own reflection that the symmetry is invisible, an organic mat of lenses is not, and mirroring Cassini would put a butterfly axis down the middle of every pane. `scripts/build-cassini-seamless.py` does a **minimum-error boundary cut**: lay a band from the left edge over the right, find by dynamic programming the path along which the two disagree least so the join runs BETWEEN lenses rather than through them, then drop the consumed band so the tile's last column is the one the original continues from.
- **THE FIRST CUT FAILED ITS OWN TEST, WHICH IS WHY THE SCRIPT PRINTS ONE.** At a 140px band the wrapped joins measured 4.9 and 7.6 against interior steps of 3.9 and 4.2 — the vertical one still read. Choosing the cut on a **smoothed** cost, so it routes around whole lenses instead of threading between grains, plus three pixels of feather across the cut itself, brought both to the interior step (4.0 vs 3.7, 4.3 vs 3.9). **A seamless-tile script must measure its join against the ordinary interior step and say so; "looks fine" is not a test.**
- **A SEAMLESS SOURCE NEEDS A DIFFERENT RENDERER PATH FROM A NON-SEAMLESS ONE.** The pinned-size tiling in `layTexture` deliberately draws a DIFFERENT sub-region per tile and mirrors alternate rows and columns — machinery built to HIDE a repeat in a source that does not tile. Run against a tile that does, it defeats the purpose twice over: roaming breaks the matched edges and mirroring goes kaleidoscopic. Materials that declare `seamless` lay the tile edge to edge and nothing else.
- **BEST OBJECTIVE MATCH OF THE WHOLE SEQUENCE:** bands 28.6/30.4/31.9/33.4, every one inside the references' 28-32/29-32/30-33/32-35, ratio 1.17, clipping 0.00%. Checked at a 2200px viewport where several tiles are visible and no join reads.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `23d5ca36`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **THE LENS OUTLINES ARE TRACED FROM THE PLATE, NOT FITTED.** An ellipse fit reproduces each lens's position, size and bearing but **not its outline**, so the sheet was Cassini's statistics rather than Cassini — the owner's phrase was "a true representation, not an interpretation". The plate photograph is the real glass, so its regions are used directly.
- **TOUCHING LENSES NEED A WATERSHED, NOT A THRESHOLD.** A plain threshold merges neighbours into one blob, which is exactly what banding this same field did at the very start of this work. The bright CORES are taken as seeds, labelled, and every label grown outward at equal rate by **multi-source breadth-first search** constrained to the lens mask; where two fronts meet they stop, and that line is the join. Each label keeps the true outline of its own lens, and is lit across a principal axis computed from its actual pixels rather than from an idealised shape.
- **TRACING COSTS CONTRAST, AND IT IS RECOVERABLE WITHOUT TOUCHING THE LAYOUT.** Traced lenses cover more of the pane than fitted ellipses and lens faces carry little texture, so the bands fell to 15.8/18.0/20.2/23.2 (ratio 1.47). Letting more fluting through on the faces (`faceClear` 0.45 -> 0.25) and raising `fluteShade` put them back at 22.8/24.9/26.6/29.1, ratio **1.28**, inside the references' 1.22-1.37 — with the layout untouched. **When a structural change costs a measured quality, look for the compensating parameter rather than reverting the structure.**
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `10ca5483`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **THE PATTERN IS FITTED TO THE PLATE, BECAUSE THE PLATE IS THE PATTERN.** Ovals placed on a jittered grid had the right STATISTICS — size, aspect and spacing all measured off the sample — but not Cassini's actual arrangement, and the arrangement is what is being sold. The source photograph already carries the true layout, so each lens blob in it is segmented and an **ellipse fitted**: centroid, second moments, and from those the two axes and the bearing. The real spacing and the real spread of sizes come out for free, and no random placement reproduces them. **When a synthetic layout is wanted and a photograph of the real thing is already in the repo, fit the photograph.**
- **ONE OF MY OWN MEASUREMENTS WAS CIRCULAR AND IS RECORDED SO IT IS NOT REUSED.** The "28% lens coverage" figure quoted off the sample was obtained by thresholding at the **72nd percentile** — which forces 28% by construction and carries no information at all. The plate shows lenses packed considerably closer. **A coverage figure derived from a percentile threshold measures the threshold, not the subject.**
- **A GLOBAL THRESHOLD MERGES ADJACENT LENSES.** Fitting at a low level flood-filled neighbouring lenses into a few huge components and the pane came out sparse with big bare fluted areas. Raising the level isolates the bright cores and the fitted ellipses are then expanded by a gain to meet again, which both separates them and keeps the packing.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `2ce3032e`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **THE LENSES ARE PLACED OVALS, NOT REGIONS CARVED OUT OF THE PLATE.** They were connected components of a BANDED HEIGHT FIELD, which can only ever give amorphous blobs that merge into one another; the real sheet carries discrete ovals lying over the fluted ground and over each other. Measured off the sample rather than guessed: **major axis 12.2% of pane width, aspect 2.16:1 (range 1.18-5.59), 28% coverage.** The **depth count** — how many ovals cover a pixel — now decides everything: bare ground fluted, one oval a smooth lens, two or more the crossed overlap. No height banding involved.
- **ONCE THE LENSES STOP CORRESPONDING TO THE PLATE, EVERY PLATE-DERIVED TERM MUST BE RE-EXAMINED.** `dome` was still taking its light and shade from the plate's relief, so plate-shaped shading was being painted inside oval-shaped masks — incoherent dark blotches that **survived turning the overlap and dimple textures completely off**, which is how they were isolated. Each oval now has its own dome across its own axis, lit from its own side; running every dome the same way made the sheet read as a tray of identical shiny pebbles.
- **PER-REGION IS NOT PER-PIXEL when regions can merge.** Texture kind was assigned per region, but overlapping ovals flood-fill into ONE connected region, so a single kind was applied across a whole cluster and the overlaps vanished. Depth is inherently per-pixel and is read at the point of use.
- **FOURTH DECLARATION-ORDER BUG IN `renderGlass`, and `hash` is now hoisted to the top permanently with a note saying why.** Each one costs a full parameter sweep to notice, because the exception falls back to the CSS layer in silence and every screenshot comes back identical. **Anything used by more than one stage is declared at the top of the function.**
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `1bb2d541`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **`emboss` WAS PRINTING THE SOURCE PHOTOGRAPH'S OWN GRAIN AS SURFACE RELIEF, and the check that would have caught it was skipped.** The critique raised it 0.60 -> 0.80 as "a cheap first test for the missing petal rim" with the explicit instruction **"revert if it only adds noise inside petal faces"** — that instruction was never carried out. It was noise: a fine speckle across the whole pane that SURVIVED turning the fluting, dimples, glints and stipple almost entirely off, which is how it was isolated. **When a change is proposed as a test with a revert condition, run the test.**
- **SEVERAL ROUNDS OF "MORE DEFINITION" ACCUMULATED INTO A NET CURTAIN.** Every critique asked for more, each was acted on, and nothing ever asked whether the TOTAL had gone too far. `fluteShade` 1.90 -> 0.70, `glint` 0.45 -> 0.12, `dimple` 0.40 -> 0.12, `overlapAmt` 0.8 -> 0.35, `stipple` 0.05 -> 0.01, and a larger share of petals left smooth and glossy, which is what the sample actually shows. **Ask periodically whether the sum of accepted changes has overshot, because per-change critique never will.**
- **REMOVING NOISE ACHIEVED WHAT ADDING TEXTURE COULD NOT.** The structural bands are now 24.9/27.0/30.0/32.5 against reference C's 28.3/29.2/30.3/32.1, with a profile ratio of **1.31** against the references' 1.22-1.37 — the flattest and closest of any build in this sequence. Earlier rounds chased that same ratio by ADDING fine texture and never got below 1.47. Clipping stays 0.00% on the inner crop.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `4def3269`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **AN OVERLAP IS A THIRD TEXTURE, and the model had no representation of it.** Where two petals overlap the surface carries BOTH impressions, so it is not petal A's texture nor petal B's but the two crossed. Every region previously got exactly one texture and an overlap was simply another region. The band level IS the stacking height, so the upper bands are the overlaps; they now flute along their own bearing and along a second at a neighbouring angle. **Not the perpendicular** — that is the dimple waffle; two overlapping petals meet at whatever angle they happen to lie at.
- **TEXTURE KIND FOLLOWS STACKING HEIGHT, NOT A HASH.** On the sample the prominent glossy ovals are the TOPMOST faces and the fluting lies on what is under and between them. Drawing the kind randomly scattered smooth petals through the stack, which is part of why the shapes failed to read as one lying over another.
- **A PARAMETER'S JUSTIFICATION CAN EXPIRE, AND THIS ONE DID SILENTLY.** `petalBands` was dropped 4 -> 2 on the critique's advice to enlarge the rib-carrying sectors, which was correct at the time. The **sector lattice added in the very next commit took over that job**, so the reason had gone — but the value stayed, leaving the petals far coarser than they needed to be, and two bands cannot separate shapes that lie over one another. That is what the owner saw as the petals "blurring together". **When a mechanism is added that subsumes a parameter's purpose, revisit the parameter in the same commit.**
- **THE REGRESSION CROP INCLUDES THE WHITE PAGE MARGIN, so it reports ~2% white clipping for ANY render.** Measure clipping on the inner crop (215,40)-(565,530), not the STAGE crop — inside the pane it is 0.00% with a max of 250 against the 251 ceiling. This nearly read as a regression of the highlight rolloff.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `91b81576`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **CASSINI IS TWO POPULATIONS AT TWO SIZES, and modelling it as one is why no scale ever fixed it.** A lattice of large sectors with dead-straight seams carries the RIB BEARING, thirty or forty ribs to a sector; the ovoid lenses sit OVER that lattice as a separate, smaller population carrying the wash, the texture kind and the dome. Ours had one population doing both jobs, so enlarging the sectors enlarged the petals with them. The lattice is a **Voronoi of jittered-grid seeds** — chosen because Voronoi boundaries are straight and meet at clean vertices, which is exactly what the seams do.
- **A RIM MUST BE A LENS, NOT A DRAWN OUTLINE, and the reference proves it by contradicting itself.** Traced across the same petal three ways, the boundary reads dark-then-bright, bright, and flat. The polarity is not fixed, so it is a slope refracting whatever lies behind it. A fixed bright/dark pair prints a cartoon outline on every petal — **which is precisely the topographic-map failure this file already records from an earlier attempt that subtracted on this same gradient.** It is now a displacement along the mask normal scaled by the mask's slope: zero across the interior, zero across the ground, peaking only in the transition band.
- **A CREST BREAKS UP ALONG ITS LENGTH EVEN WITH NOTHING BEHIND IT TO BREAK.** On the sample, a rib crossing a flat featureless patch of backdrop still carries dashes and glints, unchanged in character across brown and blue areas — a property of the surface, not of the refracted content, and the test that distinguishes the two is to find a region where the backdrop has no structure. Sampled in the rib's own frame and stretched along it at a few rib pitches. **Not a sinusoid near the rib pitch**: that cross-hatches into a woven moire and at this pitch would sit on the pixel grid.
- **THE THREE-MECHANISM ROUND CAME FROM THE CRITIQUE'S "CANNOT EXPRESS" LIST, WHICH IS THE PART WORTH KEEPING.** A critique that only returns parameter tweaks will keep tuning a model that cannot represent the thing; asking explicitly what the model CANNOT express is what surfaced all three of these.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `075ed7fe`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **BLOWN HIGHLIGHTS WERE BLOCKING EVERY ATTEMPT TO ADD DEFINITION, and no one had measured it.** We clipped pure white on **1.7%** of pixels; the clock photograph and both sample photographs clip **0.01-0.03%**. The renderer stacks several positive terms — relief, dome, petal paleness, rib crown, veil — each adding a fraction of the REMAINING headroom, so bright areas pile onto 255 and any highlight added afterwards is invisible. A soft asymptotic rolloff (`knee`) takes it to 0.00%, and only THEN could the rib crowns take the definition the owner had been asking for across several rounds. **Check clipping before adding any highlight term.**
- **RIBS PER PETAL-CELL IS THE SCALE TEST, and it is magnification-invariant so it survives the scene caveat.** Ours ~12 against 17-30 across the three references. `flutePeriod` 11 -> 6.5.
- **CONTRAST AT THE FINEST SCALE IS NOT A TARGET TO CHASE — a render can hit it with pure grain.** A candidate that scored best on fine-scale contrast (28.6, dead on the sample's 28.8) was visibly SPECKLED; the metric cannot tell structure from noise. All three references are photographs carrying sensor noise and sharpening at 1px, so the number is partly camera. **Match the structural bands (0.8%-6.4% of pane width) and confirm by eye.** The R-G chroma correlation test does not separate them either — the references are near-monochrome, so their fine detail is luminance too.
- **THE DIMPLED PETALS WERE PER-PIXEL WHITE NOISE, which only showed up when the ribs got bolder** — noise scales with amplitude where structure does not. Measured on the sample the dimple pitch EQUALS the flute pitch (13.0 against 13.2px), so a dimpled petal is simply the same fluting run in both directions, crossing into lenslets that refract. Damped to 0.40, because at full strength a crossed waffle is no more photographic than the noise it replaced.
- **FREE PER-REGION RIB ANGLES READ AS A VORONOI FIELD.** The real sheet resolves to essentially two bearings meeting along straight seams; ours had eight or more unrelated ones whose seams curve and reverse. Quantised to four families. Per-rib amplitude now varies too: reference crests vary up to 5.8x between neighbours (CV 0.47) where ours were uniform to 5% (CV 0.02), and that regularity is what read as machined.
- **A MULTI-LENS CRITIQUE WITH ADVERSARIAL VERIFICATION EARNED ITS KEEP: nine findings survived, FIFTEEN were refuted.** The refutations mattered as much as the findings — they killed additive bloom, raising `fluteShade` for specularity, raising `petalSharp`, inverting the face/ground diffusion split, and depth-ordered petal instances. One caught a self-inflicted measurement error: the "staircasing" visible in my own zoom crops is an artefact of 5x nearest-neighbour blow-up, not the renderer. **Verify findings against the images before acting; roughly two thirds did not survive.**
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `1eea9008`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **THE COMPLEXITY IS PER PETAL, AND A GLOBAL FIELD CANNOT REACH IT.** The owner: "each petal has a distinct texture that overlaps with others. you've put a general geometric pattern across where it's more complex and detailed irl." That is a structural fault, not a tuning one — this renderer laid ONE field across the whole sheet (two fixed line families, patch-selected), so however the patches were weighted it could only ever be a general pattern. **Rounds were spent tuning the weighting of a model that could not represent the thing.** The pane is now segmented by banding the petal field and flood-filling each band, and each region gets its own rib angle **measured from the plate's own fine texture inside that region**, its own texture kind (fluted, smooth or dimpled), and its own rib period.
- **WHEN THE OWNER DESCRIBES STRUCTURE, CHECK THE MODEL CAN EXPRESS IT BEFORE TUNING PARAMETERS.** "A combo of textures" was said three times across this work. Each time it was answered by adjusting one texture's strength or its patch selection, because the model only had one texture to adjust. The answer was always to add mechanisms, not to retune the one that existed.
- **A THIRD DECLARATION LEFT OUTSIDE THE SCOPE OF ITS USE** — `jxx`/`jyy`/`jxy` were block-scoped to the branch that fills them, and the segmentation needs them. With `fluteTone` and `hash`/`shiftAmt` before it that is three, and **each one costs a whole sweep** because a render exception falls back to CSS in silence. Declare at the top of `renderGlass`, and run `/tmp/guard.sh`-style checks so a dead render is caught by the screenshots being identical.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `e1e16265`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **CASSINI'S HATCH IS FLUTING, NOT LINE WORK, and this is the largest single correction in the whole sequence.** At native resolution on the owner's own sample the ribs are bold and ROUNDED, and they visibly SMEAR the scene into streaks across their width: they are cylindrical lenses, the same optics `rib` already uses for Reeded, laid at each patch's own angle. This renderer spent several rounds drawing dark LINES and nudging the sample a fraction of a pixel. **That puts a pattern ON the scene; fluting makes the pattern OUT OF the scene**, and that is the difference between a texture overlay and glass. The rib highlight is a specular CROWN, not a symmetric sine — narrow highlight, broad valley — because the asymmetry is what makes a rib read as round rather than as corrugation.
- **"A COMBO OF TEXTURES" MEANT THREE MECHANISMS, AND IT WAS SAID TWICE BEFORE IT WAS ACTED ON.** Fluted ribs in angled patches, the pebble lenses, and the dimpled stipple. **Enumerating them from native-resolution crops of the sample took one render and should have been the response the FIRST time the owner used that phrase** — several rounds were instead spent varying the strength of a single texture.
- **A THIRD HARNESS FAILURE MODE, AND THE WORST: THE LOCAL SERVER HAD DIED.** Chrome loaded an empty `main.js` and every screenshot was the CSS fallback; four parameter sweeps were run and reasoned about against it. **The served bundle hashed to `da39a3ee` — the SHA-1 of the empty string — which is what gave it away.** There is now `/tmp/guard.sh`, run before every screenshot, which refuses to trust a render unless the SERVED bundle matches the built one. Between them the three modes are: a render exception falling back to CSS, a pinned `?ver=` serving a cached bundle, and the server being gone. **All three produce byte-identical screenshots across different inputs, so that symptom means "harness", not "no effect".**
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `0c1be8e2`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **CASSINI'S HATCH NOW RUNS IN PATCHES AND THE FAULT WAS A FLOOR IN `wa`/`wb`.** The weights carried `1 - hatchBias * 0.85` on BOTH families, so at the strongest setting the losing family still held about **14%** — and these are DARKENING lines, not a blend of tones, so 14% of a dark line is a perfectly visible dark line. Both families drew everywhere and the sheet rendered as an even allover mesh **however decisive the selector was**. The weights now sum to one: where the selector commits, one family is drawn and the other is gone, and crossings survive only in the transition bands between patches, which is where the sample has them.
- **THREE ROUNDS WERE SPENT ON THE WRONG HALF OF THAT MECHANISM.** The selector was re-derived from the fine detail, then from the broad relief, its blur radius swept, and a percentile re-normalisation added — all while the real fault sat downstream in the weighting. **Dumping the intermediate field to the canvas is what settled it, and it settled it in one render**; the same trick had already found the petal-mask bug earlier in this work. When a field-driven effect looks uniform, paint the field before touching how it is computed.
- **DARKENING TERMS DO NOT BEHAVE LIKE BLENDS.** A weight that looks negligible as a proportion — 14% — is fully visible when what it scales is a dark line against a pale ground. Any "small residual" left in a subtractive term needs checking against what it draws, not against its share of the total.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `ac6dd9ca`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. All ten other glass PANES byte-identical.
- **THE OWNER'S OWN SAMPLE PHOTOGRAPHS ARE THE REFERENCE NOW, NOT THE PALLOT SHOT** (`IMG_5168`/`IMG_5169`, a Pilkington Cassini sample held to the light). They overturned a figure this file has been quoting for days: **hatch pitch ~24px against a pebble period of ~400px, so the true ratio is about 17:1, not 41:1**. The 2.6px "hatch" measured off the Pallot image was a finer SUB-texture, not the hatch lines. Ask for a photograph of the actual product before spending days measuring someone else's photograph of it.
- **THE HATCH IS BOLD AND THE PATCHES ARE LARGE.** In most patches one family runs almost purely — 0.05 relative power against 1.00 — and only some cross (0.82). `scale` is 1.6 because of the ratio, not by eye: at plain `cover` the pebbles land near 50px, which forces the hatch to ~3px, and **at 3px it cannot draw as bold lines without becoming a chequer**. Scaling the pattern up puts the hatch at 5px where it reads as line work.
- **STILL NOT RIGHT, AND THE NEXT ATTEMPT SHOULD NOT START WHERE THIS ONE DID.** The sample's hatch runs in large patches of PARALLEL lines meeting at sharp boundaries, a diamond lattice; ours is a more even allover mesh. `hatchSel` was moved onto the broad relief for this, and **dumping it to the canvas proves it already forms large decisive patches (0..255, std 86)** — the selector is not the fault. The remaining problem is in how `wa`/`wb` weight the two families into the output. Do not re-derive the selector.
- **A LARGE BLUR ON A 0..1 SELECTOR COLLAPSES IT TOWARDS ITS MEAN.** Widening the patch radius flattened `hatchSel` to ~0.5, both families weighed equally, and the sheet rendered as one uniform mesh — the opposite of the intent. It is re-normalised across its own percentiles afterwards. Note this also means a selector taken from FINE detail cannot work at patch scale at all: blur it that far and there is no large-scale structure left to keep.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `650d1163`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value.
- **RENDER THE LIVE VERSION SIDE BY SIDE BEFORE TUNING, NOT AFTER.** Two rounds were spent pushing Cassini's pattern contrast up, and one of them made it far worse — the owner's "looks too processed, doesn't show the scene enough behind it" was caused by the previous commit raising `dome` to 1.6 and `emboss` to 0.5. **Rendering `b2420743` next to it settled the direction in one image**: plain CSS multiply shows the tree, the house and the water plainly, and the reference's pebbles are mostly PALE with subtle shading rather than the near-black-and-white discs this had produced. The owner said months ago that live looked more realistic than the renderer; that comparison is cheap and should be the FIRST thing pulled up when a judgement call about "realism" is in question.
- **"TOO PROCESSED" AND "DOESN'T SHOW THE SCENE" ARE THE SAME COMPLAINT, AND NEITHER IS FIXED BY MORE OBSCURING.** Measured across every round, the surviving scene structure already matched the reference — edge p90 6.3-8.1 against 6.0-7.1, mid-scale rms 10.2-13.3 against 10.4-13.5. Adding pattern contrast on top of a correct quantity is solving the wrong problem. **Obscure by SCRAMBLING**: `strength` 44 and `pebbleShift` 24 make each pebble show a sharp piece of the scene from somewhere it did not come from, with low flattening (`groundFlat` 0.45) and gentle pattern contrast (`dome` 0.6). Fragments of real detail in the wrong places, not a smooth wash. **Reducing the blur RADIUS instead is worse** — it makes the photograph recognisable while still obviously blurred.
- **THE 6/255 "HARNESS NONDETERMINISM" IS SWATCH DECODE, NOT THE RENDERER — now located rather than assumed.** Six glasses flagged `max diff 6` in one sweep; mapping the differing pixels put every one of them inside a 40x40 SIDEBAR SWATCH THUMBNAIL of an unrelated glass, identical bounding box and pixel count across different pages. **Diff the stage region, not the whole screenshot**: on that measure all ten glass panes are byte-identical. Earlier rounds wrote this off as random noise, which was right about the conclusion and wrong about the cause.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `009cf03d`.** Live is unmoved at `b2420743`. Deletion list **EMPTY**; served bundle re-hashed to the repo's value. **All ten other glasses pixel-diffed identical**, with cache-busting on, against `e33b99af`.
- **"COMPUTERISED BLUR" WAS NOT A SHORTAGE OF OBSCURING, IT WAS A SHORTAGE OF PATTERN.** Measured first: the surviving scene structure ALREADY matched the reference — edge p90 6.3-8.1 against 6.0-7.1, mid-scale rms 10.2-13.3 against 10.4-13.5. The quantity was right and the KIND was wrong. Heavy averaging leaves only smooth gradients, and a smooth gradient is what a Gaussian looks like however well its statistics are tuned. The fix was **more pattern, then less averaging**: `dome` 0.5 -> 1.6 and `emboss` -> 0.5 so the pebbles carry real light and shade, after which `groundFlat` could come down 0.90 -> 0.66 and `faceFlat` 0.82 -> 0.48 and let the scene through as fragments. **Reducing the blur RADIUS instead was tried and is worse** — it makes the photograph recognisable while still obviously blurred.
- **A RENDER FAILURE FALLS BACK TO CSS SILENTLY, AND FOUR PARAMETER SWEEPS WERE READ OFF THE FALLBACK.** The pebble flood fill called `hash()` and `shiftAmt`, both declared BELOW it, so it hit them in the temporal dead zone and threw. Every screenshot came back identical whatever the material said, and those images were reasoned about at length — the "blurred photo with a faint pattern" that seemed to prove a direction was simply the CSS layer. **Hash the screenshots and assert they DIFFER when the inputs differ, before reading anything into them.**
- **THE RENDER HARNESS PINS `?ver=` ON `main.js`, SO THE BROWSER SERVES A CACHED BUNDLE.** Six parameter sets produced byte-identical screenshots for this reason alone, before the temporal-dead-zone bug was even found. Rewrite that query per run. Note this would also make a REGRESSION look clean, so the sweep that matters most is the one most exposed to it.
- **`npm run build 2>&1 | tail -2` RETURNS TAIL'S EXIT STATUS, NOT THE BUILD'S.** A failing build returned 0 and the `&&` chain carried on to commit and push: `64c83b46` shipped a `src/js/main.js` that does not compile, fixed in `009cf03d`. The bundle in it was the last good build so the site was unaffected, but the source could not be rebuilt from it. **`node --check` PASSED on that file and esbuild did not** — it is not a sufficient gate for this bundle. Check the build's own exit code.
- Superseded: **THE OBSCURED GLASS WORK IS ON TEST AND NOT LIVE, 2026-08-28, NOW AT `e33b99af`.** Live is unmoved at `b2420743` and has never carried any of this. Deletion list asserted **EMPTY**; the served bundle re-hashed to the repo's value.
- **CASSINI'S "LIGHT STIPPLE THAT'S ALMOST SEE THROUGH" IS THE TWO HATCH FAMILIES CROSSING, AND SUPPRESSING IT WAS A SELF-INFLICTED WOUND.** They were deliberately forced to nearly binary one commit earlier, on the reading that the fine chequer they produced was an artefact to be removed. **It was the feature.** An FFT of the reference's fine band returns BOTH families at comparable power in every patch sampled — 1.00 and 0.93, 1.00 and 0.80, 1.00 and 0.98 — and the dot mesh where they cross is the see-through stipple the whole sheet carries. What was actually wrong with the chequered build was that it was far too DARK. **Before removing a texture as an artefact, measure whether the reference has it.**
- **THE WHOLE-FRAME VIEW OF THE REFERENCE SHOULD HAVE BEEN THE FIRST THING LOOKED AT, NOT THE TENTH.** Rounds of work were done on 70x60px crops chosen blind. Drawing the crop boxes on a full-frame copy first showed immediately that the pebbles are DOMES with strong light-to-dark gradients and that the red passes through them — neither of which any earlier crop had made obvious, and both of which were wrong in the render.
- **A PEBBLE IS NOT OPAQUE AND IT IS NOT FLAT.** Averaging each one to a flat wash is what fixed the blurriness, but taken to completion it made them flat discs. `washMix` holds them between the flat average and the softened scene, and `dome` transfers the plate photograph's own per-pebble shading directly rather than modelling it — the source already carries it.
- **THE HARNESS NONDETERMINISM KEPT FIRING AND THE RULE HELD EVERY TIME.** Two more captures flagged `max diff 6` on glasses nothing had touched (Digital, Reeded); both were the capture, not the code, confirmed by re-render. **Suspect the baseline first — three times now it has been the baseline that was bad.**
- **CASSINI WAS OBSCURING BY BLURRING, AND THAT WAS THE ROOT OF ALL THREE FAULTS THE OWNER NAMED** -- "looks blurry", "multiple textures of slightly stippled and hatching", "the ovals are more defined". The reference obscures with dense CRISP texture and stays sharp everywhere; a smear at pebble scale is exactly what reads as blur. **A lens does not blur what it shows, it INTEGRATES it**: every pebble in the photograph is one flat tone with a hard edge to the next, which is how the sheet obscures completely and still looks crisp. Each pebble is now flood-filled over the petal mask and given the mean of what is behind it, instead of a fixed-radius blur. That one change defines the ovals and removes the softness. **`LIVECHANGES.md` had already identified per-facet averaging as the ceiling on the blur approach on 2026-08-27 and it sat unused for a day** -- read the existing entries before re-deriving them.
- **THE GROUND AND THE FACES WERE THE WRONG WAY ROUND, AND MEASUREMENT SAID SO.** On the photograph the hatched ground varies at broad scale by an rms of 6.7-12.9 and the pebble faces by 18.6; ours had 19.3 on both. A dense screen scatters over a wide angle, so it transmits near-uniform light and reads MILKY almost regardless of what is behind it, while a pebble passes its own wash through. The renderer had faces flattening hardest.
- **THREE TEXTURES, NOT ONE, AND THE FACES ARE NOT POLISHED.** Cross-hatch, stipple, pebbles. At native pixels the faces carry their own granular etch -- fine-band rms 22 against the ground's 33 -- so the grain is damped on them rather than removed. An earlier pass modelled them as smooth, which is part of why they read as ghosts.
- **THE HATCH IS RENDERED COARSER THAN PHYSICAL, DELIBERATELY, AND THAT IS A JUDGEMENT NOT A BUG.** At this pane size Cassini's screen works out at about 1.2px, below what a pixel grid can show: render it truthfully and it averages away to flat grey, which is not what the owner is asking to see. **2.6px, the measured pitch, lands where a 52deg diagonal rasterises into DOTS rather than lines**; 3.2px draws as continuous line work.
- **CORRECT ANTIALIASING DESTROYS THIS HATCH, so the supersampling is deliberately wrong.** Widening the span from about a seventh of a pixel to a proper box across the pixel dropped the fine-band rms from 30 to 5 -- there is barely more than one sample per line at this pitch. The narrow span only takes the edge off; what actually stops the crossings chequering is picking ONE line family per patch rather than blending both.
- **A NEARLY BINARY PICK NEEDS A SLOWLY VARYING SELECTOR.** Taken per pixel the family selection flips on noise in the orientation field and draws hard triangular wedges -- a geometric herringbone across the sheet. Blurred to roughly a pebble's width first, the patches fall where the reference's do.
- **TWO NUMBERS THAT MUST NOT BE TUNED AGAINST, both recorded because both were nearly chased.** The reference's fine-band rms of 33.6 **includes the photograph's JPEG noise and grain**; matching it numerically means adding noise for its own sake. Its pane-wide tonal spread of 37 against our 16 is **mostly the red clock against our garden** -- the scene, not the glass, and the same trap as the histogram warning above.
- **THE HARNESS IS NONDETERMINISTIC BY UP TO 6/255, PROVEN.** Three separate regression sweeps flagged a different glass each time at exactly `max diff 6` (Reeded, then Autumn, then Minster). Rendering Reeded TWICE FROM THE SAME BUILD reproduced it: run1 vs run2 = 6, while that build against the new one = 0. **Re-render before believing a small diff, and re-capture the baseline rather than the candidate** -- on two occasions it was the baseline capture that was bad, not the change.
- **CASSINI'S HATCH IS A FIXED CROSS AT 2.6px, AND THAT WAS MEASURED, NOT JUDGED.** Eighteen patches across the owner's clock photograph came back at 30-50deg and eighteen at 130-150deg, every one at a 2.6px pitch. It is two fixed line families, not a field that follows the pattern; the patchwork comes from which family dominates locally. **The FFT reports the wave NORMAL, which is 90deg off the line angle** — setting 39 where 52 was wanted put the whole cross 13deg out and the error was invisible by eye. It is also SYNTHESISED rather than lifted from the plate's fine detail, because deriving it from the plate ties the screen's pitch to the pebble size, so any change of scale coarsens the screen with it.
- **TWO BUGS WERE ONLY FINDABLE BY DUMPING THE RENDERER'S OWN FIELDS OUT AND LOOKING AT THEM.** Reasoning about the code found neither, and a standalone reimplementation of the same maths in Python produced the CORRECT result both times, which made the source look innocent. **The petal mask was reading the high-passed field**: that pass subtracts a 26px-radius blur and Cassini's pebbles run to ~100px, so it removed the brightness difference that defines them and the threshold caught the residual at the boundaries — a connected lattice instead of stones. **A rim term drew a dark ring round every pale pebble**, and a pale blob inside a dark ring is a contour, so the pane read as a topographic map. **Add a debug branch that paints the intermediate field to the canvas before theorising about a field you cannot see.**
- **A PARAMETER SWEEP THAT ISOLATES ONE TERM AT A TIME BEAT EVERY HYPOTHESIS.** Rendering four variants — paleness off, lens cut to a quarter, flattening reduced, current — showed the rings vanished with the paleness off and survived everything else, which named the cause in one pass after several wrong guesses.
- **TWO MEASUREMENT SCRIPTS SILENTLY RETURNED THE SAME NUMBER FOR EVERY INPUT and nearly drove a wrong decision.** An autocorrelation petal-size probe answered 184px for all four scales, and a band-passed FFT answered 140px for all four; both were saturating at the end of their search range. The renders provably differed (distinct hashes, mean abs diff 7.8). **If a metric returns an identical value across inputs you know differ, the metric is broken — check that before believing it.** The scale decision was made on the visual sweep instead.
- **THE REFERENCE'S PEBBLE SIZE IS NOT A TARGET FOR THE PANE.** An earlier pass enlarged the plate 1.5x to match pebble size in the photograph, but that photograph is a 150mm sample held near the camera and the visualiser is a window: matching it put four pebbles across the pane against the reference's dozen. `inc/site-data.php` already warned that enlarging a photographed pattern twice over reads as coarse blobs. Left at plain `cover`.
- **CASSINI'S PLATE WAS THE WORST-LIT TEXTURE IN THE SET AND THAT NOW MATTERS.** Low-frequency spread **168 against a set median of 70**, worse than Reeded was at 154; flat-fielding takes it to 55. It is a `tile` and not an `image`, exactly as Reeded does it, so swatches and the hero wall keep the photograph — a flat-fielded plate looks flat when it is merely shown. This only became load-bearing once the mask started reading absolute brightness.
- **THE REGRESSION CHECK IS A PIXEL DIFF AGAINST THE PREVIOUS COMMIT, AND IT EARNED ITS KEEP TWICE.** Florielle shares the `hatchlens` kind, so every shared-path edit had to be gated behind `hatchPitch`; the diff caught two leaks that reasoning had missed — a smoothstep added to the petal mask, and a `0.45` factor introduced into the emboss. Florielle, Minster, Reeded, Autumn, Digital and Sycamore all finish at **zero**. **Three intermediate `max diff` of 6/255 were transient bad screenshots, not code**: the affected glass moved between runs (Reeded, then Autumn, then Minster) and none survived a re-render, including a re-captured baseline. Re-render before believing a small diff.
- **DO NOT READ THE REFERENCE'S TOP END AS A TARGET FOR THE PANE.** That photograph has a dim room behind it; ours has a sunny exterior with white render and open sky. Matching its histogram would mean darkening our scene, not fixing our glass. The comparison is only valid on the glass, not on the whole frame.
- **THE ASPECT-STRETCH DIAGNOSIS IN THE ENTRY BELOW IS WRONG, and three more hypotheses died with it.** All four were tested rather than reasoned about. **The stretch**: rebuilt with `preserveAspectRatio="xMidYMid slice"`, which is SVG's `cover`, and the relief back on `cover` — it looked **worse**, blobbier and muddier. **The displacement scale**: 3, 6, 10, 16, 20, 32 and 52 were all rendered; below about 16 the pattern vanishes into plain blur and above it the facets smear, and no value sits between. **The rim map as a highlight source**: it cannot brighten, its shade term exceeds its light term everywhere. **The scene being too smooth at petal scale**: measured, and ours has MORE local contrast than the reference's room, 21.1 against 16.5.
- **WHAT ACTUALLY SEPARATES US FROM THE REFERENCE IS THAT ITS FACETS ARE FLAT WASHES.** Each lens in that photograph averages what it magnifies down to nearly one tone, with a hard edge to the next. **A CSS or SVG filter cannot do that over an arbitrary image**: `feDisplacementMap` moves pixels, it does not average within a region, so a displaced photograph keeps its internal gradients and reads as a smear. That is a ceiling on the live-compositing approach, not a tuning problem, and it is why four rounds of tuning did not close it.
- **PRE-RENDERING CLEARS THAT CEILING and a proof was built.** The obscured half of the stage is a static composite of one texture over one of two fixed scenes, so it can be computed offline where per-facet averaging is trivial. A numpy proof renders visibly closer to the reference than the live version. The cost is 21 textures x 2 scenes of baked images instead of the map set, and the effect becomes fixed to those scenes. **Not built: it is an architecture change and it is the owner's call.**
- **A PROBE FAULT, AGAIN: the minifier strips quotes from attribute selectors.** A post-build grep for `[data-glass-facets="yes"]` returned zero and looked like the rules had not compiled; they had, as `[data-glass-facets=yes]`. Grep the compiled CSS for the unquoted form.
- Superseded: - **THE OBSCURED GLASS REBUILD IS ON TEST AND NOT LIVE, 2026-08-27, WAS AT `3f3c61c3` AFTER A THIRD PASS. CASSINI IS NOT SETTLED.** Live is unmoved at `b2420743`. Deletion list was **40 files plus the emptied `maps/r2/` directory**, asserted before the rsync; the first attempt asserted 40 and aborted, which is the guard working rather than a fault. **`grep -c` returning zero exits non-zero, so it kills a `set -e` script** — the assertion needs `|| true`.
- **BLUR CANNOT MAKE GLASS DISTORT ANYTHING, AND TWO REVIEW ROUNDS WERE SPENT BEFORE THAT WAS OBVIOUS.** The owner pointed at Pilkington's own Cassini photograph: every petal carries its own displaced sample of the room, hard-edged. Blur does not MOVE anything, so no tuning of it could ever produce that. Cassini now runs the scene through an SVG `feDisplacementMap` off a **quantised** map — a smooth gradient of the photograph was tried first and rendered as heat haze, because continuous displacement smears rather than facets. Scoped to Cassini by a `facet` key; the other nineteen are untouched.
- **IT IS CLOSER IN TONE AND STILL WRONG IN SHAPE, and that is recorded rather than glossed.** Measured against the reference crop: saturation **reference 9.7%, round two 27.7%, now 13.5%**; luma 136.6 / 108.4 / 158.6. So the paleness is close. But the petals read as smears, because the facet map and the relief are sized `100% 100%` to align with `feImage`, and a 900x992 source stretched into a 693x520 pane squashes round petals into elongated ones. **The fix is to bake the maps pre-cropped to a fixed aspect rather than stretching them at render.** Reducing the displacement scale was tried first and changed nothing, so the scale is not the fault.
- **The deeper limit is the source.** Our texture is a photograph of a flat-lit 150mm sample; the reference is glass in situ with a room behind it. A displacement field derived from flat-lit etching is an inference about geometry, not a measurement of it. A photograph of our own Cassini with a scene behind would be a better basis and is a `PHOTO-CHECKLIST.md` item, not a code change.
- **REEDED'S PORTRAIT IS LIFTED and its tile is not.** Owner instruction. The display copy goes to mean 147.4 against a set mean of 148.6, from 118.5; the mirrored tile stays at 118.5, so the pane is byte-for-byte the one already approved. That is exactly what splitting `image` from `tile` was for.
- Superseded: - **THE OBSCURED GLASS REBUILD IS ON TEST AND NOT LIVE, 2026-08-27, NOW AT `71affb36` AFTER A SECOND REVIEW PASS.** Live is unmoved at `b2420743`. The second pass answers three owner notes: Chantilly "too inky", Cassini "not defined enough ... transparency is good though", and "reeded looks good but the thumbnail for it doesnt". **No SCSS or JS changed in it**, so both compiled assets are byte-identical to the first pass and were not rebuilt.
- **THE FIRST TWO NOTES WERE THE SAME NUMBER.** Each rim map is scaled to a fixed mean absolute deviation from 128 now. Native MAD ran **Cassini 13.6 to Chantilly 29.4**, a 2.2x spread, because MAD measures how much of the pane gets shaded and by how much. At a target of 21 Cassini gains ~55% definition and Chantilly loses ~30% of its ink. **Standard deviation was tried first and barely moved either — spread is not coverage.** The clear mask is untouched because the transparency was approved.
- **THE REEDED THUMBNAIL WAS THE MIRROR FROM THE FIRST PASS.** Seamlessness is a property of tiling and the stage is the only surface that tiles; the swatch squeezed all 1800px into 58px with the mirror axis dead centre and read as a chevron. Reeded carries two assets now: `image` for anything that displays it, `tile` for the one surface that repeats it. **The darkness of that swatch is pre-existing and is the photograph** — mean 118.5 against a set mean of 148.6 — not the mirror, and it is still there.
- **THE MAPS MOVED TO `maps/r2/` AND THAT DELETION LIST WAS 40, ASSERTED BEFORE THE RSYNC.** The rim recipe changed and maps carry no version string, so rewriting them in place would have left the reviewer's browser on r1. `git` reported 20 renames plus 20 deletes; **rsync saw 40 deletions**, which is what the assertion was set from, and it was checked that none fell outside the maps folder.
- Superseded: - **THE OBSCURED GLASS REBUILD IS ON TEST AND NOT LIVE, 2026-08-27: `76324e6b`.** Live is unmoved at `b2420743`. Deletion list asserted **EMPTY** and the dry run agreed; 50 changes, 44 of them new files. **Both compiled assets moved and both were rebuilt from source before shipping** — the last four releases guarded them as expected-unchanged, so a copied-forward guard is wrong for this one.
- **THE PANE COULD NOT HAVE A CLEAR BIT BY CONSTRUCTION, and that is the reusable part.** Owner report: the glass read "black/white rather than ... the actual glass with transparent bits". The stage painted the texture over a blurred scene with `mix-blend-mode: multiply`, and multiply only darkens — the brightest a "transparent" area could reach was "unchanged". **No amount of retouching a photograph could have fixed it**, which is why the first correction shipped, measured clean and changed nothing. The giveaway was already in the code: the scene layer carried `brightness(120%)` purely to claw back what the multiply took. It is now three layers of the same photograph varying FOCUS rather than brightness, so every pixel is scene colour. Mean saturation over the Cassini pane **24.0% to 32.1%**. Approved as option F2, chosen against four textures at once rather than against Cassini alone.
- **THE REPEAT WAS FOUR TEXTURES OUT OF TWENTY-ONE, not all of them.** Only a pinned `--texture-size` tiles; the other seventeen are `cover` and paint once. Cassini, Minster and Stippolyte gave up their pins. Reeded kept one — a rib has a real width and renders about five times too fat at `cover` — and was flat-fielded and mirrored instead: **seam 48.8 to 0.00**. **Reeded turned out to have the same lit-from-one-side fault as Cassini**, low-frequency spread 130 against a set median of 49, which is why cropping it to a whole number of ribs only got the seam to 55. The rib phase was never the problem.
- **THE NEW SITEGROUND IMAGE-TRANSFORM WARNING WAS TESTED AGAINST THIS RELEASE AND THE MAPS SURVIVE.** Twenty of the new files are alpha masks, and a flattened alpha would render a privacy 5 glass see-through — the worst available failure. Three were fetched from test and checked **on the alpha channel, not on the byte count**: md5 identical, mode still RGBA, alpha mean 75.7 with min 0 and max 255. WebP passes through untouched; the reported flattening was a PNG.
- **A CASCADE TRAP THIS PAID FOR:** two earlier blocks in `main.scss` also target `.fg-obscure-stage__glass::after` and the later one sets `multiply` at half opacity, so the replacement sheen inherited both and shipped as a darkening layer — the exact fault being removed. **Found by grepping the compiled CSS for `multiply` after the build, not by reading the diff.**
- **Verified on test as a visitor:** seventeen routes `200` with **zero PHP notices**, both new layers rendering, `data-glass-maps="yes"`, zero stale references to the two renamed textures across four routes, and the served stylesheet re-hashing to the local build (`c0da67bd…`). Rendered at 1100px across Cassini, Reeded, Charcoal Sticks and Satin: saturation 29-30% on the three photographed ones, 18.3% on Satin, which correctly falls back to plain frost because it is a gradient and has no maps.
- **NOBODY HAS DRAGGED THE DIVIDER IN A REAL BROWSER.** Headless throttles the animation frame, so every state and all the wiring is verified and the feel of the drag is not. Same limit as the roofline story.
- **This file carries `## Current Truth` twice**, from another session on 2026-08-27. Left alone rather than tidied mid-release; the lower one is the live section.

- **SITEGROUND TRANSFORMS IMAGES IN TRANSIT, AND IT BROKE AN ASSET CHECK ON 2026-08-27.** A PNG on the test server hashed **identically** to the repo copy on its own disk, and the bytes the browser received hashed **differently**. That is the host optimising images on the way out, and it flattened a transparent PNG back to an opaque one. **Hashing a served asset proves a CSS or JS deploy. It does not prove an image one** — for images, look at the rendered pixels. This sits alongside the existing rule that theme image URLs carry no version string; the two failure modes are different and both bite.
- **`/why-distinction/` IS ON TEST AT `3b8fff46`, NOT LIVE, 2026-09-02.** Live is unmoved. Five commits, one of them a revert of my own mistake — see the next bullet, which is the one worth reading. Owner: *"but you have images for the boxes. youve kept the same layout which i dont like."*
  - **THE DE-BOX CSS FROM THE PREVIOUS PASS WAS WRITTEN AND NEVER APPENDED TO `main.scss`, SO NONE OF IT SHIPPED.** It went to a scratch file, the scratch file was never `cat >>`'d onto the stylesheet, and the session recorded the work as done. `.fg-wd-band` still had a 16px radius and a box-shadow, the caveats were still a bordered white card, and the cutaway explorer was still a card inside a card inside a card. **The owner was looking at a page that had every box the previous pass claimed to have removed.** Grep the built CSS for a selector you believe you shipped before writing that you shipped it.
  - **DE-BOXING IS NOT A LAYOUT CHANGE, WHICH IS THE OTHER HALF OF WHAT HE SAID.** Removing borders leaves the same skeleton: head-then-text, head-then-text, on the same pale ground. Three sections were still text-only while `assets/images/products/composite-distinction/gallery/` sat unused. Each one changes shape now — the 50% takes a full-bleed dark band (the page's second dark moment, breaking a pale run that went from the hero to the form), the six figures get a ruby-red door beside them and drop to two columns, and the two exits become photographs of the doors they lead to, black Chatsworth for the range and anthracite for the quiz.
  - **BOTH NEW COMPOSITIONS OPENED A HOLE DOWN THE MIDDLE, FROM THE SAME CAUSE.** A grid track sized by a fraction, with content that stopped well short of it. On the band the claim prose sat under the numeral in a 610px track with a 44ch cap: **290px of dark nothing down the centre of the section.** On the figures the door was 444px tall beside a 900px column: **half the left side empty canvas.** Fixed by moving the prose into the right column so both tracks are sized to their content, and by making the door sticky rather than stretching it — stretching a 4:5 portrait to a 0.45 track crops the door's own edges off.
  - **THE TABLET WIDTH FOUND TWO MORE.** The caveats' 62ch cap was written for the two-column band and applied at every width, so stacked at 768 each `li` rule stopped 200px short of the `h3` rule above it. And the mobile door was a 16:10 crop of a 4:5 portrait, which shows the middle half: a letterbox of glass and handle rather than a door. Scoped the cap to `min-width: 861px` and squared the crop.
  - **Verified on test at 1440x900, 768x1024, 390x844 and 320x700**, zero console errors, zero failed requests, seven images all loading, no horizontal overflow at 320. `/why-distinction/` is 6,446px desktop. Regression on `/composite-doors/`: 12,771px unchanged, its own anatomy explorer still at its own 16px radius, so none of the `.fg-wd-page`-scoped de-box rules leaked.

- **I COMMITTED THREE FILES BELONGING TO ANOTHER SESSION AND ONE OF THEM WOULD HAVE FATALLED TEST.** `git add -u <theme dir>` swept in `functions.php`, `package.json` and `package-lock.json`, which were sitting modified in the working tree. `functions.php` had been changed to `require_once` `inc/experimental-home.php` and `inc/showroom.php`, **and both are untracked**, so they are not on `main`. The require loop has no `file_exists` guard and the test deploy is `git reset --hard origin/main` then an rsync of the theme, so the next deploy — mine or anybody's — would have delivered a `functions.php` requiring two files that were never sent. Reverted on `main` at `0a037a76` and the working copies restored byte-for-byte, so the other session still has its edits to commit when it is ready. **Stage by path in a repo two sessions are working in. `git add -u` is not a safe default here.**

- **TEST IS NOW THIRTY-ONE COMMITS PLUS THE SECOND COMPOSITE PASS AHEAD OF LIVE, 2026-08-27.** Live is unmoved at `b2420743`. The second pass answers the owner's review of the first: a full-bleed hero on a Distinction studio render, the style range as a contact sheet, the quiz as a full-bleed dark band with BuzzFeed's grammar, and the finishes chapter head rebuilt as an index. `/composite-doors/` is **12,998px desktop and 17,943px mobile**, from 13,909 and 21,513 before the overhaul.

- **LIVE IS STILL `b2420743`. TEST IS TWENTY-SEVEN COMMITS AHEAD OF IT, 2026-08-27, AND NONE OF IT IS APPROVED.** Nine of those are this session's composite doors and `/why-distinction/` overhaul (`fdee3d6b..03ca2f63`), on top of the eighteen already recorded below. **Twenty theme files: eight added, twelve modified, none deleted**, so a release would still assert the deletion list EMPTY. The additions are six hero WebPs, the new `composite-anatomy.php` component and the overhaul brief.
  - **Read the range before shipping it**, per the standing rule: `git log --oneline b2420743..<SHA>`. It now contains three sessions' work.
  - **`main.css` and `main.js` both changed**, so a release carries a build. `main.scss` grew by about 1,500 lines, all appended in one marked block.
  - Content: `/composite-doors/` is five chapters rather than seventeen sections, with a live slab cutaway built from the supplied design handoff; `/why-distinction/` gained two photographs, lost three em dashes and gained a route back to the range and the quiz; `/why-distinction/` is now linked from the footer and from two composite FAQ answers.
  - **Verified on test at 1440x900, 768x1024 and 390x844**, both pages, zero console errors, zero failed requests, no horizontal overflow at 390 or 320. `/composite-doors/` 13,909 to 13,683px desktop and **21,513 to 17,818px mobile**; `/why-distinction/` 4,689 to 5,413px desktop, which is the two photographs and a closing chapter it did not have.
  - **The FAQ component gained optional `link` and `link_label` keys.** Additive and unused on every other route, but it is a shared component, so a regression there would show site-wide.

- **LIVE IS STILL `b2420743` AND TEST IS EIGHTEEN COMMITS AHEAD OF IT, 2026-08-27. NOTHING WAS DEPLOYED TO LIVE THIS SESSION AND THE RANGE HAS NOT BEEN APPROVED.** Fourteen of the eighteen are the composite doors work below, one is another session's obscure glass fix (`31a8b992`) and three are docs. **148 theme files added, 8 modified, none deleted**, so a release would assert the deletion list EMPTY. The additions are almost entirely the 151 door line drawings at ~900 bytes each.
  - **Read the range before shipping it**, per the standing rule: `git log --oneline b2420743..<SHA>`. It contains another session's commit as well as this one's, and re-establish live by checksum first rather than trusting this line.
  - **`/why-distinction/` is a new indexable route** and is in the sitemap list in `inc/generated-pages.php`. A release adds a URL to `page-sitemap.xml`; check the count moves by one.
  - Content: `/composite-doors/` gains the 142-door style range, the decorative glass section that had been gated off it, a five-question quiz that ends in the quote tool, and a through-link to the new page.
- **THE COMPOSITE DESIGNER'S CRM EVENT WAS OFF AND IS NOW ON.** Owner ticked `Door_designer_submitted` in Account → CRM on 2026-08-26 after a submission created a WindowCAD project and emailed the office while nothing reached WordPress. Proven fixed by enquiry `10424`: `_fenster_adminbase_sent = 1`, status `200`, token returned. **`tracking=` is still dropped by that designer** — `_fenster_journey_ref` and `_fenster_ads_tracker` were both empty and `_fenster_windowcad_fields` carried no Tracking key at all. Owner accepted the loss on 2026-08-26; it is a WindowSoftware fix, not ours.
- **THE ACCESS LOG CANNOT ANSWER A QUESTION ABOUT TODAY.** `~/www/fensterglazing.com/logs/` holds only rotated `.gz` files, written at about 04:30, so the current day's traffic is not readable until tomorrow's rotation. To check whether a WindowCAD lead arrived, list `fenster_enquiry` posts instead — that is the thing the relay actually creates.
- **A SERVER-SIDE `curl` FROM THE SITEGROUND BOX TO `test.fensterglazing.com` RETURNS 403 FOR EVERYTHING**, including routes that plainly exist, and returns a 75KB block page rather than the site. It is the WAF, not the deploy. Verify test through the Browser pane or `scripts/shot.mjs` with the Basic Auth credentials in the URL.

- **LIVE IS `b2420743`, LEVEL WITH `main`, AND TEST IS LEVEL WITH IT, 2026-08-26 (11:35). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** Five commits over `3285863b`, four of them docs. **Four theme files modified, nothing added, nothing deleted, nothing renamed**: `inc/consent.php`, `inc/website-tracking.php`, `src/js/main.js` and `template-parts/sections/generated-page.php`, the last two comment-only. Deletion list asserted **EMPTY** and the dry run agreed at zero across six itemised changes. **Both compiled assets are byte-identical across the whole range** — the only `src/` change is a comment the minifier strips — so they were guarded as expected-unchanged rather than rebuilt, and there was no build hazard at all. Backup `fenster-pre-3285863b-to-b2420743-20260826-113043.tar.gz` (466M, 2,127 entries) **proven** by extracting `inc/consent.php` from the tarball and hashing it to the pre-deploy value `f5301413…`. Pruned to three, 1.3G; disk at 86% with 316G free. Deployed by explicit SHA, not `origin/main`. Socket purge `msg:OK`, and `wp sg purge` failed on live again, exactly as this file has recorded since July. Content: the Google Ads tag that every conversion call has been firing into a shim.
- **LIVE WAS RE-ESTABLISHED BY CHECKSUM ON SIX FILES AND RETURNED TWO COMMITS AT 6/6**, `3285863b` and its docs-only successor `e87d9a64`, which share a theme tree. The documented ambiguity rather than a failed match, and **the recorded line was right for the third release running**.
- **THE GUARD RAN ON TEN EXPECTED-UNCHANGED FILES AND ALL TEN RE-HASHED IDENTICAL**, captured in the seconds before the rsync: `assets/js/main.js`, `assets/css/main.css`, `functions.php`, `inc/enquiries.php`, `inc/adminbase.php`, `inc/ad-attribution.php`, `inc/google-ads-conversions.php`, `inc/site-data.php`, `inc/generated-pages.php` and `inc/traffic-classification.php`. The four changing files then re-hashed to the repo's values for `b2420743` exactly.
- **VERIFIED ON LIVE AS A VISITOR WITH NO CACHE-BUSTER**, and from content rather than a cache header: seven routes `200` with **zero PHP notices**, `function loadGoogleAds` and `gtag/js?id=AW-808336148` both present in the served HTML, `var trackingAllowed = true` for a normal browser user agent, and the published config carrying `"conversionId":"AW-808336148"` with `"enquiryLabel":"r6edCOm2ztgcEJT2uIED"` — so the id resolves through the new shared constant on production.
- **THE CONSENT LAYER WAS REGRESSION-CHECKED ON LIVE AND IS UNCHANGED.** Both banner strings serve byte-for-byte as before, the granted-by-default `marketing: true` and the recorded-not-required `chosen: false` are intact, GTM still loads and the Meta pixel is untouched. That was the owner's hard requirement on this change and it is verified on production, not inferred from the diff.
- Superseded: **ONE THEME COMMIT WAS ON TEST AND NOT LIVE, 2026-08-26: `418a8761`, the Google Ads tag loader.** Live was unchanged at `3285863b`. Two PHP files, `inc/consent.php` (53 insertions, **zero deletions** — no consent default, validator, gate, banner string or withdrawal path altered) and `inc/website-tracking.php` (one literal replaced by the shared constant). **No `src/` file changed, so both compiled assets are byte-identical and there is no build in this release.**
  - **What it fixes:** every enquiry, consultation, phone-click and quote-open conversion has been calling `gtag('event', 'conversion', ...)` into a dataLayer shim with no Google tag loaded anywhere in the theme, and returning as though it had worked. See `GOOGLE-ADS-SETUP.md` 6a.
  - **Verified on test**, on a real browser user agent: with marketing granted the tag loads, the consent signal is queued **before** the config, and `AW-808336148` registers as a gtag destination — which is the thing that makes `send_to` resolve. With marketing refused the loader does not run at all: zero `config`/`js` pushes.
  - **ONE TEST CONVERSION WAS FIRED FROM TEST ON THE OWNER'S INSTRUCTION, 2026-08-26, AND GOOGLE ACCEPTED IT.** `gtag('event', 'conversion', ...)` was called exactly as `sendGoogleAdsConversion('enquiry')` calls it. The request was `www.googleadservices.com/pagead/conversion/808336148/` carrying **`label=r6edCOm2ztgcEJT2uIED`** — the *Enquiry form submitted* action — with `en=conversion` and a 1,644-byte response. **`gcs=G111`** on the same request is the Consent Mode signal and confirms ad_storage and analytics_storage were both reported granted. That is the whole path proven end to end: consent, tag, destination, label, delivery. **There is now one unattributed test conversion in the account**, fired from `test.fensterglazing.com` with no click id, dated 2026-08-26; discount it when reading the first day's figures. The action can take about 24 hours to move off **Inactive**.
  - **The consultation and phone-click actions were NOT exercised**, so each stays Inactive until its first real one. Same one-line call if they are ever wanted from test.
  - **THE BROWSER PANE CANNOT VERIFY TRACKING WITH ITS DEFAULT USER AGENT, and this will catch the next person.** Its UA contains `Claude`, so `fenster_request_is_bot()` classifies it as a crawler, `trackingAllowed` renders `false`, and **no tag loads at all** — which looks exactly like a broken deploy. Switch the pane to the mobile preset, which swaps in an Android Chrome UA, and reload. Confirmed both ways with `curl`: same URL, `trackingAllowed = false` on the pane UA and `true` on a plain Chrome UA. The crawler filter is working correctly; the probe was the problem, which is the fault family this file keeps recording.

- **LIVE IS `3285863b`, LEVEL WITH `main`, AND TEST IS LEVEL WITH IT, 2026-08-25 (15:35). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** Seven commits over `6c01e4cc`, all this session's. **Nineteen files transferred, 4,985,641 bytes**; six modified, thirteen added (twelve JPEGs and one script). Deletion list asserted **EMPTY** and the dry run agreed at zero across twenty-seven itemised changes. Backup `fenster-pre-6c01e4cc-to-3285863b-20260825-153500.tar.gz` (443M, 2,114 entries) **proven** by extracting `assets/css/main.css` from the tarball and hashing it to the pre-deploy value `be2f299f...`. Pruned to three, 1.3G; disk at 86% with 315G free. Deployed by explicit SHA, not `origin/main`. Socket purge `msg:OK`, and `wp sg purge` failed on live again with `'sg' is not a registered wp command`, exactly as this file has recorded since July. Content: the Milton Keynes composite front door study, the `story` rail it introduced, and three SEO faults fixed behind a committed harness.
- **THE `x-proxy-cache: MISS` CHECK THIS FILE PRESCRIBES NO LONGER WORKS, AND THE PROOF HAD TO COME FROM CONTENT INSTEAD.** Live now returns only `x-proxy-cache-info: DT:1` and no `x-proxy-cache` header at all, and that value was **byte-identical on two consecutive plain requests**, so it separates a hit from a miss not at all. Freshness was established the way it should have been all along: the served HTML carried **eleven `data-fg-story-step` occurrences** that did not exist on live ten minutes earlier, and the **served stylesheet re-hashed to `f3df27ef...` at 731,650 bytes** with the new `.fg-cs-story__rail` rule in it, with the served bundle matching at `3f8b3680...`. **Hash the served asset and grep the served HTML for something only the new build emits; do not trust a cache header to still be the one that was documented.**
- **THE GUARD RAN ON TEN EXPECTED-UNCHANGED FILES AND ALL TEN RE-HASHED IDENTICAL.** The whole tracking strand plus the files a case-study release has no business touching: `functions.php`, `inc/consent.php`, `inc/website-tracking.php`, `inc/ad-attribution.php`, `inc/scan-links.php`, `inc/adminbase.php`, `inc/enquiries.php`, `inc/site-data.php`, `inc/generated-pages.php` and `template-parts/sections/generated-page.php`. Plus an assertion that `scripts/check-case-studies.php` did **not** already exist, and seven changing files that all re-hashed to the repo's values afterwards. Both compiled assets rebuilt **byte-identical** from source before shipping, so there was no build hazard in this release at all.
- **LIVE WAS RE-ESTABLISHED BY CHECKSUM ON SEVEN FILES AND RETURNED THREE COMMITS AT 7/7**, `84ab4fb5`, `ebc1a157` and `6c01e4cc`. All three share theme tree `741ccb30...` because the two newer ones are docs-only, so this is the documented ambiguity rather than a failed match, and **the recorded line was right for the second release running**. `6c01e4cc` is the tip of that identical tree and is what the range was read from. Note the discriminator lesson still holds: `assets/css/main.css` alone tied across six commits, and `assets/js/main.js` plus `inc/case-studies-data.php` are what cut it down.
- **`CASESTUDIES.md` CHANGED IN THIS RANGE AND DID NOT DEPLOY, WHICH IS CORRECT.** It lives at `app/public/`, outside the theme folder the rsync points at, so repo documentation never reaches a server. Do not read a docs file in `git diff --name-status` as a shipped file.

- Superseded: **LIVE WAS `ebc1a157`, LEVEL WITH `main`, AND TEST IS LEVEL WITH IT, 2026-08-21 (10:55). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** Eleven commits over `f1fbbc94`: ten on the bi-fold configuration rail and the stale docs commit that was already sitting above live. **Twenty-four files transferred, 3,281,619 bytes**; six modified, eighteen added (seventeen WebP renders and one component). Deletion list asserted **EMPTY** and the dry run agreed at zero across thirty-three itemised changes. Backup `fenster-pre-f1fbbc94-to-ebc1a157-20260821-105446.tar.gz` (443M, 2,095 entries) **proven** by extracting `assets/css/main.css` from the tarball and hashing it to the pre-deploy value `bfa5c372…`. Pruned to three; disk at 85% with 342G free. Deployed by explicit SHA, not `origin/main`. Socket purge `msg:OK`. **Verified on live with no cache-buster at `x-proxy-cache: MISS`**: eleven routes `200` with zero PHP notices, seventeen slides and six jump buttons rendering, all seventeen config images `200`, the picker shipping `hidden`, and the served `main.css` re-hashed to `be2f299f…` with the new `.fg-bfc__rail` rule present. Content: `/aluminium-bifold-doors/` gains a swipe rail of Sheerline's seventeen published Prestige configurations.
- **THE GUARD RAN ON EIGHT EXPECTED-UNCHANGED FILES AND ALL EIGHT RE-HASHED IDENTICAL.** The whole tracking strand plus Legend: `inc/consent.php`, `inc/website-tracking.php`, `inc/adminbase.php`, `inc/ad-attribution.php`, `inc/traffic-classification.php`, `inc/enquiries.php`, `inc/generated-pages.php`, `inc/legend-assistant.php`. Both compiled assets also rebuilt **byte-identical** from source before shipping, so there was no build hazard in this release at all.
- **LIVE WAS RE-ESTABLISHED BY CHECKSUM ON SIX FILES AND RETURNED TWO COMMITS AT 6/6**, `f1fbbc94` and its docs-only successor `d5caee84`. Their theme trees are byte-identical (`a58ec4ee…`), so the pair is the documented ambiguity rather than a failed match, and the recorded line was right. **`assets/js/main.js` was the discriminator**, per the 19 August lesson: keying on `main.css` alone returns false positives because a compiled stylesheet is constant across long stretches.
- **A DIRECTORY WAS ADDED AND REMOVED INSIDE THE RANGE AND IT NETS TO NOTHING.** `assets/images/products/sheerline-bifold-configurations/` (nineteen files) shipped to test, was superseded by `sheerline-bifold/` when the renders were re-exported with alpha, and was deleted. Live never held it, so `git diff --name-status f1fbbc94..ebc1a157` shows zero deletions and the rsync dry run agreed. **A deletion inside a range is not a deletion against live** — check the net diff, not the commit log.
- **THREE PROBE FAULTS IN ONE SESSION, ALL IN THE FAMILY THIS FILE KEEPS RECORDING, AND EVERY ONE LOOKED LIKE A BROKEN DEPLOY.** A CDP screenshot reported the old markup because Chrome served its own cache and `Network.setCacheDisabled` had not been set. A `while read` loop over a Python-written URL list rejected eighteen of nineteen URLs with `curl: (3) Malformed input to a URL function`, because the file carried CRLF and the trailing `\r` went into the URL. And the served-stylesheet check hashed the empty string to `da39a3ee…` because the grep looked for `href="` while this theme emits `href='` with single quotes. **When a verification says the deploy is broken, re-read the probe before re-reading the deploy.**
- **The six Gibson font 404s per live page are still there and are still not ours.** `https://fensterglazing.com/fonts/Gibson-*.woff2|otf`, requested page-relative by Microsoft Clarity at runtime, first recorded 2026-08-18. Confirmed again on this release by capturing every failed request on `/aluminium-bifold-doors/`: six, all Clarity fonts, none touching `sheerline-bifold`, `main.css` or `main.js`. The real fonts load from their absolute URLs first and nothing is broken visually. Still worth chasing separately.
- Superseded: **LIVE WAS `f1fbbc94`, LEVEL WITH `main`, AND TEST IS LEVEL WITH IT, 2026-08-19 (11:55). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** Twenty-one commits over `dbc9cc75`, the largest release this month: eight on `/slide-fold-doors/`, four on the Hanslope barn study, eight on the new `/care-and-maintenance/` page, and one backing out another session's work. **Thirty-five files transferred, 17,069,369 bytes**, most of it the two new videos. Deletion list asserted **EMPTY** and the dry run agreed at zero across forty-nine itemised changes. Backup `fenster-pre-dbc9cc75-to-f1fbbc94-20260819-115514.tar.gz` (430M) **proven** by extracting `assets/css/main.css` from the tarball and hashing it to the pre-deploy value `fe6dfaa6…`. Pruned to three; disk at 84% with 366G free. Deployed by explicit SHA, not `origin/main`. Socket purge `msg:OK`. **Verified on live with no cache-buster at `x-proxy-cache: MISS`**: nine routes `200` including the three new ones and four regression checks, zero PHP notices on any of them, all six new assets serving (both videos at full size), `care-and-maintenance` in `page-sitemap.xml`, and the served `main.css` re-hashed to `4e6c6344…` matching the local build exactly. Content: the slide and fold rebuild, the Hanslope study, and the care guides.
- **LIVE WAS RE-ESTABLISHED BY CHECKSUM, AND THE FIRST ANSWER WAS WRONG.** Six theme files were hashed on the server and matched against every version in history. Keying on `assets/css/main.css` alone returned `e90fd519` at 4/6, which is a false positive: **`main.css` is constant across long stretches, so it narrows a range rather than naming a commit.** Re-keying on `assets/js/main.js`, which is rebuilt whenever any asset changes, returned `dbc9cc75` and its docs commit `4bd40acc` at a clean 6/6 and nothing else. **Pick the discriminator that changes most often, and never accept a partial match as the answer.**
- **THE CARE GUIDES RANGE CONTAINS THREE COMMITS THAT CANCEL OUT.** `2ac39e59` rebuilt `/downloads/` in place, `3b68bb03` amended it, and `fc60dbbb` reverts both. The net diff against the pre-work tree is empty and `data/pages.json` is untouched, so **`/downloads/` shipped unchanged** and is still the noindexed scrape page it was. Verified `200` on live after the release. They are noise in the log, not a change.
- **`<button>` WRAPS ITS CHILDREN IN AN ANONYMOUS BOX, SO `grid-template-columns` ON A BUTTON DOES NOTHING.** Found on the care guides picker: the compiled CSS was byte-for-byte what was written and the render was still wrong, with the photo above the label instead of beside it. There is no check that catches this short of looking at the page, which is why it survived a deploy. The fix, and the pattern to reuse, is to position children absolutely inside the button the way `.fg-care-guide__hero` already did; if a multi-column button is genuinely needed, put the layout on a span inside it rather than on the button.
- Superseded: **LIVE WAS `dbc9cc75`, LEVEL WITH `main`, AND TEST WAS LEVEL WITH IT, 2026-08-18 (12:29). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** Five commits over `575eae99`, one of them the previous docs entry. **Twelve theme files changed and eight of them are new media**: the Drayton Parslow photographs, poster and video, plus `inc/case-studies-data.php`, `template-parts/sections/case-studies-residential.php`, `src/scss/main.scss` and `assets/css/main.css`. Deletion list **EMPTY**, confirmed by `git diff --diff-filter=D` across the range. **`assets/js/main.js` is byte-identical to live's, sha1 `a42bd802…`** — it was rebuilt by accident mid-range and put back in `65767fba`, so the compiled-JS hazard was created and closed before shipping rather than avoided. Backup `fenster-pre-dbc9cc75-20260818-122836.tar.gz` taken before the rsync and pruned to three; disk at 84%. Deployed by explicit SHA, not `origin/main`. Socket purge `msg:OK`. **Verified on live with no cache-buster at `x-proxy-cache: MISS`**: the `--tall` modifier in the markup, `aspect-ratio:3/4` in the served stylesheet, the re-cut photograph at 113,550 bytes and the video at 3,529,815, the study on `/case-studies/` and `/composite-doors/`, and its URL in `page-sitemap.xml`. Regression check: `/case-studies/flush-casement-windows-and-composite-door-wolverton/` still renders `fg-cs-gallery__masonry` with no modifier. **This release did not run the file-level checksum guard the previous four did.** Content: the Drayton Parslow case study, and a gallery cell an all-portrait study can opt into.
- **MICROSOFT CLARITY REQUESTS THE GIBSON FONTS AT A PAGE-RELATIVE PATH ON LIVE, AND EVERY LIVE PAGE 404s SIX OF THEM.** Found during this release's verification and **not caused by it**: `/case-studies/fonts/Gibson-Regular.woff2`, `.otf` and four more, with the base changing per page, which is what a document-relative fetch looks like. The theme's own `@font-face` in `#fenster-critical-css` is absolute and correct on both hosts, and no `.otf` appears in either HTML, so these are made at runtime rather than from markup. Reproduced on `/composite-doors/` and on the Wolverton study, **neither of which this release touched**. It does not reproduce on test, where Clarity is present in the markup in the same 18 places but does not appear to record behind Basic Auth. Twelve console errors per live page. **Nothing is broken visually** — the real fonts load from the absolute URLs first — but it is noise in the console and wasted requests, and it is worth chasing separately.
- Superseded: - **LIVE WAS `575eae99`, LEVEL WITH `main`, AND TEST IS LEVEL WITH IT, 2026-08-17 (14:00). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** Two commits over `d042d45a`, one of them docs. **Three theme files changed**, all of them this release's: `assets/css/main.css`, `src/scss/main.scss` and `template-parts/sections/case-studies-residential.php`. Live was re-established by checksum on six files and matched a contiguous 6/6 run from `2d301490` to `2422ca90`, whose theme trees are identical because the newer ones are docs-only; `d042d45a` is the deployed one and the recorded line was right for once. Guard passed on **fourteen** files in the seconds before the rsync — three changing and **eleven expected-unchanged, the whole tracking strand plus `assets/js/main.js`** — and all fourteen re-hashed to their target values after it. Deletion list asserted **EMPTY** and the dry run agreed at zero. Backup `fenster-pre-575eae99-20260817-140055.tar.gz` (425M, 2,062 entries) **proven** by extracting `case-studies-residential.php` and hashing it to the pre-deploy value. Pruned to three; disk at 84%. `main.css` rebuilt byte-identical from source before shipping, and the **served** stylesheet re-hashed to `f3a99026…` at 709,376 bytes with the new rule present, so the proxy is serving the new one. **`assets/js/main.js` provably unchanged across the whole range**, so there was no compiled-JS hazard at all. Socket purge `msg:OK`. Content: the Show more button on both case study archives.
- **THE SHOW MORE BUTTON HAD NEVER WORKED, AND THE JAVASCRIPT WAS NOT THE PROBLEM.** Owner report, 2026-08-17: every card was visible and the button did nothing. `[data-fg-case-studies-archive]` in `src/js/main.js` was correctly setting `card.hidden = true` on everything past the initial count — but `.fg-cs-card` sets `display: grid` and `.fg-cs-card--overlay` sets `display: block`, and **an author rule outranks the browser's own `[hidden] { display: none }`**. So nothing was ever hidden, and the click unhid already-visible cards and removed the button. `.fg-cs-card[hidden]` fixes both archives at once. **This is the failure mode to remember: `element.hidden = true` is inert on anything the stylesheet gives a display to**, and it fails silently in exactly the way that looks like broken JavaScript. The codebase already handles this per component — `[hidden]` overrides exist for the consultation stages, the enquiry steps, the lightbox and the Legend panel — so add one whenever a new component both sets `display` and gets hidden from script.
- **The residential archive now opens on six cards rather than four**, matching commercial, on the owner's instruction. Both archives read their count from `data-fg-case-studies-initial`, which is now a literal `6` rather than a commercial/residential branch. Thirteen residential and nine commercial studies ship in the HTML either way: the deferral is JavaScript-only, so a visitor without it still gets the complete archive.
- Superseded: - **LIVE WAS `d042d45a`, LEVEL WITH `main`, AND TEST WAS LEVEL WITH IT, 2026-08-16 (07:43).** Two commits over `0d116b8a`, one of them docs. **A single theme file changed**, `template-parts/sections/generated-page.php`; both compiled assets byte-identical, deletion list asserted **EMPTY**, dry run agreed. Live was re-established by checksum on five files and matched `0d116b8a` exactly. Guard passed on **eleven** expected-unchanged files — including `inc/site-data.php`, guarded deliberately so that only ONE file was permitted to move — and all eleven re-hashed identical afterwards. Backup `fenster-pre-d042d45a-20260816-074331.tar.gz` (425M, 2,062 entries) **proven** by extraction. Pruned to three. Socket purge `msg:OK`.
- **THIS RELEASE REMOVED A FALSE CLAIM ABOUT WHO DOES THE WORK, AND IT WAS LIVE IN TWO PLACES.** Owner correction, 2026-08-16: the own-installers claim does not cover roofline installation. The canonical order step 03 said "Fitted by our own installers rather than subcontractors" and the shared outside-FENSA aftercare string said "you are talking to the same people who fitted it". **Step 03 had been kept on this route deliberately two commits earlier, with "the canonical installation step is true here" written in the commit message.** It was assumed, never checked, and it shipped. **When a bespoke route takes the canonical rail, read every step against that product, not only the ones that look wrong.**
- **THE ROUTE NOW SAYS NOTHING ABOUT WHO FITS IT, IN EITHER DIRECTION**, on the owner's instruction not to state it and not to state the opposite. Step 03 describes the work rather than the workforce; step 04 keeps only that the customer rings us. **04 also drops "we look after our own work rather than passing you on"**, which reads as the safe half to keep and is not: it asserts an aftercare arrangement nobody has confirmed here. **The inverse of a false claim is not a safe claim, it is a different unverified one.**
- **Scoped, and verified scoped.** `/secondary-glazing/`, `/double-glazing-replacement/` and `/integral-blinds/` all still carry both shared strings on live; only roofline is overridden, and `order_process.aftercare_outside_fensa_and_cpa` itself is untouched. The case-study strip's "fitted by our own installers" stays, because it describes the studies shown, which are window and door jobs, and already says they cover the range rather than this product. Full rule in `AI.md` under the Roofline Rule.
- Superseded: - **LIVE WAS `0d116b8a`, LEVEL WITH `main`, AND TEST IS LEVEL WITH IT, 2026-08-16 (07:19). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** Six commits over `e945c600`: the roofline FAQ rewrite, the access FAQ and three on the process rail. Live was re-established by checksum on six files and returned `e945c600` **uniquely** — no docs-commit ambiguity this time, because the docs commit in the range sits above a data change. Working tree clean, no release branch.
- **THE SMALLEST-SURFACE RELEASE THIS MONTH: TWO FILES MODIFIED, NOTHING ADDED, NOTHING DELETED, NOTHING RENAMED.** `inc/site-data.php` and `template-parts/sections/generated-page.php`. Deletion list asserted **EMPTY** and the dry run agreed. **Both compiled assets are byte-identical across the whole range**, so they were guarded as expected-unchanged rather than rebuilt and there was no build hazard at all. Guard passed on **eleven** expected-unchanged files — both compiled assets, the whole tracking strand, `inc/assets.php`, `inc/generated-pages.php` and `functions.php` — and all eleven re-hashed identical afterwards, with the two changing files landing on their targets. Backup `fenster-pre-0d116b8a-20260816-071939.tar.gz` (425M, 2,062 entries) **proven** by extracting `inc/site-data.php` and hashing it to the pre-deploy value. Pruned to three. Socket purge `msg:OK`.
- **THE PROCESS RAIL WAS MAKING A PROMISE `/roofline/` CANNOT KEEP, and that is the reusable part.** Step 01 read "Price it yourself on the online tool" and the rail's intro opened "The first one has two ways in: price it yourself online". **Roofline is in neither `$product_quote_embeds` nor any WindowCAD collection** — it is one of the few product routes with no quote embed, and the page renders none. So a live page was telling visitors to price a roof edge on a tool that does not price it. Step 02 was worse in kind if not in effect: a factory survey naming "thresholds", on a product with no threshold that is not made to survey sizes. Fixed as per-route overrides inside the branch replacement glazing already uses, which overrides three of four, rather than as a fifth hand-written step set.
- **AN OVERRIDE PLACED ABOVE THE ASSIGNMENT IT OVERRIDES IS SILENTLY DISCARDED, AND ONLY FETCHING THE PAGE SHOWED IT.** The first pass put the `$journey_order_*` overrides ~20 lines above the unconditional assignment block, so the steps changed and the eyebrow and intro did not — the rail kept the exact sentence the commit existed to delete. It linted, it deployed, and the diff looked right. Same shape as the matrix FAQ schema going on the wrong render block. **The roofline overrides are now in two blocks either side of that assignment and the split is deliberate; there is a comment saying so.**
- **The FAQs are the questions customers actually ask**, replacing a set that opened with "What counts as roofline?" — which the hero and the four guide cards already answer — and carried "Why do people replace timber with uPVC?", a trade framing in the third person. Five render and the same five feed the schema, so a sixth renders nowhere. **Access is answered now** on owner detail supplied 2026-08-16, including that scaffolding is a third party either way so a customer may arrange their own. **Still unanswered and still with the owner:** cost, timescale, capping over versus full replacement, waste removal, and guttering on its own.
- Verified as a visitor on the apex with no cache-buster: thirteen routes `200` with **zero PHP notices**, the five new FAQs rendering with **five** `Question` nodes in the schema, the rail reading "Roofline process", and `price it yourself` / `online tool` / `threshold` all returning **zero** on the route while `/secondary-glazing/`, `/double-glazing-replacement/` and `/integral-blinds/` all still carry the canonical rail — the override is scoped.
- Superseded: - **LIVE WAS `e945c600`, LEVEL WITH `main`, AND TEST WAS LEVEL WITH IT, 2026-08-15 (20:15). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** **Forty-four commits over `34e902d6`**, and forty-three of them are the `/roofline/` rebuild — the largest single-route release this project has done. Live was re-established by checksum on **six** files including **both** compiled assets, which returned `34e902d6` and its docs-only successor `ecc4cf42`; their theme trees are identical, so both are the same answer and this is now the **fourth** recorded instance of that pattern. Working tree clean, no release branch.
- **THE RANGE WAS PROVED TO BE ONE AUTHOR AND ONE ROUTE.** All forty-four commits are Nick Baker's and every one is roofline or docs. `git diff --name-status` over the theme returned **27 added, 6 modified, ZERO deleted, ZERO renamed**, so the deletion list was asserted **EMPTY** and the dry run agreed at zero before the rsync ran. Thirty-seven files transferred.
- **`assets/js/main.js` IS PROVABLY UNCHANGED ACROSS ALL FORTY-FOUR COMMITS.** It is not in the diff at all, and a local rebuild reproduced both artefacts byte-identical — the JS to live's own hash `5690526b…`. There was no compiled-asset hazard in this release whatsoever. `main.css` also rebuilt byte-identical from source before shipping, and the **served** stylesheet on the apex re-hashed to `1a032be3…` afterwards, so the proxy is serving the new one.
- **GUARD PASSED ON FIFTEEN FILES AND ALL FIFTEEN RE-HASHED AFTERWARDS.** Seven changing and **eight expected-unchanged — the entire tracking strand plus both compiled assets** — proven byte-identical between live and the release before anything moved, then re-checked after. Plus an assertion that `template-parts/components/roofline-colour-rail.php` did **not** already exist. Backup `fenster-pre-e945c600-20260815-195844.tar.gz` (424M, 2,031 entries) **proven** by extracting `inc/site-data.php` and hashing it to the pre-deploy value. Pruned to three; disk at 84%. Socket purge `msg:OK`.
- **Verified as a visitor on the apex with no cache-buster:** twenty routes `200` with **zero PHP notices**, the section order reading hero → colours → guide, 21 colour slides, both rail groups, the specification-choices band absent, `/colour-options/` returning **zero** on the route, the head-term template assertion passing on `/double-glazing-milton-keynes/`, and `page-sitemap.xml` at **714** URLs.
- **`sitemap.xml` IS AN INDEX, NOT A URL LIST, AND COUNTING `<loc>` IN IT REPORTS 1.** It points at `page-sitemap.xml`, which carries the 714. A verification that greps the index and concludes the sitemap has collapsed is the probe being wrong, which is the fifth session running for that lesson. **There are also no `/roofline-<town>/` matrix routes** — roofline is not in the town matrix, so `/roofline-bletchley/` correctly 404s, and the comments in `inc/site-data.php` claiming the gallery pool "feeds every `/roofline-<town>/` matrix page" are inaccurate and were inherited, not introduced.
- **ONE THING TO PUT TO THE OWNER:** the case-study fallback strip on `/roofline/` shows work across the range, and one card names **Liniar** in a casement study summary. That is correct for a casement study and it is the only supplier name rendering on a route whose rule says no supplier is named. It is not a breach — the rule is about roofline's own distributor — but a reader could associate it.
- Superseded: - **LIVE WAS `34e902d6`, 2026-08-15 (end of day). `main` IS `d5581c56` AND THIRTY-ONE COMMITS SIT ABOVE LIVE, ALL DELIBERATELY UNSHIPPED.** Thirty of them are the `/roofline/` rebuild. **TEST IS `80905485`, ONE COMMIT BEHIND `main`** — the roofline colour photography was pushed and never deployed, so deploy test before reviewing anything. Working tree clean, no release branch outstanding. Theme diff over live is **32 files, zero deletions**, so a release would assert the deletion list EMPTY. No SCSS was left unbuilt and `assets/js/main.js` is untouched across the whole range.
- **ONE COMMIT FROM THAT WORK IS ALREADY LIVE AND IT WENT IN THE EARLIER RELEASE.** `afd9e161`: `product_usps['roofline']` was telling Legend that roofline carries a ten year guarantee, contradicting the windows hub FAQ in approved live copy. The strip does not render on that route, so no page could contradict it and the assistant was the only reader. **Second time that array has done exactly this** — the repairs entry claimed the same until 2026-08-06. When a route stops rendering its strip, re-read what it still feeds Legend.
- **THE ROOFLINE REBUILD IS THE UNSHIPPED WORK.** `/roofline/` now runs the shared `data-fg-aw-story` scroll canvas — the same component as `/aluminium-windows/`, and no new JavaScript — carrying one photograph, with guttering, fascia and soffit lighting in turn out of a dark scrim. Alongside that: privacy glass and frame colours gone, the Liniar logo and badge gone, no supplier named, the gallery pool cut from eight images to the two that are genuinely roofline, hedging and fear-framing out, the H1 no longer repeated as an H2, and the guarantee stated positively as the manufacturer's 25 years white and 10 foiled with no FENSA or insurance-backed claim.
- **THREE THINGS ARE UNFINISHED AND A DEPLOY SHOULD PROBABLY WAIT FOR THEM.** The colour rail is not built, though 23 roofline colour photographs are committed ready for it. One of the two roofline photographs is not surfacing in the gallery and nobody knows why. And **nobody has scrolled the page in a real browser** — headless throttles the animation frame, so every state is verified and the feel of the scrub is not.
- **A FULL PAGE AUDIT WAS LAUNCHED AND NEVER RETURNED.** Five lenses with adversarial verification, stopped before completion. **There are no findings from it; do not assume any.** Resume with `Workflow({scriptPath, resumeFromRunId: "wf_430f26a9-029"})`.
- **ELEVEN PASSES WERE SPENT PLACING THREE HIGHLIGHT ZONES BY EYE AND ALL ELEVEN MISSED.** What worked was detecting the boundaries from the photograph itself with a Hough-style sweep for the strongest luminance edges. Three reusable parts, and the full version is the Roofline Highlight Geometry Rule in `AI.md`: **lines receding from a camera are not parallel**, so forcing one slope across them cannot fit whatever the number; **a phone photograph of a screen cannot be used as a measurement**, because the screen is keystoned and a line drawn straight on the glass is not straight in page coordinates; and **when nudging is not converging, look for a sign or index error** rather than adjusting the magnitude — these zones were mirrored once and one band too high another time, and both looked like small miscalibrations.
- **TWO REGRESSIONS WERE INTRODUCED AND THE OWNER CAUGHT BOTH.** The gallery pool was cut to two images and then one was pulled as the hero, leaving a single picture repeating; and the roofline colour card was deleted, which left the specification-choices band rendering a heading over nothing. Both were removals that were right in themselves and wrong in what they left behind. **Check what a removal leaves, not just what it takes away.**
- Superseded: - **LIVE IS `34e902d6`, LEVEL WITH `main`, AND TEST IS LEVEL WITH IT, 2026-08-15 (11:38). NOTHING IS OUTSTANDING IN EITHER DIRECTION.** Nine commits over `753d07af`, all structured data and FAQ copy for the residential side. Live was re-established by checksum on **four** files including a compiled asset, which returned `df10e0a2` and `753d07af`; their theme trees are byte-identical (`e2ef50b4…`) because the newer one is docs-only, so both are the same answer and the third recorded instance of that pattern. Guard passed on **twenty-one** files in the seconds before the rsync — twelve changing and nine expected-unchanged, the whole tracking strand plus **both** compiled assets — and all twenty-one re-hashed to their target values after it. Backup `fenster-pre-34e902d6-20260815-113837.tar.gz` (424M, 2,031 entries) **proven** by extracting `inc/site-data.php` and hashing it to the pre-deploy value. Pruned to three; disk at 84%. Deletion list asserted **EMPTY** and the dry run agreed at zero. **No SCSS or JS changed, so neither compiled asset moved and there was no build hazard at all.** Socket purge `msg:OK`.
- **THE BIGGEST STRUCTURED-DATA CHANGE THIS SITE HAS HAD, BY PAGE COUNT.** Until now the three hubs, the head-term page and every one of the ~270 town matrix routes rendered visible FAQs with no markup behind them, while seven separate copies of the same `FAQPage` block existed across the theme. There is **one emitter** now, `fenster_faq_page_schema()` in `inc/generated-pages.php`, with eight call sites, taking both the `q`/`a` and `question`/`answer` shapes. The refactor was **proven to change nothing**: FAQ schema on all seven pre-existing routes is byte-identical to a live baseline captured before the deploy.
- Also live: residential `Service` schema on product routes, keyed off `product_usps` and carrying the key-specification figures as `additionalProperty`; a `WebSite` node so the homepage stops carrying only the business node; `Person` schema for the sixteen named team members; and `Article` on case studies. **Verified on the apex:** 16 Person nodes, `datePublished` 2026-07-21 on the Winslow study, and `U-value: 0.95 W/m²K (lowest achievable)` on `/casement-windows/`.
- **THE "LOWEST ACHIEVABLE" QUALIFIER HAS TO BE DERIVED THE WAY THE PAGE DERIVES IT, and the first pass got it wrong in a way worth keeping.** It tested for a trailing `*` on the `product_usps` label, which never fires: the star is not stored there. `product-pulse.php` earns it at render from `glazing_u_values`, and only where BOTH a double and a triple figure exist, because it means "the lower of two". Without that test, a page whose own strip prints `0.95 W/m²K *` over `* Lowest achievable` was publishing a flat `U-value: 0.95` to every parser — the best case restated as the standard one. It now tracks the page, including `/heritage-windows/` correctly printing 1.4 plain since triple came off Classic.
- **THE MATRIX FAQ SCHEMA WENT ON THE WRONG ONE OF TWO RENDER BLOCKS AND SHIPPED TO TEST COVERING ONE PAGE OUT OF ~270.** `location-service.php` renders FAQs twice, once inside the head-term branch and once for the whole town matrix. The diff looked correct; fetching a matrix route is what found it. Same lesson `AI.md` already records against the case-study helpers: **when a rule is enforced at a render site, grep for the other render site that answers the same question.**
- **The matrix FAQ copy is rewritten for the question a homeowner actually asks.** It ran "Can you quote for X in Y?", "Will the X be surveyed before manufacture?" and two more objection-handlers, three of which opened with the word "Yes". It now asks what it costs, whether we cover the town, **when somebody actually measures** — which corrects the common assumption that the consultation is the survey — and whether the colours can be seen first. Every answer is a published or owner-confirmed fact and no figure is quoted, because none is confirmed for a template that renders twenty-one ways. The head-term page takes a FENSA and guarantee answer that would be **wrong** on the shared set, because the matrix includes integral blinds and those sit outside both.
- **A COMPETITOR COMPARISON WAS WRITTEN, VERIFIED, SHIPPED TO TEST AND PULLED THE SAME DAY, AND THE REASONING IS THE REUSABLE PART.** `/casement-windows/` briefly carried a named comparison against the aluplast Ideal 70: six chambers against five, 0.95 W/m²K against their published 1.2, sourced from their 2025 technical brochure, with the balancing admission that their range is rated A++ where ours is A+ because A++ needs a 40mm unit we do not fit. **Owner instruction: drop competitor copy where it does not flatter us.** It was dropped *whole* rather than by halves, because a comparison that shows only our side is selective rather than persuasive and is the version a competitor can challenge. The FAQ is now "Why do you fit Liniar?" answered on our own figures. **If a comparison ever returns it comes back with the admission attached or not at all**; the full ruling and its conditions are in `AI.md` under the Supplier Naming Rule. It also removed a second fault: the versus table on that page already prints 1.2 W/m²K for the Liniar flush sash, so the page was carrying two unrelated 1.2s.
- **A FAQ CAP WAS RAISED AND PUT BACK THE SAME DAY.** Composite doors went 6 to 7 for a "why Distinction" answer, then back to 6 when the answer turned out to restate the 44.5mm slab from one existing FAQ and the Salford figure from another. **Raising the cap to fit a genuine new answer is the documented move and was not the problem; the answer was.** The question that route needed was a retitle, not an addition: "What is a composite door made from?" is now "Why do you fit Distinction composite doors?", same approved answer plus one sentence that says outright it is a judgment rather than a specification.
- Superseded: **LIVE WAS `753d07af`, LEVEL WITH `main`, AND TEST WAS LEVEL WITH IT, 2026-08-15 (10:22).** Eight commits over `1ccc8bd8`: the **three commercial specification commits that had been sitting on test since 2026-08-13**, two docs commits, and three from this session's AI-discoverability work. Live was **re-established by checksum first**, and that step earned its keep — see the next bullet. Guard passed on **fourteen** files in the seconds before the rsync, six changing and eight expected-unchanged (the whole tracking strand plus `assets/js/main.js`), **plus an assertion that `template-parts/components/faq-block.php` did not already exist**; fifteen re-hashed to their target values after it, the new component included. Backup `fenster-pre-753d07af-20260815-102203.tar.gz` (424M, 2,030 entries) **proven** by extracting `inc/generated-pages.php` from the tarball and hashing it to the pre-deploy value. Pruned to three; disk at 84%. Deletion list asserted **EMPTY** and the rsync dry run agreed at zero. `main.css` rebuilt byte-identical from source before shipping and the **served** stylesheet re-hashed to `e334d201…` at 705,942 bytes, so the proxy is serving the new one; **`assets/js/main.js` provably unchanged across the whole range**, so there was no compiled-JS hazard at all. Socket purge `msg:OK`.
- **FOUR COMMITS MATCHED THE FIRST THREE CHECKSUMMED FILES, AND ONLY TWO MORE FILES SEPARATED THEM.** `assets/css/main.css`, `inc/generated-pages.php` and `inc/commercial-product-data.php` were identical across `32b4b435`, `96dc6125`, `bd307d3c` and `1ccc8bd8`, because none of the three changed over that stretch. Adding `inc/site-data.php` and `template-parts/sections/generated-page.php`, which the candidates genuinely differ in, returned `1ccc8bd8` uniquely. **This file has said since July to pick files the candidate commits actually differ in; this is the release where that sentence was load-bearing rather than decorative.** Hashing three files and stopping would have left four answers and no way to choose.
- **THE PENDING SPECIFICATION ROWS ARE FINALLY OFF LIVE.** `/curtain-walling/` had been publishing `Confirming — ask and we will send it` in three of its seven rows since 2026-08-12, because the owner's 2026-08-13 ruling that a pending row renders nowhere shipped only to test. Verified on the apex after this release: `Confirming` returns **zero**, `As low as 0.9` is present, and `Maximum panel` and `Structural or capped` are both gone with `Glazing method` in their place. Worth noting for its own sake: a placeholder that sits on live for three days is read by crawlers and answer engines as the published figure.
- **Content of this release: the commercial pages made legible to machines.** The per-route `Service` schema stopped re-declaring its own inline business node and now references `#business` by `@id`, so thirteen routes stop describing an address-less, credential-less company that merely shares our name. The business block gained the registered name, company number and VAT number; `https://www.fensa.org.uk/` came **out** of `sameAs`, which is an identity property and not a place for an association, and FENSA moved to `hasCredential` beside Constructionline Gold and SSIP — **neither of which appeared in any structured data at all**, despite each having its own page. Fifty-nine FAQs and the first `FAQPage` markup the commercial set has ever carried. `/llms.txt` is served as a virtual route, generated from `primary_nav_fallback` and `fenster_commercial_product_pages()` so it cannot drift from the menu.
- **THE LIVE `robots.txt` IS NOT THEME-GENERATED AND NOTHING IN A DEPLOY CAN CHANGE IT.** It is a static file in the Bedrock document root advertising `sitemap_index.xml`, while `fenster_generated_robots_txt()` builds `/sitemap.xml` and strips `Sitemap:` lines. Both sitemap URLs return `200`, so nothing is broken — but **a robots.txt change made in the theme will deploy cleanly and do nothing**, which is the same shape as the `wp sg purge` trap. That is why `/llms.txt` is a route rather than a file. Fixing it needs file access to the web root, not a commit.
- **A PROBE FAULT IN THE VERIFICATION, AGAIN, AND IT LOOKED LIKE A BROKEN DEPLOY.** The served-stylesheet check returned `curl: (3) URL rejected: Malformed input to a URL function`, because a `grep -oE` for the CSS href stopped inside the `?ver=1786789087-705942` query string and handed `curl` a truncated URL. Re-extracting with the query intact returned a byte-identical match. Same family as the `d41d8cd9…` empty-string hash this file already names three times: **a verification failure is not evidence until the probe is checked.**
- Superseded: **LIVE WAS `1ccc8bd8` AND IT IS BACK ON `main`, 2026-08-13 (20:34). THE DIVERGENCE IS CLOSED IN BOTH DIRECTIONS.** This is the biggest single release this month: **thirty-four commits** over `2ff7900b`, and only four of them are the audit session's. The other thirty are **the entire commercial rebuild** (~20, on test since 2026-08-12 and owner-approved on the 13th), the other session's tracking revert and van QR link, and docs. Live was **re-established by checksum first, not read off this file**: four deployed files hashed and matched across all eighty recent commits, which returned `2ff7900b` and its parent `f5b12f0b` as the only candidates. **The other session's tracking strand was proven to survive before anything moved** — `inc/consent.php`, `inc/traffic-classification.php`, `inc/ad-attribution.php`, `inc/scan-links.php` and `functions.php` are byte-identical between live and `main`, so shipping `main` could not revert granted-by-default consent, the bot filtering or the ad click log. `inc/consent.php` was additionally guarded as expected-unchanged and re-hashed identical afterwards. Guard passed on four files in the seconds before the rsync and three re-hashed to their target values after it. Backup `fenster-pre-audit-20260813-203124.tar.gz` (422M, 2,023 entries) **proven** by extracting `inc/site-data.php` from the tarball and hashing it to the pre-deploy value. Pruned to three; disk at 84%. Socket purge `msg:OK`. Both compiled assets were rebuilt from source first and reproduced **byte-identical** to the committed artefacts, which is the evidence the artefacts are what this source produces.
- **THE DELETION ASSERTION OF EXACTLY FOUR, BY NAME, FINALLY FIRED — AND IT PASSED.** This file and `PROGRESS.md` have both been warning about it since 2026-08-12. The rsync dry run listed `BFI.jpg`, `Greensand-Country.jpg`, `Airbus-Commercial.jpg` and `ROKA-Dental-Post-Fitting-2-1-scaled.jpg` and nothing else; the deploy script asserted the count at four **and** the four paths by name, and would have exited otherwise. Forty transfers. Remember `git diff --name-status` prints the two renames as `R100` while rsync sees four deletions: the assertion was set from what rsync does, which is why it matched. Verified afterwards on live: `BFI.jpg` returns `404` and both renamed files return `200`.
- **AN AUDIT FINDING BLAMED A DEPLOY FAILURE FOR SOMETHING THAT WAS AN OWNER DECISION, AND IT WAS WRONG.** The 2026-08-13 forensic audit measured 22 third-party requests firing on a clean session with no consent stored, and reported the cause as the consent-first fix never having been deployed. It had not been deployed because **the owner reverted it**: `31da4d09`, "cookies on by default, but bots excluded and ad clicks logged", owner instruction 2026-08-12, which this file already recorded. The measurement was right and the diagnosis was wrong, because the session assumed the repository was newer than production instead of reading the log. **When live and the repo disagree, read the commits before concluding the deploy failed** — on this project a deliberate revert is at least as likely.
- Content of the audit half of this release: the uPVC-versus-aluminium insulation error and a "metal is a natural insulator" claim off a live guide; dollar prices off `/guide-noise-insulation-for-windows/`; `/patio-doors/` reading 14 and 16 colours on one page; the withdrawn 1.8 W/m²K secondary glazing figure off `/other-services/`; a PAS 24 claim for a component off the APECS page; the scrape-era `/roofline/` H1 and its ~56 link labels; porches off the homepage; secondary glazing corrected to **five** styles on the owner's 2026-08-13 ruling that a lift-out and a fixed panel are different products; the case-study fallback keeping its studies but losing the claim that they are that product; the message box made optional and the honeypot finally read; the mobile nav trapping focus; a malformed global `h2` clamp that had been shipping a flat 48px; and the third-person voice sweep. **Much of the `data/pages.json` half of that sweep turned out to be on records that never render** — see the note below before repeating it.
- **A LARGE PART OF THE VOICE SWEEP WAS SPENT ON DEAD DATA, AND THE CHECK THAT SHOULD HAVE CAUGHT IT WAS TRUSTED INSTEAD OF RUN.** Four FAQ boilerplate blocks in `data/pages.json`, duplicated across ~122 imported pages, were reported as rendering. They do not: fourteen routes were fetched afterwards, including the exact slugs holding the records, and both the old and the converted wording return **zero**. Product routes override those FAQs with `product_content`. The rendering changes are the PHP ones. **Before editing `data/pages.json`, fetch a page that is supposed to show the string and prove it renders** — surviving `$is_bad_scrape_section` is not the same as reaching a customer.
- Verified as a visitor on the apex host with no cache-buster: fourteen routes `200` with zero PHP notices including the three new commercial routes, `/sitemap.xml` `200`, the `/roofline/` H1 reading "Roofline" under a "Fascias, soffits and guttering" eyebrow, patio reading 16 options, secondary glazing reading "Which of the five" with zero "four styles", and the insulation error, dollar figures, withdrawn U-value and porches all returning zero.
- ~~**THREE COMMITS ARE ON TEST AND NOT LIVE**~~ **SHIPPED 2026-08-15 inside `753d07af`.** All commercial specification work: the curtain walling figures (U-value published as a hedged floor, capped named positively, maximum panel removed), pending rows no longer rendering at all on the owner's ruling, and the shared spec note trimmed so it stops pointing at rows nobody can see. Test is `068ccefe`.
- Superseded: **LIVE WAS `2ff7900b` AND NOT ON `main`, 2026-08-13 (14:43).** It is `f5b12f0b` — the live SHA it was branched from — plus the van QR scan link and nothing else. `main` is `77d9b158` and carries the same change on top of the twenty-seven commits below, **which are still deliberately unshipped**, so `main` was not deployed and live is a branch rather than a tip. **Do not treat `main` as live and do not fast-forward live to it.** Live was re-established by checksum first: seven files hashed against `f5b12f0b` and all seven matched (`assets/css/main.css`, `assets/js/main.js`, `inc/adminbase.php`, `template-parts/sections/generated-page.php`, `inc/website-tracking.php`, `inc/site-data.php`, `functions.php`). The rsync dry run listed exactly one content change (`functions.php`) and one addition (`inc/scan-links.php`), every other line timestamp-only; deletion list asserted **EMPTY**. Six expected-unchanged files re-hashed identical afterwards and both changed files matched the candidate tree. Backup `fenster-pre-2ff7900b-20260813-134306.tar.gz` (441M, 2,022 entries) **proven** by extracting `functions.php` and hashing it to the pre-deploy value. Socket purge `msg:OK`. No SCSS or JS changed, so both compiled assets were guarded as unchanged rather than rebuilt. Content: `/van/` 302s to the homepage carrying `utm_source=van`.
- **THE VAN REDIRECT IS THE FIRST THING HERE PROVEN UNCACHEABLE ON LIVE.** Three consecutive plain requests to `/van` each returned `X-Proxy-Cache: MISS`, so `nocache_headers()` on a redirect genuinely defeats the path-keyed proxy. Test could not have shown this — Basic Auth disables the proxy there and every response is a miss regardless. The scan is still counted on the destination page rather than on the short path, because that is where the tracker already runs and it does not depend on this holding.
- **NOTHING ELSE SHIPPED TO LIVE ON 2026-08-12 OR 13. TEST IS TWENTY-TWO COMMITS AHEAD OF IT AND THAT IS DELIBERATE.** `main` and test are both `6d7d956b`; fourteen of the twenty-two are the commercial rebuild and the other eight were already unshipped before it started. Working tree clean, no release branch outstanding. **Live was NOT re-established by checksum during that session**, so treat every SHA below as a record rather than as a verified fact and hash it before any deploy, which is what this file has said since July.
- **THIS FILE SAYS LIVE IS `e3ea19e9` AND `PROGRESS.md` SAYS `47db7aea`. BOTH ARE RIGHT AND NEITHER IS STALE.** `47db7aea` is the docs-only commit immediately after `e3ea19e9` — it changes `HANDOVER.md`, `LIVECHANGES.md` and `PROGRESS.md` and nothing else. **Their theme trees are byte-identical** (`d33c3813…`, verified 2026-08-13 with `git rev-parse <sha>:app/public/wp-content/themes/fenster`), so no checksum on a deployed file can separate them and both answers are correct. This is the second recorded instance of the pattern; the first was `0f8e8bf5` versus `d4332495` and it is described further down. **Do not report the pointer as drifted on the strength of the two docs disagreeing** — compare the theme trees first, it is one command.
- **THE LIVE DEPLOY OF THE CURRENT STACK NEEDS A DELETION ASSERTION OF EXACTLY FOUR, BY NAME.** Two deletions and two renames: `BFI.jpg` and `Greensand-Country.jpg` go, `Airbus-Commercial.jpg` becomes `commercial-glazed-elevation.jpg` and `ROKA-Dental-Post-Fitting-2-1-scaled.jpg` becomes `dental-practice-glazing.jpg`. `git diff --name-status` prints the renames as `R100` and **rsync sees four deletions**; set the assertion from what rsync will do. Every other release this month asserted zero, so a copied-forward guard will abort. The theme diff over live is 8 added, 14 modified, 2 deleted, 2 renamed. Full range and reasoning in the 2026-08-13 START HERE block in `PROGRESS.md`.
- Superseded: **Live is `e3ea19e9` and level with `main`, and TEST IS LEVEL WITH IT, 2026-08-12 (12:40).** Guard passed on **fourteen** files in the seconds before the rsync — five changing (`data/pages.json`, `inc/generated-pages.php`, `inc/site-data.php`, `template-parts/sections/generated-page.php`, `template-parts/sections/location-service.php`) and nine expected-unchanged, including **both compiled assets**, the tilt and turn template shipped that morning and the whole tracking strand. All fourteen re-hashed after it. Backup `fenster-pre-e3ea19e9-20260812-124020.tar.gz` (422M, 2,022 entries) **proven** by extracting `data/pages.json` and hashing it to the pre-deploy value. Deletion list asserted **EMPTY**. Pruned to three (1.3G). Socket purge `msg:OK`. No SCSS or JS changed, so both compiled assets were guarded rather than rebuilt. Content: product names aligned with the showroom and WindowCAD.
- **SEVEN PRODUCTS RENAMED AND NO URL MOVED, ASSERTED RATHER THAN INTENDED.** Every `home_url`, slug and canonical in the diff was extracted and compared across both sides; none appeared on only one. That check is cheap and it is the one thing that makes a naming change safe, because a rename and a redirect look identical in a commit message.
- **A PRODUCT NAME LIVES IN SEVEN REGISTRIES AND NONE OF THEM CHECKS ANOTHER.** All seven had to change and **the last two were found by rendering pages, not by grepping data.** The sixth, `$services` in `location-service.php`, is a different array from `fenster_location_matrix_products()` and names the same ~260 town routes, so the first pass left them with a renamed SERP title over an un-renamed H1. The seventh, `$route_title` in `generated-page.php`, was labelling related links from scrape-era `pages.json` records, so `/patio-doors/` rendered "Patio Doors Northampton" links under its own "uPVC Sliding Doors" H1. Both derive from the matrix now instead of restating it. Full list in the Product Naming Rule in `AI.md`.
- **THE TITLE TAGS DELIBERATELY DID NOT FOLLOW, AND THAT IS THE SEO ANSWER.** The head phrase in each is the query the route ranks for; the showroom names are not queries, because nobody searches "aluminium heritage" or "uPVC flush sash". Two changed, both because the new name **prefixes** rather than displaces: `uPVC Casement Windows Milton Keynes` still contains `Casement Windows Milton Keynes` intact and gains a 4,955-impression term, and patio keeps its head phrase with the new name in the tail. Do not "finish the job" by pushing the rest through.
- **A STALE-NAME SWEEP REPORTED 14, THEN 37, THEN 3, AND EVERY ONE WAS THE PROBE.** "Aluminium Heritage Windows" contains "Heritage Windows"; "French Casement Windows" contains "Casement Windows" and is a product that correctly keeps its name; and the three survivors were the WindowCAD collection label in the quote heading, which is the thing being aligned **to** and does not get rewritten. Fourth session running for **a verification failure is not evidence until the probe is checked.**
- Verified as a visitor on the apex with no cache-buster: twelve H1s correct across the main routes and the town matrix, zero PHP notices, the menu carrying the short form against unchanged URLs, both changed title tags correct and the kept ones untouched, and thirteen canonical routes `200` including `/sitemap.xml`.
- Superseded: live was `831bfa44` and level with `main`, and TEST WAS LEVEL WITH IT, 2026-08-12 (08:53).** Guard passed on **eleven** files in the seconds before the rsync — three changing (`inc/site-data.php`, `inc/product-hub-data.php`, `template-parts/sections/tilt-turn-windows-v2.php`) and eight expected-unchanged, including **both compiled assets**, `generated-page.php` and the whole tracking strand — and all eleven re-hashed after it. Backup `fenster-pre-831bfa44-20260812-085322.tar.gz` (422M, 2,022 entries) **proven** by extracting the v2 template and hashing it to the pre-deploy value. Deletion list asserted **EMPTY**. Pruned to three. Socket purge `msg:OK`. No SCSS or JS changed, so both compiled assets were guarded as unchanged rather than rebuilt. Content: a caption correction on `/tilt-turn-windows/`.
- **BOTH LOCKING RENDERS ARE KEEPS AND ONE SHIPPED CAPTIONED AS A CAM.** Owner correction on the live page: *"'the cam' pic is just another keep on the side of the sash, not a cam."* Liniar named **both** source files `keep` and the identification was overridden from the render. It is a screw-fixed steel keep with a lead-in ramp; a cam is machined onto the gearing and is not screwed on. Same class of error as naming keeps among the things that fail on the repairs page: **do not identify a part from a render.** The word "cam" is now off that route entirely — copy, both alt texts, the gallery pool and the parked product-hub block — because nobody has confirmed what engages these keeps.
- **The correction improved the section rather than patching it.** Two keeps in two positions prove something the page can stand behind — that it locks at intervals all the way round rather than at the handle — where before it asserted a mechanism no picture on the page showed. Frame first, so the second caption reads "another".
- Verified as a visitor on the apex with no cache-buster: `200`, zero PHP notices, `cam` and `mushroom` absent from the visible text, both new captions and both new alt texts present, and the sculptured, 0.93/1.3 and gated case-study state all still correct.
- Superseded: live was `d7118d7f` and level with `main`, and TEST WAS LEVEL WITH IT, 2026-08-12 (08:23).** Guard passed on **ten** files in the seconds before the rsync — four changing (`assets/css/main.css`, `inc/site-data.php`, `inc/product-hub-data.php`, `template-parts/sections/generated-page.php`) and six expected-unchanged (`assets/js/main.js`, `inc/consent.php`, `inc/website-tracking.php`, `inc/enquiries.php`, `inc/ad-attribution.php`, `functions.php`) — plus an assertion that the new v2 template did **not** already exist. All eleven re-hashed after it, and both new images confirmed present. Backup `fenster-pre-d7118d7f-20260812-082303.tar.gz` (422M, 2,019 entries) **proven** by extracting `inc/site-data.php` from the tarball and hashing it to the pre-deploy value. Deletion list asserted **EMPTY**. Pruned to three (1.3G). Socket purge `msg:OK`. `main.css` rebuilt byte-identical from source before shipping (`bbff7fe4…`) and the **served** stylesheet re-hashed to the same value at 694,690 bytes, so the proxy is serving the new one. **`assets/js/main.js` provably untouched across the whole range**, so there was no compiled-JS hazard at all. Content: `/tilt-turn-windows/` rebuilt as the eleventh bespoke residential middle.
- **THE RANGE WAS EXACTLY THIS SESSION'S SEVEN COMMITS AND THAT WAS PROVED, NOT ASSUMED.** `git diff --name-status` over the theme listed eight files: five modified, three added, **zero deleted**, and none of the other session's tracking strand among them. That check is what let the deletion assertion be set to EMPTY rather than guessed.
- **LIVE HASHED TO `d4332495`, NOT THE `0f8e8bf5` THIS FILE RECORDED, AND BOTH ANSWERS ARE RIGHT.** `d4332495` is the docs-only commit immediately after `0f8e8bf5`, and docs live outside the theme, so **the two have byte-identical theme trees and no checksum can separate them.** The sweep across `git log --all` returns the newer one because it walks newest-first. Worth knowing before somebody reports the pointer as stale: on this repo a docs commit silently becomes an equally valid answer to "what is live", and the range check is unaffected either way.
- **A `d41d8cd9…` APPEARED AGAIN AND IT WAS THE PROBE, AGAIN.** The served-stylesheet check returned the md5 of the empty string because the `grep -oE` for the CSS href matched nothing and `curl` was handed a blank argument. Third recorded instance on this project. **Recognise that hash on sight**; re-extracting the URL properly returned the correct match. Two other "failures" in the same sweep were also the probe: `0.93` appears twice because the strip and the banner both carry it by design, and `1.3` three times because the FAQ answer is also copied into the FAQPage JSON-LD.
- Verified as a visitor on the apex with no cache-buster: `/tilt-turn-windows/` `200` with zero PHP notices, the bespoke middle rendering with all four sections in order, the case-study strip **absent**, `sculptured` present and `chamfered` absent, `four-chamber` present and `six-chamber` absent, `0.93`/`1.3` on both the strip and the banner with no `0.95` or `1.2` anywhere, `37 dB` present and `Highly versatile` gone, Excalibur and "fire escape" both zero, both new images `200` at their exact byte sizes, the new 4:3 card rendering on `/windows-milton-keynes/`, and fifteen canonical routes `200` including `/sitemap.xml`.
- Superseded: live was `0f8e8bf5` and level with `main`, and TEST WAS LEVEL WITH IT, 2026-08-12 (06:43).** Guard passed on `inc/site-data.php`, `template-parts/sections/generated-page.php` and `assets/css/main.css` as the changing files, and on `assets/js/main.js`, `inc/consent.php` and `inc/website-tracking.php` as expected-unchanged, in the seconds before the rsync and again after it. Backup `fenster-pre-0f8e8bf5-20260812-064329.tar.gz` (417M, 1,985 entries) **proven** by extracting `inc/site-data.php` and hashing it to the pre-deploy value. Deletion list asserted at **exactly one and by name**. Pruned to three. Socket purge `msg:OK`. `main.css` rebuilt byte-identical from source before shipping. Content: `/upvc-doors/` rebuilt as the tenth bespoke residential middle.
- **THIS SESSION'S TWO RELEASE BRANCHES ARE DELETED**, after proving nothing on either was absent from `main`: `release/all-hallows` held only a revert of another session's case study, and the terminology commits are on `main` under different hashes. Fifteen stale `release/*` branches remain and every one of them would revert real work if deployed.
- **LIVE WAS NOT ON `main` AND ONLY A CHECKSUM SWEEP FOUND IT.** The pointer said one thing and live hashed to `2a8583cf`, another session's tracking release, which is not an ancestor of `main`. The tell was the compiled JS: `inc/site-data.php` still matched this session's terminology release while `assets/js/main.js` did not match anything on `main`. **Hash more than one file, and make one of them a compiled asset**, because a docs-only or copy-only release leaves the PHP looking familiar while the bundle has moved.
- **THE RANGE TURNED OUT TO BE ONLY THIS SESSION'S WORK, AND THAT WAS PROVED RATHER THAN ASSUMED.** All seven files of the other session's tracking strand — `inc/consent.php`, `inc/website-tracking.php`, `inc/ad-attribution.php`, `inc/traffic-classification.php`, `inc/adminbase.php`, `inc/enquiries.php` and `functions.php` — are byte-identical between live and `main`, so their work was already in production and `main` could ship whole. **That check is what turns a scary range into a one-line deploy**: diff the theme by file first, and only cherry-pick when the diff says you have to.
- **A REBUILD ALONE MOVED THE COMPILED JS AND IT WAS RESTORED, TWICE.** `src/js/main.js` is byte-identical between live and `main` — the door randomiser went in and came out again, so the source netted back to where it started — but the esbuild on this machine is newer than the one that built live's bundle, so `assets/js/main.js` differed. Shipping it would have replaced another session's minified tracking bundle with a rebuild of the same source for nothing. It was checked back out before the deploy, and again after `npm run build` was run to prove the stylesheet. **`git status` immediately before the rsync is the check that catches it.**
- **THE STYLESHEET ARTEFACT WAS PROVEN, NOT TRUSTED.** `npm run build` reproduced `main.css` byte-identical to the committed copy, which is the evidence the artefact is what this source produces.
- **ONE DELETION, ASSERTED BY NAME.** `upvc-door-rosewood-woodgrain.webp`, a Chartwell green stable door that had been mis-filed under a rosewood name since launch. The deploy script asserted the count at exactly one AND grepped the queued line for the filename before touching production; anything else and it exits. Verified `404` on live afterwards.
- Verified as a visitor on the apex with no cache-buster: `/upvc-doors/` `200` with zero PHP notices, seventeen sections in the intended order, all eleven uPVC door images `200`, and `/`, `/casement-windows/`, `/composite-doors/`, `/french-doors/`, `/patio-doors/`, `/flush-casement-windows/`, `/upvc-doors-bletchley/` and `/commercial-projects/all-hallows-bedford/` all `200`.
- **Live is `f5b12f0b` on `release/tracking-bots-and-ads`, 2026-08-12. Live is deliberately NOT the tip of `main`**, which carries the commercial rebuild. Cut from live `51668eb9` with one commit cherry-picked, compiled assets rebuilt byte-identical from the branch's own source, guard passed on `inc/consent.php` and `assets/js/main.js` with `inc/site-data.php` asserted **unchanged** before and after, backup proven by extraction, deletion list asserted **empty**, socket purge `msg:OK`. Verified on live: a normal visitor reads `human`, Googlebot reads `bot` with every endpoint blanked, the banner says cookies are already on, and the ad click route returns a reference. **Two release branches are now outstanding on origin** (`release/revert-tracking` and this one); delete both once the commercial strand ships.

- Superseded: live was `9c7b30c` on `release/light-terminology`, 2026-08-12 (15:46). It is DELIBERATELY NOT the tip of `main`, and TEST IS DELIBERATELY AHEAD OF IT.** Guard passed on three changing files and on **four** expected-unchanged files, `assets/css/main.css`, `assets/js/main.js`, `inc/consent.php` and `inc/enquiries.php`, in the seconds before the rsync, and all seven re-hashed after it. Backup `fenster-pre-9c7b30c-20260811-154558.tar.gz` (417M, 1,983 entries) **proven** by extracting `inc/site-data.php` from the tarball and hashing it to the pre-deploy value. Deletion list asserted **empty**. Pruned to three. Socket purge `msg:OK`. **Both compiled assets provably unchanged across the range.** Content: the word "light" comes off every window and opener on the site.
- **THE TWO UNCHANGED-FILE GUARDS WERE THE POINT OF THIS ONE.** `main` carries another session's tracking rework, which touches `inc/consent.php`, `inc/enquiries.php`, `inc/website-tracking.php`, `functions.php`, two new includes and the compiled JS, and none of it is approved. The release was cut from live and only the two copy commits cherry-picked, then proved: six theme files in the range, all copy, and each of the seven tracking-strand files asserted untouched **by name** rather than by reading the file list.
- **A SWEEP IS VERIFIED BY RENDERING, NOT BY GREPPING THE SOURCE.** All **712 routes in the sitemap** were fetched from live and matched with the tags stripped first: zero banned usages, zero errors. Source greps had already returned zero twice, and they cannot see what a template composes at runtime. Note the tag-stripping cuts both ways — it also hides alt text, which is why the visible-text count of `leaded light` is 2 and the source count is 5.
- **`roof light` SURVIVED THE SWEEP AND HAD TO.** 2,905 occurrences across live, because every page links the roof lantern routes, and those pages carry keyword coverage the SEO work added deliberately. **A search-and-replace on "light" anywhere near this rule would strip all of it.**
- **`leaded light` IS PERMITTED, and that reversal is owner ruling not drift.** It names a leaded glass panel rather than an opening, which is the test: a bathroom "light" named the opening and went, the leaded light names the glass and stays. First pass removed it and it went back before shipping.
- **`release/light-terminology` and `release/all-hallows` are both loaded guns on origin now.** Deploying either would revert the tracking strand or the Little Horwood study respectively. Delete both once tracking ships.
- Superseded: live was `f3829a35`, 2026-08-11 (15:27). It is DELIBERATELY NOT the tip of `main`, and TEST IS DELIBERATELY AHEAD OF IT.** Guard passed on `inc/case-studies-data.php` as the changing file and on **seven** expected-unchanged files, `assets/js/main.js`, `assets/css/main.css`, `inc/site-data.php`, `functions.php`, `inc/consent.php`, `template-parts/sections/generated-page.php` and `inc/enquiries.php`, three times: at preflight, in the seconds before the rsync, and again after it. The last five are the tracking session's territory and were guarded precisely so this `--delete` would abort rather than revert their strand if it had landed mid-deploy. Backup `fenster-pre-f3829a35-20260811-152702.tar.gz` (409M, 1,976 entries) **proven** by extracting `inc/case-studies-data.php` from the tarball and hashing it to the pre-deploy value. Deletion list asserted **empty**. Pruned to three (1.2G). Socket purge `msg:OK`. **Both compiled assets provably unchanged across the range**, so there was no build hazard at all. The theme diff over live was **eight files and purely additive**: seven new assets and 110 lines into one data file, with zero deletions and nothing modified outside it. Content: the Little Horwood residential case study. Verified as a visitor on the apex with no cache-buster: `200`, `x-proxy-cache: MISS`, nine copy assertions present and five wrong-spec strings absent, zero PHP notices, all six referenced assets `200` at their exact byte sizes plus the 16:10 card on the archive, the study on `/case-studies/`, `/flush-casement-windows/` and `/composite-doors/` and correctly absent from `/commercial-projects/`, one `page-sitemap.xml` entry, and the other session's All Hallows archive and detail, Heal's and `/louvre-vents/` all still `200`.
- **LIVE WAS NOT AT ANY COMMIT ON `main`, AND ONLY A SWEEP OF EVERY REF FOUND IT.** The usual "hash live and walk back down `origin/main`" check returned no match at all, because live was sitting on a `Revert "…"` commit on `release/all-hallows` that exists on no other branch. Hashing against `git log --all` found it in one pass. **When the live checksum matches nothing on `main`, widen to every ref before concluding live has drifted or been hand-edited** — on this repo the likeliest explanation is another session's release branch, and the revert on it is a deliberate act rather than damage.
- **`release/all-hallows` IS NOW STRICTLY A HAZARD AND NOTHING ELSE.** Its content is already live inside `f3829a35`, and deploying it would revert the Little Horwood study. Delete it once the tracking strand ships. Left in place only because it is another session's branch.
- **VERIFY LIVE ON THE APEX HOST.** `https://www.fensterglazing.com` returns `301` on every route, so a verification sweep against `www` reports the entire site missing and every assertion failing. It looks exactly like a catastrophic deploy and is only the canonical redirect. Use `https://fensterglazing.com`, or follow redirects.
- Superseded: live was `32e05f2` on `release/all-hallows`, 2026-08-12 (15:19).** Guard passed on `inc/case-studies-data.php` as the changing file and on `assets/css/main.css`, `assets/js/main.js` and `inc/consent.php` as expected-unchanged, in the seconds before the rsync, and all four re-hashed after it. Backup `fenster-pre-32e05f2-20260811-151909.tar.gz` (408M, 1,969 entries) **proven** by extracting `inc/case-studies-data.php` from the tarball and hashing it to the pre-deploy value. Deletion list asserted **empty**. Pruned to three. Socket purge `msg:OK`. **Both compiled assets provably unchanged across the whole range**, so there was no build hazard at all. Content: the All Hallows, Bedford commercial case study, and two fixes to `fenster_case_studies_for_town()`.
- **Update, 2026-08-11 (15:27): the Little Horwood half of the note below is spent.** The owner approved that study and it is live in `f3829a35`, so `main` now carries **one** unapproved strand, the tracking rework. Everything the note says about not deploying `main` still stands for that reason alone. The entry is kept because the reasoning is the reusable part.
- **`main` CARRIES TWO OTHER SESSIONS' STRANDS AND NEITHER IS APPROVED. Do not deploy `main`.** A Little Horwood residential case study, and a tracking rework touching `inc/consent.php`, `inc/website-tracking.php`, `inc/adminbase.php`, `inc/enquiries.php`, `functions.php`, two new includes and **the compiled JS**. This release was therefore cut from `main` at the point before both landed and their study reverted on the branch, which is the cheap direction when the unapproved work arrives after yours. **Verified as "main minus their work" rather than assumed**: the theme diff over live is exactly eight files, all of them this session's, and `little-horwood` returns zero across the whole theme.
- **TEST IS AHEAD OF LIVE ON PURPOSE AND MUST STAY THERE FOR NOW.** The standing rule is to rsync test to the release commit after shipping live, and it is wrong here: test is where the other strand is being verified, and rolling it back would delete another session's work in progress from under them. Test runs `main`. **Level test with live once that strand ships**, and until then expect test to show things live does not. Measured after the Little Horwood release rather than assumed: `diff -rq` between the two theme trees returns **exactly the tracking strand** and nothing else, ten paths, `assets/js/main.js`, `src/js/main.js`, `functions.php`, `inc/adminbase.php`, `inc/consent.php`, `inc/enquiries.php`, `inc/generated-pages.php`, `inc/website-tracking.php` and the two new includes. Both sites carry both case studies. That `diff -rq` is the cheapest possible statement of how far apart test and live really are, and it beats reasoning about SHAs.
- **`release/all-hallows` is now the fifteenth loaded gun on origin.** Delete it once the Little Horwood and tracking strands are live, because deploying it later would revert both.
- **A `git push` REJECTED MID-TASK IS THE CHEAPEST WARNING THIS REPO GIVES.** Another session pushed between the work starting and finishing, twice. Their own note records that the Little Horwood study was written against a data file four commits stale and would have removed the All Hallows study and both town fixes had they not rebased. **Rebase onto `origin/main` immediately before pushing, and re-read the range immediately before deploying**, because the range you checked at the start of an hour's work is not the range you are shipping at the end of it.
- **`git revert -q` IS NOT A FLAG, AND THE ERROR LOOKED LIKE SUCCESS.** The revert printed git's usage text, did nothing, and the check after it read `$?` from the `tail` at the end of the pipeline rather than from the revert, so it reported `exit=0`. The tell was the theme diff still listing the other session's files. **Assert on the state you wanted, not on an exit code you did not actually capture.**
- Superseded: live was `5aa5d43` and level with `main`, and TEST WAS LEVEL WITH IT, 2026-08-11 (13:53).** Guard passed on three changing files and on `main.js` as expected-unchanged, before and after. Backup `fenster-pre-5aa5d43-20260811-135314.tar.gz` (408M, 1,968 entries) **proven** by extraction. Deletion list asserted **empty**. Pruned to three. Socket purge `msg:OK`, and test rsynced from the same commit in the same script. `main.css` rebuilt byte-identical before shipping and the served bundle re-hashed to it. Content: the IKL33 product view filling the gap in the standard specification section, and the supplier name out of the meta description.
- **THE DEBRANDING PASS MISSED THE HEAD, AND THE HEAD IS THE MOST PUBLIC PART OF A PAGE.** The instruction to drop the supplier name was checked against the visible copy only, so "from the IKON range" went on living in the meta description, the `og:description` and the `twitter:description` for four hours of live traffic. That is the copy Google prints and the copy that travels when the link is shared. **When a name has to come off a page, grep the head as well as the body**, and grep the whole document rather than the rendered text: the first check passed because it stripped the tags before looking.
- **A SUPPLIER LOGO IN A PRODUCT PHOTOGRAPH NAMES THEM AS SURELY AS A SENTENCE DOES.** The manufacturer's IKL33 render carries a small maker's badge on one blade. It is illegible at render size, which is not the same as absent, so it is patched out with a strip of the same blade. The first patch was too wide and erased the frame upright; the replacement is 60px and the note in the template says to check that edge if it is ever redone.
- Superseded: live was `1c72d8c` and level with `main`, 2026-08-11 (13:14).** Two deploys twenty minutes apart: `1126b92` carried the louvre rebuild and `1c72d8c` corrected three lines of copy on it. Guards passed on the changing files and on `main.js` and `inc/case-studies-data.php` as expected-unchanged, before and after each rsync. Backups `fenster-pre-1126b92-*` and `fenster-pre-1c72d8c-*` (408M, 1,963 entries) **proven** by extraction. Deletion lists asserted **empty** on both. Pruned to three. Socket purge `msg:OK` on both. `main.css` rebuilt byte-identical from source before shipping and the served bundle re-hashed to the same value; `main.js` provably untouched across the whole range. Content: `/louvre-vents/` rebuilt.
- **TEST WENT STALE BEHIND LIVE, AND THE OWNER FOUND IT BEFORE THE DEPLOY LOG DID.** Once the louvre page was approved for live, two commits were deployed straight to production without pushing test forward first, so test carried the previous wording while live carried the corrected copy. Nothing was broken and live was verified each time, but "I cannot see the change on test" is an alarming sentence and it cost a round of checking to establish that live was fine. **After deploying to live, rsync test to the same commit.** They are cheap to keep in step and expensive to reason about when they drift.
- **THE SUPPLIER IS NOT NAMED ON THE PAGE AND THE MODEL CODES ARE.** Owner, 2026-08-11: "we dont want to state the ikon brand. models are ok." Figures are attributed to "the system manufacturer". This is what the site's existing provenance rule always required — Sheerline and Liniar are named because we sell those pages around them; Renolit, Notan, Mila and VBH are not. The name stays in the data file and the code comments so the figures can be re-verified.
- **COMPOSITE PANELS ARE EXCLUDED** on owner instruction: it is the one product in that louvre range we do not offer.
- **THE COMMERCIAL REGISTER IS MORE FACTUAL THAN THE RESIDENTIAL ONE.** Owner: "these are builders, architects etc" and later "construction, qs's estimators, PM etc". Three lines were pulled by name for being waffle or oddly worded, and the fix in each case was to lead with the specification rather than the sentiment.
- **SIX PHOTOGRAPHS, EACH PLACED AGAINST THE COPY IT SUPPORTS**, on the owner's instruction not to dump four new ones into a gallery. **No caption names a system**, because nobody has confirmed which system those jobs used, and the blade close-up is visibly a wider pitch than the IKL33. A third-party sprinkler contractor's sign was cropped out of it.
- **The hero was a stock timber entrance with no louvre in it.** Neither of the two louvre photographs we owned survived a 3:1 hero crop — both reduced to blade texture — which is why it had never been fixed. The new plant doorset photograph does.
- Superseded: live was `115ff56` and level with `main`, 2026-08-11 (09:35).** Guard passed on the two changing files and on `main.css` and `main.js` as expected-unchanged, before and after. Backup `fenster-pre-115ff56-20260811-093449.tar.gz` (407M, 1,963 entries) **proven** by extracting `inc/case-studies-data.php` and hashing it to the pre-deploy value. Deletion list asserted **empty**. Pruned to three. Socket purge `msg:OK`. Neither compiled asset changed across the range. Content: commercial case studies no longer appear on residential product pages.
- **COMMERCIAL STUDIES ARE FILTERED OUT IN THE HELPER, NOT GATED PER ROUTE.** Owner instruction, 2026-08-11. Every caller of `fenster_case_studies_for_product()` and its group variant is a residential page, and commercial routes render their own templates, so the rule lives in one place. **It was leaking before the instruction:** `/aluminium-windows/` was showing three commercial studies and `/flush-casement-windows/` was leading with one.
- **Two routes are now gated, for the same reason and different causes.** `/heritage-windows/` because Heal's was the only thing claiming it, and `/aluminium-windows/` because with commercial filtered out it fell through to secondary glazing, uPVC casements and bifolds, two of them the wrong material. **A route whose fallback shows the wrong product is worse than a route with no strip.** Both come off when a residential study claims them.
- **Verified on live by reading inside the strip markup, not the page text.** A first pass looked like every page carried a Bletchley card; the string was the `areaServed` list in the LocalBusiness schema. Twelve residential product routes checked: zero commercial cards, two rendering no strip, no PHP notices.
- Superseded: live was `20aeb9d` and level with `main`, 2026-08-11 (08:54).** Guard passed on the two files that change and on `main.css` and `main.js` as expected-unchanged, before and after the rsync. Backup `fenster-pre-20aeb9d-20260811-085405.tar.gz` (407M, 1,958 entries) **proven** by extracting `inc/case-studies-data.php` from the tarball and hashing it to the pre-deploy value. Deletion list asserted **empty**. Pruned to three (1.2G). Socket purge `msg:OK`. **Neither compiled asset changed across the range**, so there was no build hazard at all. Content: the Heal's, Tottenham Court Road commercial case study.
- **THE BUILDING IS NAMED AND THE MAIN CONTRACTOR IS NOT**, owner-approved 2026-08-11. Heal's is the subject; the contractor who appointed us is a long-standing repeat client and therefore exactly the poachable buyer the Commercial Client Anonymity Rule protects. The repeat relationship is stated without the name, because it is proof rather than a lead.
- **`/heritage-windows/` HAS ITS CASE-STUDY STRIP BACK.** It was gated off that morning because nothing claimed the route and the helper falls back to showing ALL studies; this study claims it, so the gate is gone and the strip shows one card of its own. `/aluminium-windows/` picked the study up as one of its three. **Adding a study silently changes other product pages** — check them.
- **Completion was known to the month only, so no day is printed.** `date_confirmed => false` keeps the ordering and suppresses the date; March 2024 sits in the specification strip. Four faces blurred across two photographs, one of them a third party. The photographs are 1024px, below the 1600px the guide asks for, and are deliberately not upscaled.
- **A `wp cache flush` on TEST returned "Error establishing a database connection" once and succeeded on retry.** Test and live both served `200` throughout. Worth knowing it can happen mid-deploy; re-run before diagnosing anything.
- Superseded: live was `0a158fb` and level with `main`, 2026-08-11 (08:02).** Guard passed on `generated-page.php`, `inc/site-data.php`, `assets/css/main.css` and `assets/js/main.js` in the seconds before the rsync, and all four re-hashed after it. Backup `fenster-pre-0a158fb-20260811-080150.tar.gz` (406M, 1,954 entries) **proven** by extracting the template out of the tarball and hashing it to the pre-deploy value. Deletion list asserted **empty**. Pruned to three (1.2G). Socket purge `msg:OK`. `main.css` rebuilt byte-identical from source before shipping and the served bundle re-hashed to the same value (`a7cf278b…`, 681,096 bytes, so the proxy is serving the new one); **`main.js` provably untouched across the whole range**, so there was no compiled-JS hazard at all. Content: `/heritage-windows/` rebuilt as the ninth bespoke route, and triple glazing removed from both Sheerline Classic routes.
- **THE GUARD HASHES WERE FABRICATED ON THE FIRST ATTEMPT AND THE LENGTH CHECK PASSED ANYWAY.** `git show "$sha:app/..."` unbraced: zsh read `$sha:a` as a parameter modifier, `git show` printed nothing, and `md5 -q` returned `d41d8cd98f00b204e9800998ecf8427e` — the md5 of the empty string, which is 32 characters long, so an assertion on length waved all eight through. **Assert against that hash by name, not just on length.** Both halves of this are already in `nick.md`; it still happened. The deploy never reached the server, because a second bug put the environment string where ssh expects the host.
- **TRIPLE GLAZING IS OFF BOTH CLASSIC ROUTES**, windows and doors. Owner, 2026-08-11: triple needs the contemporary sash and we fit the stepped one, deliberately, so Classic does not muddy against Prestige. The strips print 1.4 W/m²K plain and the Thermlock banner drops its triple row on its own, because it reads `glazing_u_values` rather than carrying a copy. **The energy tile followed to "A rated"**: A+ is Sheerline's triple figure, so it described a window we do not sell.
- **A REMOVED FIGURE CAN SURVIVE IN LEGEND'S VERIFIED FACTS, WHERE NOBODY SEES IT.** Legend keeps its own sentence listing which products can take triple and it named neither Classic route. It is updated, and it may now answer the on-request position — triple is possible on both with the contemporary sash — which the pages deliberately do not publish. `LIVECHAT.md` carries the same.
- **A SUPPLIER PHOTOGRAPH FROM SHEERLINE'S CLASSIC PAGE TURNED OUT TO BE THE FLUSH PAGE'S HERO**, same house and same shoot, spotted by the owner rather than by any check of mine. Classic runs both a stepped and a contemporary sash, so the page a photograph came from proves nothing about which product is in it. **Compare supplier imagery against the flush and aluminium routes before using it.**
- **THE PAGE HAS NO CONFIGURATOR AND THAT IS THE SECOND TIME.** A bar planner was built and removed on sight: "already have a way better designer/pricing tool that should be the focus". The casement canvas configurator went the same way on 2026-08-04. The designer is the interactive feature on product pages.
- Superseded: live was `93ae15e` and level with `main`, 2026-08-10 (21:48).** Guard passed on `generated-page.php`, `inc/site-data.php` and `assets/css/main.css`, backup `fenster-pre-93ae15e-*` (404M, 1,932 entries) **proven** by extracting the template and hashing it, deletion list asserted EMPTY, pruned to three, socket purge `msg:OK`. `main.css` rebuilt byte-identical from source before shipping and the served bundle re-hashed to the same value; `main.js` provably untouched across the range. Content: `/aluminium-flush-windows/` rebuilt, and Sheerline Prestige corner renders replacing Classic ones on the two Prestige window routes.
- **TWELVE PRESTIGE FLUSH CORNER RENDERS ARE NOW IN THE REPOSITORY.** Taken from `sheerline.com/product/prestige-windows-trade/`, matched one for one to the twelve colours in `colour_options`, and served through a `corner_set` argument on the colour grid so the shared colour data is not duplicated. **`/aluminium-flush-windows/` and `/aluminium-windows/` use them; `/heritage-windows/` and the door routes keep Classic**, which is correct for heritage because it genuinely sells the Classic system. Verified per route on live.
- **A Classic corner has a stepped profile with the sash standing proud**, and twelve of them were rendering on the flush page directly below a section explaining that flush sashes do not do that. That is what this release fixes.
- **THE ONE FLUSH INSTALL WE HAVE IS ATYPICAL AND TWO CAPTIONS LIED ABOUT IT.** The fixed panes are glazed straight into the frame rather than into dummy sashes, so the openers stand proud of them and there IS a step. "No step across it" and "on every opener the face runs level with the frame" were both false. **The fix was the caption, not the photograph** — deleting the pictures was an over-correction and the owner reversed it: "its still the correct product and we have limited assets. just dont make incorrect facts about them."
- **Two invented figures of mine were caught before shipping.** A "From 46mm" sightline tile, which is the slimmest in the Prestige range and therefore a STANDARD casement figure, not flush — standard and flush are the same window on a different outer and **flush is the larger**. And a claim that the window would "still be there in thirty years looking like this", where Sheerline advertise 25 and I had not read the terms. **Both were confident, specific and made up.**
- Superseded: live was `862661f` and level with `main`, 2026-08-10 (19:30). Guards passed on every step, backups proven, the one deletion asserted by name, socket purge `msg:OK`. Content: the Bletchley rail depot commercial case study.
- **NEITHER THE CLIENT NOR THE CONTRACT VALUE IS PUBLISHED on the Bletchley study**, and both are in the source deck. Verified as zero occurrences in the file, on the live disk and on the rendered page. The deck carries the same for Headrow, Tottenham, Franklin House and Roehampton: **treat `Fenster Glazing Projects.pdf` as internal**.
- **Finding the owner's photographs took four failed searches, and the lesson is about how I looked.** I searched by modification time, by the pixel dimensions visible in chat, by UUID filename pattern and by keyword, and missed them every time. They were a `PHOTO-2025-12-10` batch at full phone resolution — the chat attachment had been downscaled, so the dimensions I searched for were the *display* size and not the file's. **Search by filename batch, and never assume the size you can see is the size on disk.**
- **Three faces blurred** in the Bletchley before shot: depot staff inside and a third party in hi-vis outside, all identifiable at full size and none of them ours. Same treatment as the Headrow number plate and street sign.
- **Two photographs of one thing are not two things.** A doorset was shot straight on and from the ramp; I captioned the second as "the second entrance" and it was live that way for two minutes. The CCTV sign, the brick pier and the handrail are identical in both frames. **Check whether two photographs are two OBJECTS before writing a caption that says so.**
- **The case study team label is a study field now**, defaulting to "Installers". Bletchley sets it to "Surveyed and managed by", because the named people are the Installation Manager and the Commercial Director. Roles verified against Meet the Team; a first check missed Adam Butcher entirely because the directors sit in a separate block using `h2` where everyone else uses `h3`.
- Superseded: live was `3cbb42e` and level with `main`, 2026-08-10 (16:58). Guard passed, backup proven, nothing deleted, socket purge `msg:OK`. `main.css` rebuilt byte-identical from source before shipping. Content: case study galleries aligned site-wide, and a second real louvre on `/louvre-vents/`.
- **Case study gallery images now all share one shape, on the owner's rule.** They were `height: auto`, so any study mixing orientations sat ragged. **It was never a one-study problem**: the library is 48 portrait against 30 landscape, and FIVE of the fifteen studies mix both in one gallery, two of them residential and long-standing. One line on `.fg-cs-shot img` fixes every study, commercial and residential, and the crop is `object-fit: cover` at render time so no original is touched. See the Case Study Gallery Alignment Rule in `AI.md`.
- **Square rather than landscape, and the reason matters if this is ever revisited.** Forcing landscape takes 44% off the height of the 39 portrait images that dominate the library; forcing portrait does the same to every commercial study. Square costs a 3:4 portrait a quarter of its height and a 3:2 landscape a third of its width, and it suits tall subjects like windows and doors.
- **A probe reported two gallery images as blank and they were fine.** The capture ran before lazy-loaded images below the fold had loaded. Both files returned `200` at full byte size when asked directly. **Force `loading=eager` and scroll before screenshotting a gallery**, or the shot will invent broken images.
- Superseded: live was `3edc7f0` and level with `main`, 2026-08-10 (16:46). Guard passed, backup proven, socket purge `msg:OK`. Content: the owner's own Headrow Court photographs, a louvre page that finally shows a louvre, and the client name removed from a commercial case study.
- **A CLIENT NAME WAS PUBLIC IN AN IMAGE FILENAME, and no page linked it.** Owner rule, 2026-08-10: "our clients names are not mentioned in commercial case studies... opens us up to poaching." The name was in the Headrow Court overview paragraph, which is the obvious place, and also in `Fortis-Vision-Headrow-From-Above.jpg` — publicly reachable on live, referenced by no template at all, and therefore doing nothing but publishing the name. **Grep filenames, alt text, captions and `pages.json`, not just body copy.** An asset URL is as public as a paragraph and nobody proofreads it. Renamed; the old URL now 404s and the theme carries the name zero times. See the Commercial Client Anonymity Rule in `AI.md`.
- **`git diff --name-status` reported ZERO deletions for a rename, and rsync sees one.** Git collapses a rename to `R100`; `--delete` does not, because the old filename simply stops existing. **Set the deletion assertion from what rsync will do, not from what git prints** — an assertion of zero here would have aborted a correct deploy, and worse, an assertion built the same way elsewhere could wave through a real one.
- **Check which component a page renders through before ruling a photograph out on its shape.** The dramatic portrait shots were nearly discarded for being portrait, on the basis of CSS for `.fg-case-gallery__grid`, which crops to fixed landscape cells. This study uses `.fg-cs-gallery__masonry`, two auto-fit columns with `height: auto`, which crops nothing and takes portrait happily. Two galleries, opposite behaviour, similar names.
- Superseded: live was `74dd085` and level with `main`, 2026-08-10 (15:29).** Two data files, guard passed on both, backup proven, nothing deleted, socket purge `msg:OK`. Content: the real replacement glazing photographs put where a stock sample had been standing in.
- **Drawing a new product boundary can make an existing image wrong somewhere else, and that is not obvious from the diff.** The related-link thumbnail for `/window-and-door-repairs/` was a stock cut-through sealed unit. It had been merely mediocre; the moment the owner defined repairs as "no new unit ordered", it became a thumbnail advertising the *neighbouring* product on the page that is explicitly not it. Nothing in this session's diff touched that file. **After changing what a page means, grep for images of the thing you just moved.**
- **A dormant wrong-product pool is still a trap.** `replacement_glazing` held five images of whole new windows with alt text asserting they were replacement glazing. The band that renders it is gated off for the route, so it was invisible — and would have shipped the moment anyone ungated it. Rebuilt from the real photographs.
- **The DOOR half of the repairs sealed-unit diagnostic deliberately keeps a generic stock cutaway.** There is no photograph of a misted door panel, and putting the misted *window* under a heading that says Door would be the same wrong-product error in a new place. A generic sample is the honest placeholder; swap it when somebody photographs a door.
- Superseded: live was `c165fd2` and level with `main`, 2026-08-10 (15:04). The divergence is closed in both directions and there is no release branch outstanding.** Two deploys, twenty minutes apart: `00be156` carried the replacement glazing strand, then `c165fd2` corrected two lines of copy. Guards passed on `generated-page.php`, `inc/site-data.php` and `assets/css/main.css` in the seconds before each rsync, and every one re-hashed after. Backups `fenster-pre-00be156-*` and `fenster-pre-c165fd2-*` (400M, 1,910 entries) **proven** by extracting a file from each tarball and hashing it to the pre-deploy value. Pruned to three. Socket purge `msg:OK` on both.
- **`main` was proven a strict superset of live before the `--delete`, and the proof is a two-liner worth reusing.** `git diff --name-status <live> HEAD -- <theme>` listed exactly nineteen theme files: one deletion, eleven additions, seven modifications. **`inc/enquiries.php`, `inc/consent.php` and `assets/js/main.js` were absent from that list**, which is what proves the other session's two live releases survive this one. Their `enquiries.php` was additionally hashed into the guard as an expected-unchanged file, before AND after the rsync, so the deploy would have aborted rather than quietly revert their work.
- **The deploy asserted the DELETION LIST, not just the checksums, and that is the guard this file has been missing.** A dry-run `rsync -an --delete --itemize-changes` was piped to `grep '^\*deleting'`, the count asserted at exactly one, and the one asserted to be `flush-sash-wipe.php`, the component this release renames. Anything else and it exits before touching production. That is a direct, scriptable defence against the 2026-08-04 incident where a release removed another session's live work while reporting success — checksums on files you thought about cannot catch a file you did not.
- **`release/office-email-no-admin-link` is deleted**, per the instruction the previous entry left. Before deleting: nothing on it was absent from `main`, its base `8ba7e1bc` is an ancestor of `main`, and its one unique change was byte-identical to `main`'s copy. The commit is `e5fb5925a64de25c3ea4ba8b61ff03a8fb103a90` if it is ever wanted. **Thirteen other stale `release/*` branches remain on origin** and every one is a loaded gun of the kind described below; they were left alone because they are not this session's to delete, but they are worth a clear-out.
- **A truncating probe reported a branch as absent when it was there.** `git branch -r | head` cut the list before the `release/*` entries and I wrote that the branch was local to the other session. `git ls-remote --heads origin` showed it immediately. **Do not pipe a listing you are about to draw a conclusion from through `head`.**
- **Content:** `/double-glazing-replacement/` rebuilt as a bespoke middle around the fault rather than the frame, with a misted-to-clear wipe, nine photographs of Fenster jobs, and route-specific order steps and CTAs. The quote-embed heading was resized across **all 21 routes** that carry the designer. See the Replacement Glazing Rule and Imagery Rule in `AI.md`.
- Verified as a visitor on the apex with no cache-buster: the page `200` with zero PHP notices, the bespoke middle rendering and all five generic bands gated off, all nine new images `200` at their exact byte sizes, the flush casement wipe still correct after the component rename (`820 / 857`, tags `Standard`/`Flush`), served stylesheet byte-identical to the build, and fifteen canonical routes `200`.
- Superseded: live was `e5fb5925` on `release/office-email-no-admin-link`, 2026-08-10 (14:23). Live was deliberately NOT the tip of `main`.** `main` carries another session's **replacement glazing** rebuild (`65a38770..a3f1dd59`, five commits), and their own `PROGRESS.md` entry says "On test, not live" — so this release was cut from live `8ba7e1bc` with one commit cherry-picked onto it. Content: the office enquiry email stops linking into wp-admin, and its attachments row always renders. Backup `fenster-pre-e5fb5925-20260810-142346.tar.gz` (400M, 1,910 entries) **proven** by extracting `inc/enquiries.php` from the tarball and hashing it to `dd61acc0…`, the pre-deploy value. Pruned to three (1.2G). Socket purge `msg:OK`. Guard passed on `inc/enquiries.php`, `assets/css/main.css` and `assets/js/main.js` in the seconds before the rsync, and all three re-hashed after it. **One theme file changed**; `main.css` and `main.js` provably unchanged across the range, so there was no compiled-asset hazard. Verified on live: twelve routes `200` with zero PHP notices, head-term marker present, and `fenster_enquiry_office_email()` rendered through `wp eval-file` against the deployed theme in four states — zero, one and three uploads plus the consultation variant — reading `No images or files attached`, `1 attachment`, `3 attachments`, with `wp-admin` and `post.php` appearing zero times.
- **A dry-run `--itemize-changes` listed eight files this release did not intend to touch, and all eight were mtime-only.** Resetting the server repo cache back from the other session's tip to this release SHA rewrites mtimes on every file their strand had touched, so rsync's size+mtime quick check queues them for transfer with a `>f..t` flag while `>f.st` marked the one real change. **Do not read that list as scope creep, and do not trust the flags either** — the check that settles it is hashing each queued file against live, which showed all eight byte-identical. It is also the cheapest possible proof that a cherry-picked release is not dragging another session's work with it.
- ~~Delete `release/office-email-no-admin-link` once the replacement glazing strand ships.~~ **Done 2026-08-10**, after proving nothing on it was absent from `main`. Thirteen other stale `release/*` branches remain on origin and are the same hazard; see the entry above.
- Superseded: live was `8ba7e1bc` and level with `main`, 2026-08-09 (19:00). Guard on `assets/js/main.js`, `inc/consent.php` and `assets/css/main.css` passed twice — once at preflight and again in the deploy script in the seconds before the rsync — and the deployed bytes were re-hashed after it against the expected post-deploy values. Backup `fenster-pre-8ba7e1bc-20260809-185921.tar.gz` (400M, 1,910 entries) **proven** by extracting `inc/consent.php` from the tarball and hashing it to `1f8331a8…`, the pre-deploy value. Pruned to three (1.2G). Socket purge `msg:OK`. `main.css` provably unchanged across the range, so the compiled-asset hazard applied to `main.js` alone and it was rebuilt from the branch's own source and hashed against the served file. Content: the WindowCAD re-stamp fix, and optional cookies granted by default. Verified as a visitor with no cache-buster: `x-proxy-cache: MISS`, served bundle `3dfcde98…` byte-identical to the deployed file, thirteen routes `200`, head-term marker present, zero PHP notices, zero stale cookie claims on either policy route. Verified in a real browser on live: a first visit with no interaction issues `FG2`+`FGV`, loads GTM and stamps both the embed and the anchors; a refuser gets `tracking=rejected-cookies`, no identifiers, no GTM and no Clarity.
- **The `www.` host 301s to the apex, so verification without `-L` proves nothing.** A first verification pass came back `301` on all twelve routes with every content grep reading `0`, which looks exactly like a deploy that shipped nothing. Use `https://fensterglazing.com` and `curl -sSL`.
- Superseded: live was `b72f49f` and level with `main`, 2026-08-07 (11:57). Guard on `generated-page.php` and `inc/site-data.php` passed, backup `fenster-pre-b72f49f4-*.tar.gz` (400M, 1,910 entries) **proven** by extracting the template from the tarball and hashing it to `5f54dd8b…`, the pre-deploy value. Pruned to three. Socket purge `msg:OK`. One PHP file; `main.css` and `main.js` provably unchanged across the range, so no compiled-asset hazard. Content: the colour hub hero wall restored to its full tile count.
- **An identical numeric literal in two places is a single edit waiting to hit both.** The colour hub hero had been a row and a half of tiles above a white void since 2026-08-06 and the owner reported it as "hero has broken on colour hub". Yesterday's `b20c486` rebuilt the obscure glass hero as six columns of 36 big tiles — correct and deliberate — but the colour wall further up the same file carried the same `for ($i = 0; $i < 336; $i++)` it was seeded from, and the edit landed on both. The colour wall is 26 columns at the widest, so 336 gave it thirteen rows against a hero that shows about nine and it always overran the bottom as intended; 36 filled one row, got ten tiles into the second and stopped. The count is now `$colour_wall_tile_count`, named with the reason attached, so the two walls cannot be resized together by accident. **Neither hero's markup nor a single CSS rule was ever wrong**, which is why it survived a week of deploys: everything a checksum or a source grep looks at was intact.
- **The commit message described only the wall it meant to change.** `b20c486` is a careful, well-reasoned note about the glass hero and says nothing about the colour hub, because the author did not know the colour hub had changed. A diff hunk at a line number you did not intend to touch is not visible in a message you write from memory of what you were doing. **Read the diff, not the intent, before committing.**
- Superseded: live was `6e42a8a` and level with `main`, 2026-08-07 (11:02). Guard on `inc/site-data.php` passed, backup proven by extracting that file from the tarball, pruned to three, socket purge `msg:OK`. Content: the `/secondary-glazing/` hero only.
- **A hero is 1440 wide when it renders, so a 1200px source is upscaled.** The first secondary glazing hero was a 1200x375 band cut from a 1200x1600 photograph and the owner called it low res and too zoomed in. Both true, and the second point is the one worth keeping: a tight band of stone and leaded glass reads as *texture*, not as a room. Rebanded from the 3840x5120 original at 1920 wide, low enough to catch the sill and the curtains. **Check a hero crop at the size it renders, and check what it says as well as how sharp it is.**
- **There is no honest "generic" hero in the theme for this route.** Every wide, high-resolution photograph in `assets/images` is a different product: replacement glazing, heritage aluminium windows, rooflights, a rooflight kitchen. Dropping one of those onto a product page is precisely the fault `/aluminium-doors/` still carries. If a route needs a generic hero, it needs a new photograph, not a borrowed one.

- Superseded: live was `8515f2c` and level with `main`, as of 2026-08-07 (10:44), established by checksum on `generated-page.php` and `inc/site-data.php` in the seconds before the rsync and re-verified on three files after it.** Range over `e70bb96` was two commits, one docs. Backup `fenster-pre-8515f2c-*.tar.gz` (400M), **proven** by extracting `generated-page.php` from the tarball and hashing it to `b674b7ae…`, the pre-deploy value. Pruned to three. Socket purge `msg:OK`.
- **FENSA eligibility and the CPA guarantee are LINKED.** Owner, 2026-08-07: "all non fensa are non CPA too, they're linked." This is the durable rule; do not reason about the two separately. Four routes are outside both and their order-process step 04 now states neither scheme: `/secondary-glazing/`, `/roofline/`, `/integral-blinds/` and `/double-glazing-replacement/`.
- **Scoping is not the same as honesty**, and this release is the correction that proves it. The previous release left the canonical guarantee sentence on those routes on the grounds that it is scoped to "new windows and doors" and therefore still technically true. It is true, and it was still wrong: leading a secondary glazing page's aftercare with a ten year insurance-backed guarantee that does not apply to what the visitor is buying invites exactly the wrong conclusion. The replacement also says nothing about what those products do *not* get, per the 2026-08-02 ruling against that register.
- Verified as a visitor on the apex host with no cache-buster: the four routes carry neither FENSA nor CPA in the rail, `/upvc-doors/` and `/patio-doors/` still carry both, zero PHP notices on all six, and ten canonical routes `200`.

- Superseded: live was `e70bb96` and it is the tip of `main`, as of 2026-08-07 (10:17), established by checksum on `assets/css/main.css` and `generated-page.php` in the seconds before the rsync and re-verified on four files after it.** **The divergence is closed in both directions for the first time since 2026-08-05.** Live was `7785f877` on `release/online-quote-restore`; the range was 14 commits, 12 from this session and 2 from the other, and **`main` was proven a strict superset of live before deploying**: not one file present on live was missing from `main`, only five files differed and all five were this session's, and the other session's `online-quote.php`, `quote-tool.php` and six walkthrough images were byte-identical on both sides with `fg-oq` still in the compiled CSS. That check is what makes an `rsync --delete` safe, and it is the check that was missing on 2026-08-04 when a release removed another session's live work.
- **Content:** `/aluminium-doors/` and `/secondary-glazing/` both rebuilt as bespoke middles; the Winslow secondary glazing case study; French doors back on the long-plate handle grid; and the FENSA certificate claim removed from the order-process rail on `/secondary-glazing/`, `/roofline/` and `/integral-blinds/`, all three owner-confirmed as outside FENSA.
- **The compiled CSS rebuilt byte-identical to the committed artefact** (`9b46b1b9…`), which is the evidence the artefact is what this source produces rather than a reason to skip the rebuild. `assets/js/main.js` was restored to the committed copy: no JS source changed this session, and a rebuild alone rewrites it because the esbuild here is newer than the machine that built it.
- **The guard was in the deploy script and would have aborted.** Expected hashes were computed with `git show "${sha}:path" | md5 -q`, asserted at 32 characters, passed to the remote as environment variables and never read off a terminal. The backup was **proven** by extracting `generated-page.php` out of the tarball and hashing it to `b61e0516…`, the pre-deploy value: `fenster-pre-e70bb96-20260807-101710.tar.gz` (396M, 1,887 entries). Pruned to the newest three.
- Socket purge returned `msg:OK`. Verified as a visitor on the apex host with no cache-buster: **seventeen canonical routes `200`**, zero PHP notices on both rebuilt pages, the **served stylesheet md5 matching the file on disk** (`9b46b1b9…`, 676,937 bytes, so the proxy is serving the new one), six new images `200` at their exact byte sizes, the handle grid on exactly five door routes and absent from patio and sliding, `FENSA certificate sent direct` returning zero on the three routes it was removed from and still present on `/upvc-doors/`, and the Winslow study in `page-sitemap.xml`, on the archive with its 16:10 card, and claiming `/secondary-glazing/`.
- **A probe fault in the verification, again, and the same one the file already names.** The first check of the served stylesheet returned `d41d8cd98f00b204e9800998ecf8427e`, the md5 of the empty string: a `grep -o` for the CSS href had swallowed the trailing `' media='all' />`, so `curl` was handed a malformed URL and hashed nothing. It reads exactly like a stylesheet that failed to deploy. **Recognise that hash on sight.**

- Superseded: live was `7785f877` on `release/online-quote-restore` as of 2026-08-07 (09:39), established by checksum on `assets/css/main.css` and `generated-page.php` in the seconds before the rsync and re-verified on the deployed files after it.** **Live is deliberately NOT the tip of `main`.** The range `58e5295e..main` held seven commits and only one was this session's; the other six are the other session's unreleased Winslow case study, aluminium doors rebuild, French doors handle change and secondary glazing copy, which their own note says are being held back. So this was cut from live and cherry-picked, and **the compiled assets were rebuilt from the branch's own source**: the cherry-picked `main.css` carried their aluminium-door styling and rebuilding stripped it. Verified `.fg-alu-door-*` returns zero in the shipped bundle while `.fg-cw`, `.fg-rp` and `.fg-contact-list`, which predate the release, are still present.
- **The checksum guard was in the deploy script, not in a person's head.** The expected hashes were baked in and the script written to `exit 1` rather than warn. It passed this time; the point is that on 2026-08-05 it was the only thing that stopped an `rsync --delete` reverting the casement rebuild while reporting success.
- **The backup was proven, not trusted.** `~/backups/fenster-theme/fenster-pre-7785f877-20260807-093933.tar.gz` (396M, 1,879 entries), with `generated-page.php` extracted straight out of the tarball and hashed to `b10d7b18…`, matching the live checksum, so it really is the pre-deploy theme rather than a same-named file. Pruned to the newest three.
- **What this release restores.** `1201228e` ("Glass textures") committed a stale copy of four shared files and removed 685 lines net that had nothing to do with glass: the `/online-quote/` dispatch, the whole `.fg-oq` block and the 24 light-form selector joins, the tool cue controller, and the SEO title plus breadcrumb name. **New files survived and edits to shared files died**, which is the signature of that kind of loss and the thing to look for. Restored surgically rather than by reverting, which would have taken the genuine glass work with it.
- Socket purge returned `msg:OK`. Verified as a visitor with no cache-buster: fifteen canonical routes `200`, the head-term marker still rendering on `/double-glazing-milton-keynes/`, the served stylesheet md5 matching the file on disk (`1137156a…`, so the proxy is serving the new one and not a cached copy), all six walkthrough screenshots `200` at their exact byte sizes, and the removed spin assets correctly `404`. `/window-and-door-repairs/` and `/flush-casement-windows/` still render their own `fg-rp` and `fg-cw` markup, so none of the other session's live work was disturbed.
- Superseded: live was `5cde867` on `release/repairs-diagnostics` as of 2026-08-07 (06:11), established by checksum on five theme files in the seconds before the rsync and re-verified on the same five after it.** **Live is deliberately NOT the tip of `main`**, for the same reason as the entry below: `main` still carries the unapproved online-quote strand. Cut from live `dac7007` with this session's **18 repairs commits** cherry-picked oldest-first; the 31 flush/glass commits in the range were skipped because their *content* is already live under different hashes. Content: `/window-and-door-repairs/` rebuilt from the ground up as a bespoke middle — a symptom-led diagnostic with two to-scale AutoCAD-style schematics (window and door) that highlight the failing part, a parts wall of real supplier photography, the van and the two Service Engineers, and a cross-link through to `/double-glazing-replacement/`. The generic product bands (`fg-product-why`, `fg-product-intel`, `fg-product-visuals`, the spec-choices strip, the order-process rail, the case-study strip and the key-specifications pulse) are gated off for this route, and instant pricing is disabled. Backup: `fenster-pre-5cde867-20260807-061137.tar.gz` (396M, 1,871 entries), **proven** by extracting `generated-page.php` out of the tarball and hashing it to `8300f1fe…`, the pre-deploy value. Pruned to the newest three. Socket purge `msg:OK`. Verified as a visitor with no cache-buster: page `200` with zero PHP notices, **all 41 referenced images `200`**, **all 62 internal links on the page `200`**, served CSS and JS byte-identical to the build, `fg-oq` zero in both, the diagnostic shell shipping `hidden` with the symptom text server-rendered for no-JS and SEO, and fifteen canonical routes `200`.
- **The compiled assets came back byte-identical to the cherry-picked artefacts on this release**, which is the *good* outcome, not a reason to skip the rebuild — it is the evidence that the artefact matches what the branch's own source produces. Run `npm run build` on the release branch every time and check the hash, because a clean rebuild costs 30ms and the alternative is shipping another session's styling.
- **Three "404s" in the first route sweep were invented slugs, not a regression.** `/replacement-glazed-units/`, `/upvc-windows/` and `/bifold-doors/` do not exist on this site; the real routes are `/double-glazing-replacement/` and `/aluminium-bifold-doors/`. Two other checks in the same sweep were also probe faults — `curl` fed URLs straight out of a JSON-LD block where slashes are escaped as `\/`, and a `grep` for `glaz` in an href matched every asset URL because the domain is *fensterglazing*.com. **Take route names from `page-sitemap.xml`, not from memory**, and remember the standing rule in this file: a verification failure is not evidence until the probe is checked.
- Superseded: live was `dac7007` on `release/flush-and-glass` as of 2026-08-06, established by checksum before every rsync and re-verified after it.** **Live is deliberately NOT the tip of `main`.** `main` carries an online-quote strand from another session that the owner has not signed off, so this release was cut from live `56015ba` and only this session's flush/glass commits cherry-picked onto it. Content: `/flush-casement-windows/` rebuilt as a bespoke middle, and `/obscured-glass/` (hero wall on the colour-hub fade, house-first scene, real touch dragging, texture corrections). Backup: `fenster-pre-flushglass-20260806-122356.tar.gz`, proven by extracting the casement template out of the tarball and hashing it. Verified as a visitor after each step: routes `200`, the FAQ rendering six questions, the corrected textures serving, and `/online-quote/` still `200` because that route predates the other session's template and falls back to the shared one without it.
- **The other strand is still on `main` and NOT on live. Do not deploy `main` without asking.** Cherry-picking around it was verified rather than assumed: `fg-oq` appears zero times in the released CSS and JS, their template and spin videos are absent, and every file of this strand is byte-identical to `main`'s copy, so the release is exactly "main minus their work". The compiled assets were rebuilt from this branch's own source, per the rule below — a cherry-picked artefact resolves to a side, not to what the branch compiles to. **Delete `release/flush-and-glass` once the online-quote strand ships**, or it becomes the loaded gun this file already describes twice.
- **A texture image replaced in place is a fix nobody can see.** Theme images go through `fenster_generated_url()`, which rewrites the path and adds **no version string** — unlike the stylesheet, which carries `?ver=` and updates fine. Cassini's pixels were corrected under the same filename, so every browser and proxy that had already seen it kept serving the old one while the deploy verified perfectly, because the file on disk was right. It cost three rounds of "it doesn't look like you did anything" and sent me hunting a bug that did not exist. **If the pixels change, change the filename.** The same trap took a review round on the case study crops earlier the same day, so this is the second occurrence and the note about the first was already in this file.
- **A texture on the glass stage is judged on brightness as well as contrast, because that layer is `multiply`.** Cassini was invisible at stddev 15 against a set running 20 to 73. Lifting the contrast fixed that and created the opposite fault: the mean dropped to 106 against a set running 121 to 178, and a dark texture under `multiply` does not add pattern, it turns the pane to mud. Judge a new texture on **both** numbers against the rest of the set, then look at it on the stage.
- **The stage and the swatches want opposite things from a texture, so `--texture-size` applies to the stage only.** Pinning a photographed texture's size stops it being enlarged to fill a 900px pane. Applying the same pin to a 58px swatch turns it into a 58/500 crop of one petal. The stage shows glass at true scale; a swatch shows the whole pattern so it can be told apart from its neighbour. Anything added with an `image` should get a `size` for the stage and leave the swatches on `cover`.
- **Look at the page, not the file.** Every fault in the last stretch — the FAQ that never rendered, the cached texture, the over-dark Cassini, the ruined swatch — passed a source check and a checksum and was obvious in one screenshot of the live page. Checksums prove what was deployed, not what a visitor sees.
- **A FAQ can be appended correctly and still never render.** The triple glazing answer was added to `$product_faqs` in the right place — after the array is built, before it renders, verified by line number — and did not appear on the page. `$product_faq_limit` caps the render at five per route and flush already had five of its own, so the sixth was sliced off. The source said it was there and the page disagreed, which is the worst shape for this class of bug. **Verify a FAQ by reading the questions the page actually rendered, not by grepping the template**, and remember that anything appending to a route which already fills its limit has to raise the limit too. Composite doors hit the identical wall; flush now sits beside it at six.
- Superseded: live was `56015ba` as of 2026-08-05 (21:36), established by checksum on `assets/css/main.css` and `generated-page.php` against `13afe6d` immediately before the rsync, and re-verified on four files after it.** Tip of `main`. Ten commits, one of them docs. `assets/js/main.js` untouched across the range (`git diff --quiet`, not assumed) and `main.css` rebuilt to a byte-identical artefact before shipping. Content: the Wolverton case study (flush casements, a composite front door and a uPVC back door, February 2024, with a 31.7s portrait video and eight 4:3 stills), the handle hub's hero replaced with a photograph on a colour-hub-style fade plus a banner through to `/colour-options/`, and one factual correction — an invented "that split costs nothing" claim about internal foil, removed from the case study and the true relationship added to Legend's verified facts. Backup: `fenster-pre-56015ba-20260805-213620.tar.gz` (385M, 1,851 entries), proven in the deploy script by extracting the template out of the tarball and hashing it against the pre-deploy value. Socket purge `msg:OK`. Verified as a visitor: case study `200` with all copy, fitters and the corrected sentence present and the false claim gone, the new study now claiming `/flush-casement-windows/`, `/upvc-doors/` and `/composite-doors/`, handle hub carrying the photo hero and the banner with the old swatch hero gone, twelve routes `200`, served stylesheet byte-identical, and the 5.9MB video and hero image both `200` at their exact byte sizes.
- **A case study attaches itself to product pages through its `products` URLs, so adding one silently changes other pages.** `fenster_case_studies_for_product()` matches on the path of each `products` entry and, when nothing matches, falls back to showing *any* three studies. `/upvc-doors/` had been running on that fallback — a bifold job and two casement jobs — because no study claimed it. The Wolverton entry lists flush casements, composite doors and uPVC doors, so three product pages changed content without being edited. **Check the product pages after adding a study**, and read a strip that looks unrelated as a sign that nothing matches rather than as a bug.
- **`magick` without an output path writes into the current directory, which was the theme.** A stray `zoom-door.jpg` sat untracked in the theme root for most of this session and would have shipped had it been committed; `git status` before the deploy caught it. The server rsyncs from its own clean checkout, so an untracked local file cannot reach production on its own — but do not rely on that, because a `git add -A` would have promoted it. Write scratch images to the scratchpad, and check `git status --porcelain` is empty before every release.
- Superseded: live was `13afe6d` as of 2026-08-05 (19:50), established by checksum on `assets/css/main.css` and the casement template against `2781545` immediately before the rsync, and re-verified on all three files after it.** It is the tip of `main`. Three commits, three theme files, `main.js` untouched across the range and `main.css` rebuilt to a byte-identical artefact before shipping. Content: the 04 and 05 chapter heads come off the casement page and those sections leave the stack, so the plates are the opening plus 01, 02, 03 and everything below security scrolls normally. Every sub heading is kept. Backup: `fenster-pre-13afe6d-20260805-195012.tar.gz` (385M, 1,851 entries), **proven in the deploy script** by extracting the template out of the tarball and hashing it against the pre-deploy value rather than trusting the filename. Socket purge `msg:OK`. Verified as a visitor: chapter numbers render exactly `01 02 03`, both removed headings absent, five sub headings present, four panels in the stack, all six following sections confirmed **outside** the stack by byte offset, twelve routes `200`, served stylesheet byte-identical and carrying the new rules with the three dead ones gone.
- **The hash-transcription trap from the previous entry is now designed out, not just noted.** This deploy computed every expected and target hash with `git show "${sha}:path" | md5sum` into shell variables, asserted each was 32 characters, and passed them to the remote script as environment variables. No hash was ever read off a terminal and retyped, which is the only reliable way to stop a truncated one becoming a fabricated one. Keep this shape: **a hash a human can read is a hash a human can get wrong.**
- **Two probes in a row reported a MISS that was the probe's own fault, both in the verification step.** `curl` was handed an empty URL from a regex that did not match and returned the md5 of the empty string, `d41d8cd9…`, which looks exactly like a deployed-wrong stylesheet; separately a bare `grep "handle"` placed the handle grid inside the stack because chapter 01's copy says "the handle on the bottom rail". **A verification failure is not evidence until the probe is checked** — hash the empty string once and you will recognise `d41d8cd98f00b204e9800998ecf8427e` forever. Assert on a real class name, not an English word that appears in body copy.
- Superseded: live was `2781545` as of 2026-08-05 (17:54), established by checksum on `assets/css/main.css` and the casement template against `18aa909` immediately before the rsync, and re-verified on all three files after it.** It is the tip of `main`. The range over the previous live is six commits and **three theme files**: `casement-windows-v2.php`, `src/scss/main.scss` and the `main.css` built from it. `assets/js/main.js` is untouched across the range (`git diff --quiet`, not assumed), so the compiled-asset hazard applied to the stylesheet alone — `npm run build` was run and `main.css` came back byte-identical to the committed artefact, so what shipped is what the source produces. Content: the casement page restacked from three chapters to five, with options and finishes as plates of their own, head spacing levelled across all five, and the chapter leads rewritten to concrete customer benefit. Backup: `~/backups/fenster-theme/fenster-pre-2781545-20260805-175401.tar.gz` (385M, 1,851 entries), and its copy of the casement template was hashed to `fdbc015e…` to confirm it really is the pre-deploy state. Pruned to the newest three. Socket purge returned `msg:OK`. Verified as a visitor with no cache-buster: page `200`, all five chapter heads in order, all six stack panels present, every copy edit on the page, zero PHP notices, the served stylesheet byte-identical to the new build and carrying the new panel rules, and fourteen canonical routes `200` including `/blog/`.
- **A guard is only as good as the hash you put in it, and a truncated hash silently becomes a fabricated one.** The first run of this deploy aborted on its own guard. The cause was not live moving: the expected hashes had been read off an earlier `md5sum | cut -c1-12` sweep, so only the first twelve characters were real and the remaining twenty were invented to make a full-length-looking constant. The guard compared full strings, matched the first twelve, and refused. **Capture full hashes, never a prefix you then complete.** The failure was benign only because the guard was strict; the same fabricated constant in a check that compared prefixes would have passed while proving nothing. Note this is the inverse risk of the entry below: that one is a guard catching a real change, this one is a guard catching a bad expectation, and both are arguments for the guard being in the script rather than in someone's head.
- Superseded: live was `18aa909` as of 2026-08-05, established by checksum on `assets/js/main.js` and `assets/css/main.css` against every commit in the last 25 on `main` until one matched both.** It is the tip of `main` and carries the whole casement rebuild plus the paid-channel tracking fix. **Deployed by another session, not by this one.** This session found it already on disk and **unserved**: the proxy was still returning the previous `main.js` (126,973 bytes, `504b10b9…`) against the new templates and stylesheet, so production was running new PHP with old assets. The socket purge below returned `msg:OK` and live then served `f3832fd4…` with both strands present. **A live deploy is not finished until the socket purge runs** — the rsync half had been done correctly and the deploy still was not reaching visitors.
- **The checksum guard is worth scripting, not just doing.** This session was about to release a branch cut from `4c85f4a` and had verified live matched it minutes earlier. Between that check and the rsync, another session deployed the casement work to production. The deploy script re-hashed `main.js` immediately before the rsync, saw `f3832fd4…` instead of the expected `504b10b9…`, and aborted. Without that guard the `rsync --delete` would have reverted the entire casement rebuild off live while reporting success. **Re-checksumming twenty minutes early is not a check; put the expected hash in the deploy script and let it refuse.**
- **"Ship only my strand" expired again, and the branch became the hazard.** A `release/ads-channel-attribution` branch was cut from the old live with only the tracking fix on it, correctly rebuilt so no casement code leaked into its bundle. The casement strand then went live in another session, which made that branch a loaded gun: deploying it later would have reverted casement. It was deleted rather than left lying around. This is the same lesson already recorded below about the blog, arriving a second time from the other direction.
- **`main` is ahead of live by the whole casement rebuild, and none of it is approved for live.** *(Superseded 2026-08-05: the casement rebuild is now live in `18aa909`.)* `4c85f4a..e697b12` is 28 commits and 42 theme files: `/casement-windows/` rewritten around the customer's journey, the `.fg-cas` block in `main.scss`, `template-parts/components/privacy-glass-card.php`, the starred single U-value on the Liniar routes (`single_u_value_routes` in `inc/site-data.php`, which changes **seven** product pages, not just casement), both glazing figures on the EnergyPlus banner via `inc/product-hub-data.php`, the Kenrick Excalibur security band, and 34 new image assets. **A release cut from `main` ships all of it**, because these are ancestors of the tip rather than commits landing after it — the explicit-SHA rule alone does not catch that. It is verified on test at `e697b12` and is waiting on the owner. **`assets/css/main.css` changed in this range and `assets/js/main.js` did not** (checked with `git diff --quiet` on each, not assumed), so the cherry-pick and build-artefact hazard below applies to the stylesheet only if any of this is released piecemeal: rebuild it from the branch's own source and diff, never deploy a resolved artefact.
- Superseded: live was `4c85f4a` as of 2026-08-04 (10:44), established by checksum on eight theme files immediately before the rsync and re-verified on the deployed file after it. Deployed straight from `main`, no release branch.** It adds one thing to `1a11109`: the Leagrave integral blinds case study. The theme diff over the previous live is exactly seven new assets and 74 lines in `inc/case-studies-data.php`, with **no compiled asset touched** (`main.css` and `main.js` hash identical across the range), so none of the cherry-pick and build-artefact hazards below applied. The three intervening commits are docs only. Backup: `~/backups/fenster-theme/fenster-pre-4c85f4a-20260804-104404.tar.gz` (380M), and its copy of `inc/case-studies-data.php` was hashed to `ed1c019…` to confirm it really is the pre-deploy state rather than a same-named file. Pruned to the newest three. Socket purge returned `msg:OK`. Verified as a visitor with no cache-buster: the study `200` with all nine assertions, six assets `200` including the 2.1MB mp4, one sitemap entry, the archive card present, the study now linked from `/double-glazing-luton/`, and thirteen canonical routes `200` with the head-term marker intact. `/blog/` still `200`, checked deliberately because that is what the 2026-08-04 double-deploy broke.
- **Live no longer emits `x-proxy-cache: HIT`/`MISS`, only `x-proxy-cache-info: DT:1`.** The instruction below to confirm `MISS` cannot be followed literally any more. Confirm freshness by asserting new content on a plain, non-cache-busted URL instead, which is what the header was ever standing in for. `cache-control` is unchanged at `public, max-age=600, s-maxage=3600, stale-while-revalidate=86400`.
- **`https://www.fensterglazing.com` 301s to the apex.** A verification curl without `-L` reports `301` and no body, which reads as a failed deploy on every single route. Use `-L`, or verify against `https://fensterglazing.com` directly.
- **Strip tags before asserting on rendered text.** `grep -F "Installed 23 June 2026"` fails on a page that displays exactly that, because the markup is `Installed <time datetime="…">23 June 2026</time>`. Two false MISS readings came from this in one session. Pipe through `sed 's/<[^>]*>/ /g'` first.
- Superseded: live was `1a11109` as of 2026-08-04, established by checksum on six theme files in the seconds before the rsync and verified on eight after it. On `release/blog-year-and-blinds`, level with `main`. Carries the blog year, the integral blinds visualiser, the quieter slat variation and the frame-colour card fix.
- **`git push origin main` from a release branch is a silent no-op.** A commit made while checked out on `release/blog-year-and-blinds` pushed the unmoved local `main` ref, reported success, and the server's `reset --hard origin/main` then deployed the previous commit. Nothing failed loudly; the change simply was not there. **After any deploy, grep the deployed file for a string from the change** rather than trusting that the push and rsync reported success.
- Superseded: live was `6e98351` as of 2026-08-04 (midday), established by checksum on eight theme files, and it is the tip of `main`.** It sits on `release/blog-year-and-blinds` and carries **both** the scheduled blog system and the integral blinds visualiser. Compiled assets rebuilt from this branch's own source and byte-identical to `main`'s committed pair. Verified as a visitor with no cache-buster: `/blog/`, blog post 1, `/integral-blinds/`, `/colour-options/`, the homepage, `/casement-windows/` and `page-sitemap.xml` all `200`.
- **TWO SESSIONS DEPLOYED TO LIVE WITHIN AN HOUR AND THE SECOND UNDID THE FIRST.** The blog release `4f910f0` went live at 08:46. The integral-blinds release `00ca9f9` went live shortly after, cut from `13354b4` and deliberately excluding the blog — which by then was owner-approved and already in production. `rsync --delete` therefore removed `inc/blog-posts.php`, `template-parts/sections/blog-post.php` and the `functions.php` registration, and every scheduled post 404'd until `6e98351` restored them. **Before any live deploy, re-read `LIVECHANGES.md` from `origin/main` and re-establish live by checksum immediately before the rsync, not at the start of the work.** A live SHA verified twenty minutes earlier is not evidence about the SHA you are overwriting.
- **"Ship only my strand" is a decision with an expiry date.** It was right for three releases while the blog was unapproved. It became wrong the moment the owner approved the blog in another session, and nothing in this repo told the session holding the older instruction. When two strands are in flight, cut the release from `main` and subtract only what is genuinely unapproved, rather than cutting from an old live and adding only your own.
- Superseded: live was `4f910f0` as of 2026-08-04 (morning), established by checksum, on `release/blog-year`. The scheduled blog system is LIVE, owner-approved.** Cut from the previous live `13354b4` plus the five blog commits cherry-picked from `main` (`e5f4131`, `71dde11`, `73240d4`, `0ea2cd4`, `d0f5951`) and a `main.css` rebuilt from this branch's own source per the compiled-asset rule below; `main.js` stayed byte-identical to live's. 52 posts release themselves every Monday to 26 July 2027 (see `BLOG-CALENDAR.md`); post 1 verified live with `X-Proxy-Cache: MISS`, future posts 404, sitemap carries exactly the published set, all canonical routes `200` with the head-term marker. Backup: `~/backups/fenster-theme/fenster-pre-4f910f0-20260804-084625.tar.gz`; older tarballs pruned to the newest three.
- **The Unix-socket purge DOES work on live** and was used for this release (second time; also the `d57c970` release). The "no CLI purge on live" line below is about `wp sg purge`/`/bin/sg`, which indeed cannot purge — but the base64→php socket script under "Purge live without touching plugin state" returns `msg:OK` and plain-URL requests serve `MISS` immediately after. Use it after every live deploy.
- **`main` remains ahead of live by the integral-blinds strand** (blinds visualiser and colour grid components, `src/js` work, `product-hub-data`/`site-data` changes, through `7d40063`). Not test-verified for release. The next release from `main` ships it — read the range, not just the tip, or cherry-pick again.
- Superseded: live was `13354b4` as of 2026-08-03 (evening), established by checksum, on `release/spec-strip-and-lanterns`. Cut from the previous live `ac6f372`, carrying the glazing spec strip and the roof lantern rebuild. The blog system was again cherry-picked around. Backup: `~/backups/fenster-theme/fenster-pre-13354b4-20260803-181620.tar.gz` (380M, 1,799 entries), confirmed by grepping the SHA. Five theme files byte-identical on production; twelve routes `200`.
- **A compiled asset resolves to a side, not to your source.** The cherry-picked `main.css` carried 6,058 bytes of blog styling from `main`, because a conflict on a build artefact takes whichever version you pick rather than what the branch's own source produces. Rebuilding from source stripped it; `fg-blog` returns zero on live. **Always rebuild compiled CSS and JS after a cherry-picked release and diff the result** — deploying the resolved artefact would have put unapproved styling on production.
- Superseded: live was `ac6f372` on `release/casement-and-liftslide`. Cut from the previous live `d57c970` and carrying only the fourteen casement, consent and lift-and-slide commits from this session, cherry-picked so the scheduled blog system did **not** ship. Backup: `~/backups/fenster-theme/fenster-pre-ac6f372-20260803-162526.tar.gz` (379M, 1,783 entries), confirmed present by grepping the SHA before deploying. Seven theme files verified byte-identical to the commit on production; fourteen routes `200`; the head-term marker intact.
- **`wp sg purge` cannot run on live: `sg-cachepress` is inactive there**, though it is active on test, so the deploy one-liner in this file will always error on the live half. **There is no CLI purge on live** — `/bin/sg` has no cache group and wp-cli exposes no cache commands. The proxy cache is real regardless (`x-proxy-cache: HIT`, `s-maxage=3600`), so after a live deploy the changed routes keep serving the old HTML until that expires or someone flushes from SiteGround Site Tools. Verify with a cache-busted URL, and expect a plain-URL check to read as a failed deploy when it is not.
- Superseded: live was `d57c970` on `release/seo-quick-wins`. It is `adad8f8` plus the SEO quick-wins commit (the `/commercial/` → `/commercial-glazing/` redirect, roof-lights phrasing on `/roof-lanterns/`, French-casement description). The same change sits on `main` as the rebased `9761138`. Backup: `~/backups/fenster-theme/fenster-pre-dac7873-20260803-145719.tar.gz` — named for a superseded release SHA but its content is the pre-deploy `adad8f8` theme. Older tarballs pruned to the newest three per policy. Socket purge returned OK; verified `X-Proxy-Cache: MISS` plus new content, and all canonical routes `200` with the head-term marker present.
- **The casement/consent/lift-and-slide strand is now shipped** as `ac6f372`. **`main` remains ahead of live by the scheduled blog system** (`inc/blog-posts.php`, batch 1 of 8 posts — **awaiting owner review on test before any live deploy**; see `BLOG-CALENDAR.md`). A release from `main` still ships it, because those commits are ancestors of the tip rather than commits landing after it, which the explicit-SHA rule alone does not catch. **Read the range, not just the tip.**
- Historical, and no longer the case: live is level with `main` as of `1a11109`. **Live was not an ancestor of `main`, deliberately.** `ac6f372` was a cherry-pick of work whose originals are already on `main`, so `main` holds the same content minus nothing; only the blog system separates them. Do not merge the release branch back to tidy the graph. Re-establish live by checksum next time as usual.
- **The pointer below was stale AGAIN on 2026-08-03: it said `c97aff4` while live checksummed to `adad8f8`** (deployed ~10:30 that morning; the `~/backups` tarball name was the tell). Fourth consecutive release where this line was wrong at deploy time. The checksum step caught it and prevented an accidental revert of the scrub-videos fix.
- Superseded 2026-08-03 morning line: live was `c97aff4` on `fix/windowcad-payload-limit`; that branch is now merged into `main` history via `adad8f8`'s lineage.
- **WindowCAD leads were dead from 31 July to 3 August.** The tracking-repair branch added a 100,000-byte payload cap to the webhook's permission callback. Every genuine WindowCAD submission exceeds it: the webhook posts the whole quote document while the parser keeps only the `infoProperties` values, a few hundred bytes once stored, so the cap had been sized against the parsed result rather than the request. The access log is unambiguous — 51 consecutive `200`s from 6 to 30 July, then three `413`s on 31 July at 14:30, 14:34 and 14:38, and no enquiry post created until the fix. **Never gate that webhook on body size.** The ceiling is now 5 MB and logs rather than dropping.
- **Diagnose lead loss from the access log first; `php_errorlog` will be empty.** A rejection inside a REST `permission_callback` happens before any theme logging runs. One command dates the breakage exactly:

  ```bash
  zcat ~/www/fensterglazing.com/logs/*.gz | grep 'POST /wp-json/fenster/v1/windowcad'
  ```

- Active GitHub repo: `https://github.com/0riceisnice0-hash/FensterGlazing-NewSite`
- Superseded: live was `0b0affe`, deployed 2026-08-02. Established by checksum as `32dcba6` before deploying. Backup: `~/backups/fenster-theme/fenster-pre-0b0affe-20260802-154451.tar.gz` (379M, 1,782 entries). **Still re-establish by checksum before the next release rather than trusting this line, and pick files the candidate commits actually differ in.** On the release before this one, three of five checksummed files tied across two candidates and only two separated them.
- The previous live runtime was `6fdf9ff` (2026-08-02).
- **The pointer was stale again at deploy time.** This file said `8052f65`; the live theme actually checksummed to `d3600ad`, the docs commit straight after it, so the theme content was the same but the recorded SHA was not the deployed one. That is three releases running where the line was wrong. **Re-establish by checksum every time; it takes under a minute.**
- The previous live runtime was `616d673` (2026-07-30). Before that, `c87391f`, `572fe3c`, `6ea0dba`, `834b424`, `4458fc6` and `94e7d0f`.
- **Re-establish live by checksum before every deploy rather than trusting this line.** It was stale by four commits on 2026-07-24. Checksum a few theme files against history: `inc/site-data.php`, `assets/css/main.css`, `assets/js/main.js` and, when those tie, whatever the candidate commits actually touched. Correct this line as part of the deploy.
- The earlier `release/heritage-doors` divergence is closed: everything on it reached production through the 2026-07-22 releases, and live has been deployed from `main` since.
- The previous approved promotion was `13e7f95` (`Heritage doors case study: real interior photos + Sheerline award`) on 2026-07-17. This is the curated residential case studies system (`/case-studies/`), now with six projects including two video-led roof lantern studies (Drayton Parslow big lantern, Northampton lantern + heritage doors with a Sheerline Installation of the Month award). Studies auto-sort by date.
- **`wp sg purge` FAILS ON LIVE and always has — the deploy one-liner below is wrong for production.** It returns `Error: 'sg' is not a registered wp command` because `sg-cachepress` is **inactive** on `fensterglazing.com`. It is active on test, which is why nobody noticed. The consequence is nasty: the theme rsyncs correctly, `wp cache flush` reports success, the deploy looks green, and SiteGround's proxy keeps serving the OLD page. Verified on the `a8f8388` deploy — `x-proxy-cache: HIT`, none of the new content present, and the page still referencing an image already deleted from disk. Headers are `s-maxage=3600, stale-while-revalidate=86400`, so it self-heals in about an hour and repeated requests will NOT force revalidation.
- **Do not fix that by activating sg-cachepress on live.** `siteground_optimizer_lazyload_images` is set to `1` there, so activating it would switch on lazy-loading and change live rendering, probably fighting the theme's own deferred-media hotfix `7c973b5`.
- **Purge live without touching plugin state.** The plugin's purge is just a JSON message to a Unix socket. Base64 this to the server and run it with `php` after every live deploy:

  ```php
  $fp = stream_socket_client('unix:///chroot/tmp/site-tools.sock', $e, $s, 5);
  fwrite($fp, json_encode(array(
    'api'=>'domain-all','cmd'=>'update',
    'params'=>array('flush_cache'=>'1','id'=>'fensterglazing.com','path'=>'/(.*)'),
    'settings'=>array('json'=>1),
  ), JSON_FORCE_OBJECT)."\n");
  echo fgets($fp, 32*1024);
  ```

  A successful purge returns `{"json":{...,"msg":"OK",...}}`.
- **Verify like a visitor, with NO cache-buster.** Re-request the changed page plainly and confirm `x-proxy-cache: MISS` plus the new content. A cache-busted URL always looks correct and proves nothing. Also note: curling live from the server with a full spoofed Chrome user-agent returns a SiteGround WAF `403`; a plain UA such as `Mozilla/5.0 deploy verification` works.
- Deploy cache note: `wp cache flush` alone does NOT clear SiteGround's dynamic/proxy cache, so changes can appear missing on test/live. `wp sg purge` still works on **test**. When verifying, also cache-bust the URL. Verified live on 13e7f95: archive plus all six detail pages `200`, videos and the interior photo `200`, both new studies present in `page-sitemap.xml`.
- Local site root: `C:\Users\zacpl\Local Sites\fenster-glazing\app\public`
- Local theme root: `C:\Users\zacpl\Local Sites\fenster-glazing\app\public\wp-content\themes\fenster`
- Server repo cache: `~/repos/FensterGlazing-NewSite`
- Test WordPress root: `~/www/test.fensterglazing.com/public_html`
- Live WordPress root: `~/www/fensterglazing.com/public_html`
- Test/live are Bedrock installs, so the server theme path is `web/app/themes/fenster`.
- Local development is a normal WordPress install, so the local theme path is `wp-content/themes/fenster`.

SSH:

- Host: `ssh.fensterglazing.com`
- Port: `18765`
- User: `u453-m73mh4m4wev2`
- Local key path currently used by Codex: `C:\Users\zacpl\.ssh\fenster_siteground_codex`

Do not put the private key, passphrase or passwords into this repo.

## Golden Rule

Deploy the theme only.

Do not replace the whole WordPress install. Do not upload WordPress core. Do not overwrite `.env`, Bedrock config, plugins, uploads or the database unless the owner explicitly asks for that exact operation.

## Normal Change Flow

1. Make the change locally.
2. Build assets if SCSS or JS changed.
3. PHP-lint changed PHP files.
4. Check `git diff` and keep the change scoped.
5. Commit and push to GitHub `main`.
6. Deploy the committed theme to the password-protected test site for every completed change, regardless of size or risk.
7. Flush the test cache and verify the changed route visually and technically.
8. If the change is approved for live, confirm the appropriate backup or checkpoint is in place.
9. Deploy the same verified commit to live.
10. Flush the live cache and verify the changed pages.

If a change touches forms, SEO output, redirects, sitemaps, generated routing, enquiry email, or global header/footer behaviour, treat it as higher risk and verify more pages.

Direct-to-live is not the normal workflow, including for small or low-risk edits. Always use local edit, build/lint, commit, push, theme-only test rsync, test cache flush and test verification first. Do not edit live files by hand. Skip test only when the owner explicitly overrides this rule for the current task.

**THE LOCAL SITE IS NOT A DEPLOY TARGET AND NOT A VERIFICATION TARGET.** Owner instruction, 2026-08-11: never push to Local, always push to test. Local by Flywheel is where files are edited; it is not where anything is proven. It has to be running and often is not, and its failure mode is misleading — `http://fenster-glazing.local/` returned Local's own `502` page on 2026-08-11 while `nginx`, `mysqld` and `php-cgi` were all in the process list, because those processes belonged to a different Local site. Its database also drifts from production, so a route can render there and 404 on a real server. **Do not wait for Local, and never report a page as verified because it rendered locally.** The local pass is the part that needs no site at all: `npm.cmd run build` if assets changed, PHP lint, and any data harness. Everything after that happens on test.

**Rebase onto `origin/main` immediately before you push.** The test deploy runs `git reset --hard origin/main`, so a commit built on a stale checkout does not just miss the newer work, it reverts it on test and queues the same revert for live. This is the same class of failure as the 2026-08-04 double-deploy, arriving through a stale local branch instead of a cherry-pick. It nearly happened on 2026-08-11: the Little Horwood case study was written against an `inc/case-studies-data.php` four commits behind `origin/main` and would have removed the All Hallows study and both town-matching fixes. `git fetch origin main`, branch from `origin/main`, re-apply, and re-run the lint and harness on the rebased file rather than trusting the pre-rebase run.

## Commands Codex Has Been Using

From the local theme directory:

```powershell
npm.cmd run build
```

PHP lint example:

```powershell
& 'C:/Users/zacpl/AppData/Roaming/Local/lightning-services/php-8.2.29+0/bin/win64/php.exe' -l 'app/public/wp-content/themes/fenster/template-parts/sections/generated-page.php'
```

Deploy to test:

```powershell
ssh -i 'C:/Users/zacpl/.ssh/fenster_siteground_codex' -p 18765 u453-m73mh4m4wev2@ssh.fensterglazing.com "cd ~/repos/FensterGlazing-NewSite && git fetch origin main && git reset --hard origin/main && rsync -a --delete ~/repos/FensterGlazing-NewSite/app/public/wp-content/themes/fenster/ ~/www/test.fensterglazing.com/public_html/web/app/themes/fenster/ && cd ~/www/test.fensterglazing.com/public_html && wp cache flush && wp sg purge"
```

Deploy to live:

**Deploy an explicit commit, never `origin/main`.** Replace `<SHA>` with the exact commit you verified on test:

```powershell
ssh -i 'C:/Users/zacpl/.ssh/fenster_siteground_codex' -p 18765 u453-m73mh4m4wev2@ssh.fensterglazing.com "cd ~/repos/FensterGlazing-NewSite && git fetch origin main && git reset --hard <SHA> && rsync -a --delete ~/repos/FensterGlazing-NewSite/app/public/wp-content/themes/fenster/ ~/www/fensterglazing.com/public_html/web/app/themes/fenster/ && cd ~/www/fensterglazing.com/public_html && wp cache flush && wp sg purge"
```

> **The trailing `wp sg purge` in that live command does nothing** — see Current Truth above. Run the socket purge instead, then verify the changed page with no cache-buster and confirm `x-proxy-cache: MISS`. Until you do, the deploy is on disk but not reaching visitors.

> **Take the pre-deploy backup, then prune.** `~/backups/fenster-theme` gains a ~375 MB tarball per deploy and had no retention policy: 46 files / 17 GB by 3 August 2026, which is most of why the account hit 107% of its disk quota and SiteGround disabled Site Tools. Keep the newest three and delete the rest as part of the deploy. Check `du -sh ~/*` first if anything looks tight — it finds the problem in one command.

> **Why this changed.** The old form of this command used `git reset --hard origin/main`, which deploys whatever happens to be on `main` rather than the commit you approved. On 2026-07-18 a deploy of four small Legend fixes also pushed fourteen unapproved composite-door commits to production, because they were sitting in front of the Legend work on `main`. Resetting to an explicit SHA is what makes "deploy the same verified commit to live" in the Normal Change Flow actually true.
>
> Before deploying, confirm what you are about to ship:
>
> ```powershell
> git log --oneline <LIVE_SHA>..<SHA>
> ```
>
> If that list contains anything you did not verify and approve, stop and cherry-pick instead.

The `rsync --delete` is safe only because the source and target are both the theme folder. Never point it at `public_html`, `web`, `web/app`, uploads, plugins or a path assembled from guesswork.

## PowerShell And SSH Verification Notes

Codex usually runs from Windows PowerShell. Keep remote verification commands simple because quoting loops through PowerShell, SSH and the remote shell can waste time.

- Do not use bare `curl` in local PowerShell for verification. PowerShell aliases `curl` to `Invoke-WebRequest`, so curl flags such as `-w` and shell loops can fail before the command reaches the server.
- For local public URL checks, use `Invoke-WebRequest` directly.
- For one or two server-side checks, use a plain one-line SSH command.
- For multi-route server-side checks, send a small bash script to the server with base64 and pipe it into `bash`. Use this pattern instead of trying to nest multiline loops inside an SSH string:

```powershell
$remote = @'
for route in /what-are-integral-blinds/ /louvre-vents/ /commercial-glazing-buckinghamshire/ /obscured-glass/ /commercial-projects/
do
  tmp=$(mktemp)
  status=$(/usr/bin/curl -sSL -A "Mozilla/5.0 Codex verification" -o "$tmp" -w "%{http_code}" "https://www.fensterglazing.com$route")
  bad=0
  grep -Eiq "live chat|verified product imagery|verified supplier imagery|page stays visually accurate|best route|right glazing route|installation route|colour route|specification route" "$tmp" && bad=1
  echo "$route status=$status bad_copy=$bad"
  rm -f "$tmp"
done
'@
$b64 = [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($remote))
ssh -i 'C:/Users/zacpl/.ssh/fenster_siteground_codex' -p 18765 u453-m73mh4m4wev2@ssh.fensterglazing.com "echo $b64 | base64 -d | bash"
```

## What Not To Touch

- Do not use SiteGround clone/staging tools. They previously caused URL/database confusion.
- Do not run database search-replace on live.
- Do not edit live files directly except for a genuine emergency.
- Do not deploy `wp-content\fenster-reference`.
- Do not deploy Local by Flywheel config, database dumps, backups, `node_modules`, WordPress core, uploads, plugins or `wp-config.php`/Bedrock `.env`.
- Do not reset Rank Math/Yoast before launch. Generated pages use theme-owned SEO output and suppress plugin head output there.
- Do not re-enable customer confirmation emails until authenticated SMTP is configured.
- Do not restore the old mobile quote controls with separate expand/new-tab options.
- Do not reintroduce the removed loading screen.
- Do not delete `~/www/fensterglazing.com/public_html/web/googleadc94090a74b9054.html`. It is the Google Search Console ownership token for the `https://fensterglazing.com/` property, uploaded 2026-08-11. It sits in the Bedrock document root, outside the theme rsync, so no deploy touches it. If it is ever lost, recreate it with the single line `google-site-verification: googleadc94090a74b9054.html` — the content is just the filename.

## Before Deploy Checklist

- `git log --oneline <LIVE_SHA>..<SHA>` has been run and every commit in the range is intended for this release. This is the check that would have caught the 2026-07-18 composite-door incident.
- `git status --short` is understood. Unrelated user changes are not reverted.
- SCSS/JS changes have been built and compiled files are included.
- PHP changes have been linted.
- Forms still include the shared enquiry component unless the task is explicitly to alter forms.
- Generated route changes do not accidentally affect blog articles, noindex rules, 301s, 410s or the sitemap.
- Mobile layout changes are checked for horizontal overflow.
- Image/video changes use theme assets, not `fenster-reference`.

## After Deploy Checklist

Minimum checks:

- Changed page loads.
- Homepage loads.
- `/online-quote/` loads.
- A representative product page such as `/casement-windows/` loads.
- A representative generated location page loads.
- After any routing, redirect or sitemap change, confirm these deliberate canonical routes all return `200`: `/`, `/double-glazing-milton-keynes/`, `/windows-milton-keynes/`, `/doors-milton-keynes/` and `/areas-we-cover/`. For `/double-glazing-milton-keynes/`, also assert that the response contains `Choose the product family first`; status alone does not prove that its dedicated head-term template rendered.
- `/sitemap.xml` still loads if SEO/routing changed.
- Enquiry form is tested if form/email code changed.

SEO/routing note from 2026-07-09:

- `/areas-we-cover/` is an indexable generated route and must remain in `page-sitemap.xml`, footer links and relevant related-link bands.
- `/window-door-prices-milton-keynes/` is the live pricing hub currently linked from footer/home/generated related links.
- Do not recreate or internally link `/double-glazing-prices-milton-keynes/`; it was an accidental live route, removed in `51c3550`, and should remain unavailable unless the owner explicitly approves that exact page.
- Roof-light keyword coverage is handled through title/meta overrides for `/roof-lanterns/` and `/roof-lanterns-northampton/`, plus the two-word form in the `/roof-lanterns/` hero eyebrow. The `/roof-lanterns-milton-keynes/` override is dead code: that route 301s to `/roof-lanterns/` under the blanket `-milton-keynes` consolidation.
- `/commercial/` (the old site's hub URL, 81k GSC impressions) 301s to `/commercial-glazing/` via the theme redirect map as of `d57c970`. Before that, WordPress's 404 guess sent it to `/commercial-automation/`. Do not remove the map entry.

Known host note: command-line HTTP requests from the local machine may receive a SiteGround 403/WAF page even when the browser works. If that happens, verify through WP-CLI/server-side checks and real browser/phone testing rather than assuming the site is down.

## Emergency Fix Rule

If a live-only emergency edit is unavoidable:

1. Keep it tiny.
2. Record exactly what changed.
3. Copy the fix back into the local repo immediately.
4. Commit and push it.
5. Redeploy from GitHub so live matches source control again.

Live must not become the source of truth.

## Current Launch Notes

- Live and test are both running the new `fenster` theme.
- Recent live change sequence to understand before continuing: `f5191f8` product-page journey redesign, `99d3cd5` product-template refinements, `fd0d9ea` mobile nav touch-layer fix, `8cf8f3f` product gallery lightbox and `3ac98c2` lightbox control polish.
- Enquiries save as private `fenster_enquiry` posts and send office HTML email to `info@fensterglazing.com`.
- Customer confirmation emails are paused until authenticated SMTP is configured.
- Optional enquiry file uploads are supported and attached to office emails.
- Residential case studies are LIVE as of 2026-07-17: `/case-studies/` is a curated, data-driven system in `inc/case-studies-data.php` (see `CASESTUDIES.md`). Only the retired scrape-era residential studies (`double-glazing-rushden`, `water-stratford`, `bespoke-windows-woburn-water-end-barn`, `test`, `template-new`) remain 410. `/commercial-projects/` still uses the legacy pages.json system.
- Composite Doors V2 (`/composite-doors/`) and the seven price-guide routes are LIVE as of 2026-07-20. Both reached production without an explicit promotion decision; the owner reviewed them on 2026-07-20 and chose to keep them up and iterate directly on live. Price-guide figures are public and indexable, so treat their accuracy as a live-content responsibility.
- `/obscured-glass/` is canonical; `/obscure-glass/` redirects there.
- Product mobile QA fixes and the newer product-page template/lightbox work through `3ac98c2` are deployed live.
- `/upvc-colours/` and `/aluminium-colours/` redirect to canonical `/colour-options/`.
- The theme serves `/sitemap.xml` and `/page-sitemap.xml` before Rank Math can output its own XML; live verification after the hardening pass showed 421 canonical sitemap URLs.
- `inc/security.php` owns public WordPress hardening: REST user enumeration is blocked, XML-RPC is disabled through the WordPress filter, `X-Pingback` is removed, and WordPress generator/RSD/shortlink/REST/oEmbed/emoji head output is stripped.
- Performance hotfix `7c973b5` defers heavy media and quote embeds without removing premium visuals. Do not make the homepage hero video or WindowCAD iframes eager again unless there is a measured reason.
- Microsoft Clarity had unstyled/giant-image recordings because Clarity-like replay/resource fetches could receive the SiteGround/nginx `403 - Forbidden` HTML page instead of the real stylesheet. The live fix is theme-owned: Clarity plugins are removed, `inc\assets.php` adds `data-clarity-unmask="true"` to CSS/font/image resource links, and `inc\consent.php` injects `style#fenster-clarity-replay-css[data-clarity-unmask="true"]` after accepted consent and before loading `clarity.ms/tag/xi7rk1pic8`. Do not remove this inline replay CSS or reinstall the Clarity plugins without retesting recordings.
- Public tracking is gated by the theme consent layer in `inc\consent.php`. Do not re-add raw GTM, Clarity or Meta Pixel snippets in Insert Headers/Footers or plugin settings unless they remain blocked until consent.
- **Ad destination URLs are not carrying UTM parameters.** Nearly every attributed lead in the dashboard shows its source as "Direct or unknown", so paid traffic is invisible as paid and no channel can be credited with a lead. Nothing in the dashboard can recover a source that was never sent — fix the tagging on the ad URLs. Related: WindowCAD completions relay no `product_collection` or `price_amount`, so lead value and cost-per-lead cannot be computed at all.
- The Marketing Dashboard Website Tracker is also consent-gated. Its production code and API are hosted separately at `https://github.com/0riceisnice0-hash/Marketing-Dashboard`; deploy dashboard changes from that repository, not from this theme deployment. Read `WEBSITE-TRACKER.md` in that repository before changing the tracker or interpreting an outcome. Analytics consent permits opaque `FGV-…` visitor tracking and 30-minute `FG2-…` journeys. Non-consented activity is aggregate-only, environment-separated and cannot join a person. Completed lead relays require the shared signing secret and deterministic event IDs. Do not change `src\js\main.js`, `inc\consent.php`, `inc\website-tracking.php` or `inc\adminbase.php` in a way that sends rejected/no-choice browsing, form or WindowCAD events to an identified dashboard journey.
- WindowCAD URL attribution belongs in its separate **Tracking** field, never the office-owned **Reference** field. Analytics uses `FG2-…`; marketing-only attribution uses `FGA-…`; rejected/no-choice values are `rejected-cookies` / `cookie-consent-not-accepted`. Only `FG2-…` may create or join a dashboard journey.
- `test.fensterglazing.com` is intentionally Basic Auth protected to avoid a public duplicate. Username: `fenster`; password: `Fenster`. The test `.htaccess` also serves public `robots.txt` with `Allow: /` and uses a custom 401 handler so blocked test URLs return `X-Robots-Tag: noindex, nofollow, noarchive`; keep that combination so Google can drop already-discovered test URLs.
